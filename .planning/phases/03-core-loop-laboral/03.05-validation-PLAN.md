---
phase: 03-core-loop-laboral
plan: 05
type: execute
wave: 6
depends_on: [01, 02, 03, 04a, 04b]
files_modified:
  - nakama/test/laboral-test.sh
  - nakama/test/laboral-test.md
  - nakama/test/admin-curl-examples-laboral.md
  - .planning/INFRA-NOTES.md
  - .planning/VALIDATION.md
  - .planning/STATE.md
  - .planning/phases/03-core-loop-laboral/03-VALIDATION.md
autonomous: true
requirements:
  - AGT-01
  - AGT-02
  - AGT-03
  - AGT-04
  - AGT-05
  - PIB-01
  - PIB-02
  - PIB-03
  - PIB-04
  - PIB-05
  - PIB-06
  - PIB-07
  - JER-01
  - JER-02
  - JER-03
  - JER-04
  - ONB-05
  - ONB-06
tags:
  - phase-3
  - validation
  - test-harness
  - infra-notes

must_haves:
  truths:
    - "nakama/test/laboral-test.sh runs to completion against live Railway endpoint with PASS/FAIL/SKIP tallies and exit code 0 when all green"
    - "Full suite covers 19 invariants from 03-VALIDATION.md (idle/turno/recruit/rank/mesa/season/copy/tutorial-duration)"
    - "Quick mode (--quick) runs 3 highest-value smoke tests in <30s (LAB-IDLE-IDEMPOTENT + LAB-TURNO-IDEMPOTENT + LAB-RECRUIT-DAILY)"
    - "admin-curl-examples-laboral.md documents curl flows for admin_force_recruit_refresh + admin_grant_rep + admin_seed_ai_baseline"
    - "Vocabulary gate test LAB-COPY-VOCABULARY greps source for banned words and fails on any match"
    - "INFRA-NOTES.md has Phase 3 sections documenting 4 new collections, 13 RPCs added, 2 new leaderboards, AI baseline mechanism, idle accrual lazy compute, tutorial flow"
    - "VALIDATION.md has Phase 3 rows describing the 19 invariants + LAB-COPY-VOCABULARY gate + harness location"
    - "STATE.md updated to mark Phase 3 plans 03.01-04 complete + Phase 3 entering closing checklist phase"
  artifacts:
    - path: "nakama/test/laboral-test.sh"
      provides: "bash + curl + jq harness with 19 invariant tests + quick mode + admin operations"
      min_lines: 400
    - path: "nakama/test/laboral-test.md"
      provides: "Manual-only verification checklist (UI walkthroughs, Godot dev build steps) — companion to bash script"
      min_lines: 30
    - path: "nakama/test/admin-curl-examples-laboral.md"
      provides: "curl recipes for Phase 3 admin RPCs"
      min_lines: 50
    - path: ".planning/INFRA-NOTES.md"
      provides: "EXTENDED — Phase 3 architecture notes (4 collections, 13 RPCs, 2 leaderboards, AI baseline, tutorial flow)"
      contains: "Phase 3: Core Loop Laboral"
    - path: ".planning/VALIDATION.md"
      provides: "EXTENDED — Phase 3 invariant rows added to existing per-phase table"
      contains: "LAB-TURNO-IDEMPOTENT"
    - path: ".planning/STATE.md"
      provides: "Phase 3 status updated to complete; plan 03.05 marked done"
      contains: "Phase 3"
    - path: ".planning/phases/03-core-loop-laboral/03-VALIDATION.md"
      provides: "Frontmatter nyquist_compliant: true + Per-Task Verification Map populated"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "nakama/test/laboral-test.sh"
      to: "Live Railway endpoint /v2/rpc/*"
      via: "curl --max-time 15 against published RPCs; ADMIN_BEARER for admin RPCs; basic_auth for player RPCs"
      pattern: "curl.*v2/rpc"
    - from: ".planning/INFRA-NOTES.md"
      to: "All Phase 3 artifacts (helpers + RPCs + cron + UI)"
      via: "narrative sections describe how the pieces fit together for future phase planning"
      pattern: "Phase 3"
    - from: ".planning/STATE.md"
      to: "Phase 3 closing checklist"
      via: "marks 5/5 plans complete + lists carried-over items for Phase 4"
      pattern: "Phase 3.*COMPLETE"
---

<objective>
Ship the Phase 3 validation harness, runbook, infra documentation, and STATE update. After this plan, Phase 3 has the same observability + regression coverage as Phase 2: bash invariant suite (`laboral-test.sh` parallel to `heartbeat-test.sh`), admin curl recipes, INFRA-NOTES architecture summary, VALIDATION.md invariant table, and STATE.md status flip.

Purpose: Phase 4 (Combate Estratégico) planning can rely on Phase 3 being verified, observable, and documented. The bash harness becomes the regression gate before every Phase 4+ deploy.

Output:
- `nakama/test/laboral-test.sh` runs cleanly with all 19 invariants green (or documented skips for env-missing scenarios).
- `nakama/test/laboral-test.md` lists manual-only checks (Godot UI walkthroughs from plan 03.04 already covered, but doc references them).
- `nakama/test/admin-curl-examples-laboral.md` gives copy-paste curl flows for the 3 admin RPCs.
- INFRA-NOTES extended with Phase 3 sections (5 sections mirroring Phase 2's structure).
- VALIDATION.md extended with 18 Phase 3 invariant rows.
- STATE.md marked Phase 3 complete (or complete-with-deferral if any item rolls forward).
- 03-VALIDATION.md frontmatter `nyquist_compliant: true` set + Per-Task Verification Map populated.

Honoured locked decisions: every D-01..D-16 has at least one invariant test asserting its behavior. Vocabulary gate LAB-COPY-VOCABULARY enforces UI-SPEC §8.5 blacklist as a CI-style check. LAB-TUTORIAL-DURATION enforces ONB-05 (<10 min) as a server-log assertion (Phase 3 client captures elapsed_ms in TutorialScreen per plan 03.04b; plan 03.03 complete_tutorial logs `tutorial_duration_ms=<N>` on step 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/INFRA-NOTES.md
@.planning/VALIDATION.md
@.planning/phases/03-core-loop-laboral/03-CONTEXT.md
@.planning/phases/03-core-loop-laboral/03-RESEARCH.md
@.planning/phases/03-core-loop-laboral/03-VALIDATION.md
@.planning/phases/03-core-loop-laboral/03-01-SUMMARY.md
@.planning/phases/03-core-loop-laboral/03-02-SUMMARY.md
@.planning/phases/03-core-loop-laboral/03-03-SUMMARY.md
@.planning/phases/03-core-loop-laboral/03-04a-SUMMARY.md
@.planning/phases/03-core-loop-laboral/03-04b-SUMMARY.md
@CLAUDE.md

# Source patterns for bash harness:
@nakama/test/heartbeat-test.sh
@nakama/test/admin-curl-examples.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: laboral-test.sh — bash + curl invariant harness with 18 tests + --quick mode + admin recipes</name>
  <read_first>
    - nakama/test/heartbeat-test.sh (whole file — exact harness scaffolding pattern: bash boilerplate, pass/fail/skip helpers, basic_auth / console_auth, curl test pattern, 20 invariants)
    - nakama/test/admin-curl-examples.md (whole file — recipe doc template)
    - .planning/phases/03-core-loop-laboral/03-VALIDATION.md (invariants 1-18 numbered list — owner column TBD now to be set)
    - .planning/phases/03-core-loop-laboral/03-RESEARCH.md §Validation Architecture lines 1607-1675 (full test map by REQ-ID + additional internal invariants)
    - .planning/phases/03-core-loop-laboral/03-CONTEXT.md (D-01..D-16 — each decision needs at least one assertion)
    - .planning/phases/03-core-loop-laboral/03-PATTERNS.md §"laboral-test.sh" lines 1248-1291 (bash test pattern from heartbeat-test.sh)
    - .planning/phases/03-core-loop-laboral/03-UI-SPEC.md §8.5 (vocabulary blacklist for LAB-COPY-VOCABULARY test)
  </read_first>
  <files>nakama/test/laboral-test.sh, nakama/test/laboral-test.md, nakama/test/admin-curl-examples-laboral.md</files>
  <action>
    1) Create `nakama/test/laboral-test.sh`. Copy bash scaffolding from `heartbeat-test.sh` lines 1-25 verbatim (set -euo pipefail, NAKAMA_HOST/KEY env defaults, basic_auth/console_auth helpers, PASS/FAIL/SKIP counters, pass/fail/skip functions). Add an `--quick` flag parsing block. Implement these 19 game-logic invariants + 1 vocabulary gate (20 tests total — each in a clearly-delimited function or section, prefixed with the LAB-XXX id):

       **Core compute invariants:**
       - **LAB-IDLE-IDEMPOTENT**: register a test user → grant 5000 Plata via admin → recruit a pibe (tutorial bypass) → assign trapito → sleep 5s → call collect_idle twice within 1s of each other → assert second response `plata_credited <= 1` (idempotent).
       - **LAB-IDLE-CAP**: Hard to test live (12h elapsed). MOCK approach: use admin_grant_rep to set up a state where last_collected_at is 25h ago (requires admin storage write — for Phase 3 SKIP this test with `skip "12h cap test requires storage-write admin endpoint not in scope"` and document as manual test).
       - **LAB-IDLE-RATE-TRAPITO**: assign trapito → known elapsed seconds → assert `plata_credited == floor(rate * seconds / 3600 * multiplier)`. Tolerate ±1 for rounding.
       - **LAB-ENERGIA-REGEN**: get_roster twice with 1s sleep → assert `energia` field unchanged (regen too small to register on integer floor) OR for larger sleeps, assert energia increases per +5/h formula.

       **Turno invariants (D-03, D-04, D-06):**
       - **LAB-TURNO-WINDOW-GATE**: call submit_turno with arbitrary fixture_id that has no match_window → assert `error == "no_window"` OR `error == "window_not_active"`.
       - **LAB-TURNO-ENERGY-GATE**: with a pibe at energia 100, submit turno on a live test fixture (use admin_inject_test_fixture from Phase 2 first to create one) → 1 turno success → re-submit same with energia 60 → still works (≥30). Force energia <30 (consume via multiple turnos)... For Phase 3 simplicity: just test that submit_turno with valid pibes returns `ok:true` once. Energy-low path = SKIP or rely on RPC contract.
       - **LAB-TURNO-OUTPUT**: submit_turno with 2 pibes on a live test fixture → assert response `aguante_credited == 100` AND `reputacion_credited == 40` (50*2 and 20*2, D-06).
       - **LAB-TURNO-IDEMPOTENT**: submit_turno once → re-submit same fixture_id → assert second response `idempotent_replay == true` AND `aguante_credited` identical to first (no double-credit).

       **Recruit invariants (D-09, D-10, D-12):**
       - **LAB-RECRUIT-DAILY**: call `admin_force_recruit_refresh` twice in same UTC day → assert second response's audit log marker shows `regenerated: 0` OR the second response payload reports same `generated_date_art` (no-op on day match).
       - **LAB-RECRUIT-TRAIT-REDACT**: call get_recruit_pool → assert all `picks[].trait_2_hidden` are boolean `true` (not strings).
       - **LAB-RECRUIT-RACE**: spawn 5 concurrent recruit_pibe calls with the same pick_id (use `&` bash subshell or `xargs -P 5`) → tally responses → assert exactly 1 returns `ok:true` and the others return `pick_already_taken`. Pragmatic test: 5 concurrent should yield 1 ok + 4 errors.
       - **LAB-RECRUIT-COST**: rank=pibe user with 400 Plata attempts recruit (cost 500) → assert `error == "plata_insufficient"`.
       - **LAB-RECRUIT-CAP**: rank=pibe user recruits 2 successfully (tutorial pibe + 1 paid) → 3rd recruit attempt → assert `error == "lifetime_cap_reached"`.

       **Rank + Mesa invariants (D-13, D-14):**
       - **LAB-RANK-THRESHOLD-PROMOTE**: use admin_grant_rep to grant +500 Rep to a pibe-rank user → call get_roster → assert response `rank == "soldado"`.
       - **LAB-MESA-DEBOUNCE**: call get_barra_state twice within 5min → assert `barra_state.mesa_recompute_last_at` unchanged on second call (recompute not triggered within debounce window).
       - **LAB-VBC-LIDER-ONLY**: as rank=pibe user, call assign_profession with profession="hablar_cana" → assert `error == "lider_only"`.

       **Lider invariants (D-15):**
       - **LAB-LIDER-ELECTION**: trigger via admin or storage state where season transitions. Phase 3 SKIP if no admin endpoint to flip season status; document as manual via Nakama Console. Otherwise check `barra_state.lider.season_id` after season transition. Mark `skip` if Phase 3 lacks the admin trigger.

       **Tutorial invariants (ONB-05, ONB-06):**
       - **LAB-TUTORIAL-REWARD-ATOMIC**: register new user → complete_tutorial(step=6) → assert response contains `reward: {trapo, cantico}` → call again → assert `idempotent_replay: true` AND `tutorial_done: true` AND no second trapo grant (`aguantadero.trapos_robados.length == 1`).
       - **LAB-TUTORIAL-DURATION (NEW — ONB-05)**: register new user → call `complete_tutorial(step=6, elapsed_ms=<N>)` with an explicit elapsed_ms (test value e.g. 120000 = 2 min). Then SSH/curl the Nakama logs endpoint OR (preferred) read the local Nakama log file via Railway log streaming. Grep for the line `[complete_tutorial] user=<id> tutorial_done=true tutorial_duration_ms=<M>`. Parse M and assert `M < 600000` (10-minute ceiling per ONB-05). If logs are not programmatically accessible from the test harness environment, mark as SKIP with `skip "tutorial duration check requires Railway log access — verified manually in plan 03.04b human playthrough"`. The point is the harness DOCUMENTS the invariant; manual verification carries when automation is impractical. NEVER fail-soft on a numeric value > 600000 if the log line IS accessible.

       **Vocabulary + safety (CLAUDE.md tone, UI-SPEC §8.5):**
       - **LAB-COPY-VOCABULARY**: `grep -rE "\\b(ataque|raid|pelea|atacar|robar)\\b" nakama/src scripts/screens scripts/components scripts/autoloads | grep -v "^.*:.*#" | wc -l` must be 0. If non-zero, FAIL with first 3 matches printed.

    2) Implement `--quick` mode: when `$1 == "--quick"`, run only LAB-IDLE-IDEMPOTENT + LAB-TURNO-IDEMPOTENT + LAB-RECRUIT-DAILY + LAB-COPY-VOCABULARY (4 smoke tests, <30s total).

    3) Final tally + exit code:
       ```bash
       echo ""
       echo "=== Phase 3 Laboral Test Results ==="
       echo "PASS: $PASS  FAIL: $FAIL  SKIP: $SKIP"
       if [ "$FAIL" -gt 0 ]; then exit 1; else exit 0; fi
       ```

    4) Create `nakama/test/laboral-test.md` with manual-only checks complementary to bash:
       - UI walkthroughs from plan 03.04 (Tutorial timing, vocabulary in UI, visual rank badge animations, ConfirmationDialogs).
       - 12h idle cap (requires waiting 12h or admin storage edit).
       - Season transition Líder election (requires admin storage write).
       - Mesa Chica recompute hourly (requires waiting for cron OR Nakama Console "trigger leaderboard").
       - D-08 no-daily-caps audit: scan source for any "daily_cap" or "per_day_limit" guard in idle_accrual / collect_idle / submit_turno — confirm absence; throttle is natural (energía + 12h idle cap + skill grind only).
       - Android device build deferred to Phase 7.

    5) Create `nakama/test/admin-curl-examples-laboral.md` with curl recipes for:
       - admin_force_recruit_refresh (all clubs + specific club)
       - admin_grant_rep (test promote)
       - admin_seed_ai_baseline (with + without force)
       - Bash variable convention: `BASE`, `HTTP_KEY`, `ADMIN_BEARER` (same as Phase 2)

    Use bash 4+ syntax. Tolerate macOS bash 3 by avoiding associative arrays. Test helper variables: `TEST_EMAIL_PREFIX="test+p3-$(date +%s)"` to ensure unique users per run.
  </action>
  <verify>
    <automated>test -f nakama/test/laboral-test.sh && test -x nakama/test/laboral-test.sh 2>/dev/null || chmod +x nakama/test/laboral-test.sh; test -f nakama/test/laboral-test.md && test -f nakama/test/admin-curl-examples-laboral.md && bash -n nakama/test/laboral-test.sh && grep -q "LAB-IDLE-IDEMPOTENT" nakama/test/laboral-test.sh && grep -q "LAB-TURNO-IDEMPOTENT" nakama/test/laboral-test.sh && grep -q "LAB-RECRUIT-RACE" nakama/test/laboral-test.sh && grep -q "LAB-COPY-VOCABULARY" nakama/test/laboral-test.sh && grep -q "LAB-TUTORIAL-REWARD-ATOMIC" nakama/test/laboral-test.sh && grep -q "LAB-TUTORIAL-DURATION" nakama/test/laboral-test.sh && grep -q "LAB-VBC-LIDER-ONLY" nakama/test/laboral-test.sh && grep -q -- "--quick" nakama/test/laboral-test.sh && grep -q "admin_force_recruit_refresh" nakama/test/admin-curl-examples-laboral.md && grep -q "admin_grant_rep" nakama/test/admin-curl-examples-laboral.md && grep -q "admin_seed_ai_baseline" nakama/test/admin-curl-examples-laboral.md && echo "TASK1-PASS"</automated>
  </verify>
  <acceptance_criteria>
    - laboral-test.sh exists + is executable + passes `bash -n` syntax check
    - All 19 LAB-* invariants appear as labeled blocks in the script (including LAB-TUTORIAL-DURATION)
    - --quick mode supported
    - laboral-test.md exists with manual checks
    - admin-curl-examples-laboral.md exists with all 3 admin RPC recipes
    - `TASK1-PASS` printed
  </acceptance_criteria>
  <done>Harness syntactically valid. Manual run against live Railway endpoint yields `PASS: N FAIL: 0 SKIP: M` (skips are acceptable for env-dependent tests like 12h cap and season transition). Vocabulary gate green (0 banned matches in source). At least one full run captured + summary documented in 03-05-SUMMARY.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: INFRA-NOTES + VALIDATION + STATE + 03-VALIDATION.md frontmatter — Phase 3 documentation closure</name>
  <read_first>
    - .planning/INFRA-NOTES.md (read all — observe Phase 1/2 sections to mirror structure for Phase 3)
    - .planning/VALIDATION.md (read all — observe per-phase invariant table format)
    - .planning/STATE.md (read all — mirror Phase 2 complete-with-deferral table)
    - .planning/phases/03-core-loop-laboral/03-VALIDATION.md (frontmatter + tables that need final pop)
    - All 5 prior SUMMARY files (03-01-SUMMARY.md, 03-02-SUMMARY.md, 03-03-SUMMARY.md, 03-04a-SUMMARY.md, 03-04b-SUMMARY.md) — synthesize outcomes
    - .planning/phases/03-core-loop-laboral/03-RESEARCH.md §Architectural Responsibility Map lines 25-40 (for INFRA-NOTES architecture summary)
  </read_first>
  <files>.planning/INFRA-NOTES.md, .planning/VALIDATION.md, .planning/STATE.md, .planning/phases/03-core-loop-laboral/03-VALIDATION.md</files>
  <action>
    1) EXTEND `.planning/INFRA-NOTES.md` with a new top-level section `## Phase 3: Core Loop Laboral` (mirror Phase 2's structure). Include 5 subsections:

       **a) Storage Schema (Phase 3 additions)**
       - 4 new collections: `aguantaderos` (per-user singleton), `barra_state` (system-owned per club), `recruit_pool` (system-owned per club), `turnos` (per-user append-only, key=fixture_id).
       - Extensions: `pibes` migrated from single-key `main` to multi-record keyed by uuid; `players/profile` extended with `rank`, `plata`, `reputacion`, `vbc`, `tutorial_done`, `tutorial_step`, `pibes_recruited_total`, `pibes_migrated_at`, `faccion`, `aguante_contributed_total`, `cantico_unlocked`.
       - Meta keys: `KEY_RECRUIT_LOCK`, `KEY_AI_SEED_VERSION`, `KEY_MESA_DEBOUNCE_PREFIX`.

       **b) RPCs added (13 total)**
       - 4 read: get_roster, get_aguantadero, get_barra_state, get_recruit_pool.
       - 6 write: assign_profession, collect_idle, recruit_pibe, upgrade_aguantadero, submit_turno, complete_tutorial.
       - 3 admin: admin_force_recruit_refresh, admin_grant_rep, admin_seed_ai_baseline.
       - Total system RPC count now: 28 (15 prior + 13 Phase 3).

       **c) Scheduler additions**
       - 2 new cron leaderboards: bb_recruit_05_art (`0 8 * * *` UTC = 05:00 ART), bb_mesa_recompute_1h (`0 * * * *`).
       - Single dispatcher in main.ts unchanged — extended branches in onSchedulerLeaderboardReset.
       - seasons.ts hook: on status active→ended, walks barra_state, writes Líder per club.

       **d) Lazy compute pattern (RESEARCH summary)**
       - Idle Plata accrual computed on every collect_idle (write-bearing) AND projected on every get_roster (read-only). Skip stamp prevents double-credit.
       - Energía regen persisted on every get_roster.
       - Mesa Chica recomputed by hourly cron OR inline on get_barra_state when debounce window elapsed.
       - AI baseline Rep grows via `barra_age_days * 30 * division_mult`; recomputed lazily on barra_state read.

       **e) Tone + safety gates (App Store)**
       - Vocabulary blacklist enforced by LAB-COPY-VOCABULARY in laboral-test.sh.
       - AI Mesa/Líder names canned ("Capo de la Barra #N") — NO LLM until Phase 5.
       - Procedural pibe names from hardcoded APODOS + NOMBRES lists; no real barra leader names.
       - All resource mutations server-authoritative; client never computes economy.

    2) EXTEND `.planning/VALIDATION.md` with 19 Phase 3 rows. Follow existing row format. For each LAB-* invariant (incl. LAB-TUTORIAL-DURATION which covers ONB-05): ID, requirement coverage (REQ-IDs), test type (smoke/invariant/manual), automated command (`bash nakama/test/laboral-test.sh --invariant LAB-XXX` or `--quick` for the smoke ones), file location, status (auto-set green after first successful run; pending until then).

    3) UPDATE `.planning/STATE.md`:
       - Add Phase 3 to the Progress table: status changes from `⏳ Pending` → `✅ Complete-with-deferral` (4 specific items roll to Phase 7 — see deferral subsection below).
       - Add "Phase 3 — Closing Checklist" section mirroring Phase 2's at lines 107-115:
         - All 6 plans accounted for (03.01, 03.02, 03.03, 03.04a, 03.04b, 03.05).
         - All commits pushed to GitHub main.
         - Railway auto-redeployed.
         - Production smoke test results.
         - Run `bash nakama/test/laboral-test.sh` when convenient — full 19-invariant verification.
         - Android device build deferred to Phase 7 (same as 02-07).
       - Update top frontmatter `progress.completed_plans` from 13 → 19 (13 Phase 1+2 + 6 Phase 3) and `progress.percent` recompute.
       - Update Recent Activity log with a Phase 3 closing line referencing today's date.
       - **Add an explicit "Phase 3 → Phase 7 Deferred" subsection** with these four items (each line must say "deferred to Phase 7 (Hardening) — admin endpoint not in Phase 3 scope" or equivalent):
         - **LAB-IDLE-CAP** — 12h elapsed simulation needs admin storage-write endpoint. Deferred to Phase 7 (Hardening) — admin endpoint not in Phase 3 scope.
         - **LAB-IDLE-RATE-TRAPITO partial** — known-elapsed-seconds rounding tolerance acceptable for v1; full rate-table assertion needs admin storage-write to seed deterministic state. Deferred to Phase 7 (Hardening) — admin endpoint not in Phase 3 scope.
         - **LAB-TURNO-ENERGY-GATE energy-low path** — testing the <30-energía rejection branch needs admin energy-set endpoint. Deferred to Phase 7 (Hardening) — admin endpoint not in Phase 3 scope.
         - **LAB-LIDER-ELECTION** — requires simulated season-end clock advancement (or admin-flip of season state). Deferred to Phase 7 (Hardening) — admin endpoint not in Phase 3 scope.
       - Mark other carried-forward items: release_pibe RPC → Phase 4 (permadeath handles loss).

    4) UPDATE `.planning/phases/03-core-loop-laboral/03-VALIDATION.md`:
       - Frontmatter: `nyquist_compliant: false` → `true`; `wave_0_complete: false` → `true`.
       - Populate the Per-Task Verification Map table (currently TBD): for each plan 03.01-04 task, list its REQ-IDs + the LAB-* invariant(s) that cover it.
       - Populate Plan Owner column in Invariants table (which plan implemented each).
       - Validation Sign-Off checklist: mark all [ ] → [x] where applicable.
       - **Approval**: pending → approved (executor + auto-checker).
  </action>
  <verify>
    <automated>grep -q "Phase 3: Core Loop Laboral" .planning/INFRA-NOTES.md && grep -q "bb_recruit_05_art\|bb_mesa_recompute_1h" .planning/INFRA-NOTES.md && grep -q "LAB-TURNO-IDEMPOTENT\|LAB-RECRUIT-RACE" .planning/VALIDATION.md && grep -q "LAB-TUTORIAL-DURATION" .planning/VALIDATION.md && grep -q "Phase 3 → Phase 7 Deferred\|Phase 3 to Phase 7 Deferred" .planning/STATE.md && grep -q "Phase 3.*✅\|Phase 3.*Complete" .planning/STATE.md && grep -q "Phase 3 — Closing\|Phase 3.*Closing\|Phase 3.*closing" .planning/STATE.md && grep -q "nyquist_compliant: true" .planning/phases/03-core-loop-laboral/03-VALIDATION.md && echo "TASK2-PASS"</automated>
  </verify>
  <acceptance_criteria>
    - INFRA-NOTES.md has new Phase 3 section with 5 subsections
    - VALIDATION.md has Phase 3 invariant rows (19 total) including LAB-TURNO-IDEMPOTENT + LAB-RECRUIT-RACE + LAB-TUTORIAL-DURATION
    - STATE.md shows Phase 3 status as Complete-with-deferral + closing checklist + Recent Activity updated + Phase 3 → Phase 7 Deferred subsection with 4 named items
    - 03-VALIDATION.md frontmatter nyquist_compliant: true
    - `TASK2-PASS` printed
  </acceptance_criteria>
  <done>All documentation files updated. STATE.md accurately reflects Phase 3 closure. Future Phase 4 planner reads INFRA-NOTES + VALIDATION + STATE and has full context to begin combate planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laboral-test.sh → Live Railway endpoint | Test harness uses real Nakama API; ADMIN_BEARER required for admin operations |
| Test fixtures → production data | Tests create unique users per run (timestamp-suffixed emails); never reuse identities |
| Vocabulary gate → source code | grep-based audit fails build on banned terms in committed code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-VAL-01 | Tampering (test integrity) | Test creates real users that pollute production | accept | Users named `test+p3-{timestamp}@example.com` — easily filterable. Production has no real users in Phase 3 (pre-soft-launch). Bulk-delete via admin RPC at any time. |
| T-3-VAL-02 | Information disclosure | ADMIN_BEARER printed in test logs | mitigate | laboral-test.sh never echoes the bearer — only passes via curl `-H "Authorization: Bearer $ADMIN_BEARER"`. Test failure messages show response body only. |
| T-3-VAL-03 | DoS (rate limit) | Concurrent recruit race test (LAB-RECRUIT-RACE) | accept | 5 concurrent calls is well under any rate limit; pragmatic for race verification. Documented in test as deliberate concurrency. |
| T-3-VAL-04 | Regulatory (App Store) | Banned vocabulary slips into committed code | mitigate | LAB-COPY-VOCABULARY runs in every test execution; fails if banned terms present. Recommend manual `grep` as pre-commit habit. |
| T-3-VAL-05 | Repudiation | INFRA-NOTES drift from actual code | mitigate | Documentation generated from SUMMARY files; Phase 4 planner cross-references actual files. Manual review at Phase 3 closing checkpoint. |
</threat_model>

<verification>
After both tasks complete:

1. **Harness syntax**: `bash -n nakama/test/laboral-test.sh` exits 0.

2. **Full suite manual run**:
   ```bash
   export NAKAMA_HOST=nakama-production-7ea8.up.railway.app
   export NAKAMA_KEY=aee9c099d52a6c22f52fb8bc9f4b72d9
   export HTTP_KEY=defaulthttpkey
   export ADMIN_BEARER=<rotate before run>
   bash nakama/test/laboral-test.sh
   ```
   Expected: `PASS: N FAIL: 0 SKIP: M` where N ≥ 12 (most invariants run cleanly), M ≤ 5 (env-dependent skips documented — 4 are the Phase 7 deferred items listed in STATE.md; LAB-TUTORIAL-DURATION may skip if Railway log access unavailable from test harness).

3. **Quick mode**: `bash nakama/test/laboral-test.sh --quick` completes in <30s with 4 tests run.

4. **Vocabulary gate**: included in suite; if banned terms exist anywhere in source, full suite fails with FAIL count > 0.

5. **Documentation reads cleanly**: open INFRA-NOTES.md, scroll to Phase 3 section — narrative makes sense in isolation (Phase 4 planner test). Open VALIDATION.md — 18 new rows present. Open STATE.md — Phase 3 status visibly complete.

6. **03-VALIDATION.md**: opens, frontmatter shows nyquist_compliant: true, Per-Task Verification Map populated, sign-off section ticked.
</verification>

<success_criteria>
- 19-invariant bash harness ships + runs (including LAB-TUTORIAL-DURATION).
- --quick mode covers 4 highest-value tests in <30s.
- 3 admin RPCs documented in admin-curl-examples-laboral.md.
- INFRA-NOTES Phase 3 section complete (5 subsections).
- VALIDATION.md extended with 19 invariant rows.
- STATE.md flips Phase 3 to Complete + closing checklist + Recent Activity updated.
- 03-VALIDATION.md frontmatter nyquist_compliant: true.
- Vocabulary gate LAB-COPY-VOCABULARY passes (0 banned matches).
- All 6 Phase 3 plans accounted for in STATE.md (03.01, 03.02, 03.03, 03.04a, 03.04b, 03.05).
- Phase 7 deferral subsection in STATE.md documents 4 specific items (LAB-IDLE-CAP, LAB-IDLE-RATE-TRAPITO partial, LAB-TURNO-ENERGY-GATE energy-low path, LAB-LIDER-ELECTION).
- Phase 4 planner has full context (research + roadmap + state + infra + validation) to begin combate planning.
- ADMIN_BEARER rotation reminder carried forward.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-loop-laboral/03-05-SUMMARY.md` per template at $HOME/.claude/get-shit-done/templates/summary.md. Include:
- Final bash test suite results (PASS/FAIL/SKIP counts).
- Documentation diff summary (INFRA-NOTES + VALIDATION + STATE lines added/changed).
- Phase 3 closing checklist status (5 plans complete, deferrals noted).
- Pointer to Phase 4 planning (next: `/gsd-plan-phase 4` Combate Estratégico).
- Any items rolled into deferral table for Phase 7 (e.g., 12h idle cap automated test, release_pibe RPC, Android device build).
</output>
