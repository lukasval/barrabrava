---
phase: 03-core-loop-laboral
plan: 04a
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - scripts/autoloads/AppTheme.gd
  - scripts/autoloads/NakamaService.gd
  - scripts/autoloads/PlayerStore.gd
  - scripts/autoloads/FlowRouter.gd
  - scripts/components/PibeCard.gd
  - scenes/components/PibeCard.tscn
  - scripts/components/RecruitCard.gd
  - scenes/components/RecruitCard.tscn
  - scripts/components/ResourceWidget.gd
  - scenes/components/ResourceWidget.tscn
  - scripts/components/RankBadge.gd
  - scenes/components/RankBadge.tscn
  - scripts/components/TraitChip.gd
  - scenes/components/TraitChip.tscn
  - scripts/components/EnergiaBar.gd
  - scenes/components/EnergiaBar.tscn
  - scripts/components/ProfessionIcon.gd
  - scenes/components/ProfessionIcon.tscn
  - scripts/components/SkillProgressRing.gd
  - scenes/components/SkillProgressRing.tscn
  - scripts/components/TurnoModal.gd
  - scenes/TurnoModal.tscn
autonomous: true
requirements:
  - AGT-01
  - AGT-02
  - AGT-03
  - AGT-05
  - PIB-01
  - PIB-02
  - PIB-03
  - PIB-04
  - PIB-06
  - PIB-07
  - JER-01
  - JER-02
  - JER-03
tags:
  - phase-3
  - godot
  - ui
  - laboral
  - foundation

must_haves:
  truths:
    - "AppTheme.gd exports Phase 3 tokens: resource tints (RES_PLATA, RES_AGUANTE, RES_REPUTACION, RES_VBC), rank palette (RANK_PIBE..LIDER), profession palette (PROF_TRAPITO..HABLAR_CANA, PROF_SIN_LABURO), trait sentiment (TRAIT_POSITIVE/NEGATIVE/NEUTRAL), energy thresholds (ENERGIA_FULL/MID/LOW/EMPTY), and FONT_HEADING := 20 (Phase 3 override drops Phase 1's 22)"
    - "NakamaService.gd exports 10 new async wrappers (get_roster, get_aguantadero, get_barra_state, get_recruit_pool, assign_profession, collect_idle, recruit_pibe, upgrade_aguantadero, submit_turno, complete_tutorial) following the WR-09 dict-guard pattern"
    - "PlayerStore.gd caches pibes Array, aguantadero Dictionary, recruit_pool Dictionary, rank, plata, reputacion, vbc, tutorial_done, tutorial_step, roster_cap; emits roster_updated, resources_updated, aguantadero_updated signals"
    - "FlowRouter.gd adds go_roster, go_recruit, go_pibe_detail(pibe_id), go_profession_assign(pibe_id), go_aguantadero, go_tutorial, go_post_pibe_create (tutorial_done gate), tutorial_advance(step)"
    - "9 reusable components exist (PibeCard, RecruitCard, ResourceWidget, RankBadge, TraitChip, EnergiaBar, ProfessionIcon, SkillProgressRing, TurnoModal) — each .gd + .tscn pair, instancing tested in-editor"
    - "No autoload or component .gd file contains banned vocabulary: 'ataque', 'raid', 'pelea', 'atacar', 'robar' (App-Store gate — UI-SPEC §8.5 blacklist)"
  artifacts:
    - path: "scripts/autoloads/AppTheme.gd"
      provides: "Extended with Phase 3 color/typography tokens"
      contains: "RES_PLATA"
    - path: "scripts/autoloads/NakamaService.gd"
      provides: "10 new RPC wrappers"
      contains: "func submit_turno"
    - path: "scripts/autoloads/PlayerStore.gd"
      provides: "Cache + signals for pibes/aguantadero/recruit/resources/tutorial state"
      contains: "signal roster_updated"
    - path: "scripts/autoloads/FlowRouter.gd"
      provides: "6+ new go_ helpers + tutorial_advance orchestrator + tutorial gate"
      contains: "go_post_pibe_create"
    - path: "scripts/components/PibeCard.gd"
      provides: "Card 120px height, avatar + traits + energia mini + rank badge — set_pibe(Dictionary) setter, tapped(pibe_id) signal"
      contains: "set_pibe"
    - path: "scripts/components/RecruitCard.gd"
      provides: "Card 180px, hidden trait_2 placeholder '?', rank-gate visual state"
      contains: "set_pick"
    - path: "scripts/components/ResourceWidget.gd"
      provides: "VBox 80x60, glyph tile + numeric value, set_value(int) with Spanish-locale thousands separator"
      contains: "set_value"
    - path: "scripts/components/RankBadge.gd"
      provides: "Bg colored by rank, set_rank(name) setter, promotion flash A-07"
      contains: "set_rank"
    - path: "scripts/components/TraitChip.gd"
      provides: "Sentiment border + label, hidden variant for trait_2"
      contains: "set_trait"
    - path: "scripts/components/EnergiaBar.gd"
      provides: "ProgressBar tinted by AppTheme.get_energia_color; regular + mini variants"
      contains: "get_energia_color"
    - path: "scripts/components/ProfessionIcon.gd"
      provides: "Tile colored per profession, inline 24x24 and 40x40 variants"
      contains: "set_profession"
    - path: "scripts/components/SkillProgressRing.gd"
      provides: "Custom _draw() arc, level number centered"
      contains: "_draw"
    - path: "scripts/components/TurnoModal.gd"
      provides: "Modal multi-select pibes + aporte estimado + confirm CTA"
      contains: "submit_turno"
  key_links:
    - from: "scripts/autoloads/NakamaService.gd"
      to: "AuthManager.session + Nakama RPC API"
      via: "Each wrapper guards is_authenticated(), awaits rpc_async, applies WR-09 dict-guard, returns {ok, data} or {ok:false, error}"
      pattern: "rpc_async\\("
    - from: "scripts/components/TurnoModal.gd"
      to: "scripts/autoloads/NakamaService.gd::submit_turno"
      via: "Confirmar CTA calls submit_turno(fixture_id, pibe_ids); on success closes modal"
      pattern: "submit_turno\\("
    - from: "scripts/components/RankBadge.gd"
      to: "AppTheme.RANK_*"
      via: "set_rank(name) swaps cached StyleBoxFlat from AppTheme palette"
      pattern: "RANK_PIBE|RANK_SOLDADO|RANK_CAPO|RANK_MESA|RANK_LIDER"
---

<objective>
Ship the Godot foundation surface for Phase 3 Core Loop Laboral: 4 autoload extensions (AppTheme, NakamaService, PlayerStore, FlowRouter) and 9 reusable components (PibeCard, RecruitCard, ResourceWidget, RankBadge, TraitChip, EnergiaBar, ProfessionIcon, SkillProgressRing, TurnoModal).

Purpose: This plan delivers the building blocks. Plan 03.04b consumes them in HomeScreen extension + 6 new screens + the tutorial state machine. The split keeps each plan within the 15-file modification budget and isolates the human-verify checkpoint to the screen wiring stage.

Output: All 22 files (4 autoloads + 18 component .gd/.tscn pairs) parse cleanly in Godot 4.3. Vocabulary audit passes (zero banned terms in scripts/components/ or scripts/autoloads/). Project still boots from AuthScreen — no regression.

Honoured locked decisions:
- D-04 (Energía thresholds + display)
- D-06 (Aporte estimado label in TurnoModal: +50 Aguante × N · +20 Rep × N)
- D-07 (Hablar cana profession color palette + label)
- D-10 (RecruitCard renders trait_2 as "?" pre-recruit)
- D-13 (RankBadge promotion flash hook for rank-up animation A-07)
- D-14 (Mesa palette + label-only rendering)
- D-16 (Faccion palette + label-only)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-core-loop-laboral/03-CONTEXT.md
@.planning/phases/03-core-loop-laboral/03-RESEARCH.md
@.planning/phases/03-core-loop-laboral/03-PATTERNS.md
@.planning/phases/03-core-loop-laboral/03-UI-SPEC.md
@.planning/phases/03-core-loop-laboral/03-01-SUMMARY.md
@.planning/phases/03-core-loop-laboral/03-02-SUMMARY.md
@.planning/phases/03-core-loop-laboral/03-03-SUMMARY.md
@CLAUDE.md

# Source autoloads + components to mirror:
@scripts/autoloads/AppTheme.gd
@scripts/autoloads/NakamaService.gd
@scripts/autoloads/PlayerStore.gd
@scripts/autoloads/FlowRouter.gd
@scripts/autoloads/AuthManager.gd
@scripts/components/ClubCard.gd
@scripts/components/NavButton.gd
@scripts/components/ChipButton.gd
@scenes/components/ClubCard.tscn

<interfaces>
<!-- Wrapper signatures + component contracts. Executor extends/creates per UI-SPEC. -->

## AppTheme.gd additions (UI-SPEC §3b + §4)
```gdscript
# Append after existing tokens. NOTE: FONT_HEADING := 22 is REPLACED with 20 (UI-SPEC §0 OVERRIDE).
# Keep existing FONT_DISPLAY := 28, FONT_BODY := 16, FONT_LABEL := 14.
# If existing FONT_HEADING = 22 present, change its value to 20 and add comment:
#   "[OVERRIDE Phase 1] 22 -> 20; serves dual Heading + Numeric Counter role."

# Phase 3 — Resource tints (§4.6)
const RES_PLATA       := Color(0.984, 0.749, 0.141, 1)  # #FBBF24 gold
const RES_AGUANTE     := Color(0.839, 0.157, 0.157, 1)  # #D62828 accent (shared)
const RES_REPUTACION  := Color(0.659, 0.333, 0.969, 1)  # #A855F7 purple
const RES_VBC         := Color(0.580, 0.639, 0.722, 1)  # #94A3B8 steel

# Phase 3 — Rank palette (§4.2)
const RANK_PIBE     := Color(0.612, 0.639, 0.686, 1)  # #9CA3AF
const RANK_SOLDADO  := Color(0.231, 0.510, 0.965, 1)  # #3B82F6
const RANK_CAPO     := Color(0.659, 0.333, 0.969, 1)  # #A855F7
const RANK_MESA     := Color(0.961, 0.620, 0.043, 1)  # #F59E0B
const RANK_LIDER    := Color(0.984, 0.749, 0.141, 1)  # #FBBF24

# Phase 3 — Profession palette (§4.3)
const PROF_TRAPITO     := Color(0.024, 0.714, 0.831, 1)  # #06B6D4
const PROF_VENDEDOR    := Color(0.133, 0.773, 0.369, 1)  # #22C55E
const PROF_PATOVICA    := Color(0.937, 0.267, 0.267, 1)  # #EF4444
const PROF_REMISERO    := Color(0.918, 0.702, 0.031, 1)  # #EAB308
const PROF_HABLAR_CANA := Color(0.580, 0.639, 0.722, 1)  # #94A3B8
const PROF_SIN_LABURO  := Color(0.239, 0.239, 0.239, 1)  # #3D3D3D

# Phase 3 — Trait sentiment border (§4.4)
const TRAIT_POSITIVE := Color(0.063, 0.725, 0.506, 1)  # #10B981
const TRAIT_NEGATIVE := Color(0.976, 0.451, 0.086, 1)  # #F97316
const TRAIT_NEUTRAL  := Color(0.239, 0.239, 0.239, 1)  # #3D3D3D

# Phase 3 — Energía bar thresholds (§4.5)
const ENERGIA_FULL  := Color(0.133, 0.773, 0.369, 1)  # #22C55E (70-100)
const ENERGIA_MID   := Color(0.918, 0.702, 0.031, 1)  # #EAB308 (30-69)
const ENERGIA_LOW   := Color(0.976, 0.451, 0.086, 1)  # #F97316 (1-29)
const ENERGIA_EMPTY := Color(0.627, 0.627, 0.627, 1)  # #A0A0A0 (0)

# Resource-tint lookup helper (used by ResourceWidget).
func get_resource_color(name: String) -> Color:
  match name:
    "plata":      return RES_PLATA
    "aguante":    return RES_AGUANTE
    "reputacion": return RES_REPUTACION
    "vbc":        return RES_VBC
    _:            return TEXT_PRIMARY

# Energía threshold lookup (used by EnergiaBar).
func get_energia_color(value: int) -> Color:
  if value <= 0: return ENERGIA_EMPTY
  if value < 30: return ENERGIA_LOW
  if value < 70: return ENERGIA_MID
  return ENERGIA_FULL
```

## NakamaService.gd additions — 10 RPC wrappers
```gdscript
# Append after existing wrappers (lines ~111+). Each follows the WR-09 pattern from
# get_current_window (lines 99-111): auth guard, JSON payload, await rpc_async,
# exception check, dict typeof guard, return {ok, data} or {ok:false, error}.

func get_roster() -> Dictionary:
  if not AuthManager.is_authenticated():
    return {"ok": false, "error": "not_authenticated"}
  var resp = await client.rpc_async(AuthManager.session, "get_roster", "")
  if resp.is_exception():
    return {"ok": false, "error": str(resp.get_exception().message)}
  var data = JSON.parse_string(resp.payload)
  if typeof(data) != TYPE_DICTIONARY:
    return {"ok": false, "error": "invalid_response"}
  return {"ok": true, "data": data}

func get_aguantadero() -> Dictionary:
  if not AuthManager.is_authenticated():
    return {"ok": false, "error": "not_authenticated"}
  var resp = await client.rpc_async(AuthManager.session, "get_aguantadero", "")
  if resp.is_exception():
    return {"ok": false, "error": str(resp.get_exception().message)}
  var data = JSON.parse_string(resp.payload)
  if typeof(data) != TYPE_DICTIONARY: return {"ok": false, "error": "invalid_response"}
  return {"ok": true, "data": data}

func get_barra_state(club_id: String = "") -> Dictionary:
  if not AuthManager.is_authenticated():
    return {"ok": false, "error": "not_authenticated"}
  var payload = JSON.stringify({"club_id": club_id}) if club_id != "" else ""
  var resp = await client.rpc_async(AuthManager.session, "get_barra_state", payload)
  if resp.is_exception():
    return {"ok": false, "error": str(resp.get_exception().message)}
  var data = JSON.parse_string(resp.payload)
  if typeof(data) != TYPE_DICTIONARY: return {"ok": false, "error": "invalid_response"}
  return {"ok": true, "data": data}

func get_recruit_pool() -> Dictionary:
  if not AuthManager.is_authenticated():
    return {"ok": false, "error": "not_authenticated"}
  var resp = await client.rpc_async(AuthManager.session, "get_recruit_pool", "")
  if resp.is_exception():
    return {"ok": false, "error": str(resp.get_exception().message)}
  var data = JSON.parse_string(resp.payload)
  if typeof(data) != TYPE_DICTIONARY: return {"ok": false, "error": "invalid_response"}
  return {"ok": true, "data": data}

func assign_profession(pibe_id: String, profession) -> Dictionary:
  # profession may be String or null (rest mode)
  if not AuthManager.is_authenticated():
    return {"ok": false, "error": "not_authenticated"}
  var payload = JSON.stringify({"pibe_id": pibe_id, "profession": profession})
  var resp = await client.rpc_async(AuthManager.session, "assign_profession", payload)
  if resp.is_exception():
    return {"ok": false, "error": str(resp.get_exception().message)}
  var data = JSON.parse_string(resp.payload)
  if typeof(data) != TYPE_DICTIONARY: return {"ok": false, "error": "invalid_response"}
  return {"ok": true, "data": data}

func collect_idle(pibe_id: String = "") -> Dictionary:
  if not AuthManager.is_authenticated():
    return {"ok": false, "error": "not_authenticated"}
  var payload_dict := {}
  if pibe_id != "": payload_dict["pibe_id"] = pibe_id
  var payload = JSON.stringify(payload_dict)
  var resp = await client.rpc_async(AuthManager.session, "collect_idle", payload)
  if resp.is_exception():
    return {"ok": false, "error": str(resp.get_exception().message)}
  var data = JSON.parse_string(resp.payload)
  if typeof(data) != TYPE_DICTIONARY: return {"ok": false, "error": "invalid_response"}
  return {"ok": true, "data": data}

func recruit_pibe(pick_id: String, tutorial: bool = false) -> Dictionary:
  if not AuthManager.is_authenticated():
    return {"ok": false, "error": "not_authenticated"}
  var payload_dict := {"pick_id": pick_id}
  if tutorial: payload_dict["tutorial"] = true
  var payload = JSON.stringify(payload_dict)
  var resp = await client.rpc_async(AuthManager.session, "recruit_pibe", payload)
  if resp.is_exception():
    return {"ok": false, "error": str(resp.get_exception().message)}
  var data = JSON.parse_string(resp.payload)
  if typeof(data) != TYPE_DICTIONARY: return {"ok": false, "error": "invalid_response"}
  return {"ok": true, "data": data}

func upgrade_aguantadero(target_level: int) -> Dictionary:
  if not AuthManager.is_authenticated():
    return {"ok": false, "error": "not_authenticated"}
  var payload = JSON.stringify({"target_level": target_level})
  var resp = await client.rpc_async(AuthManager.session, "upgrade_aguantadero", payload)
  if resp.is_exception():
    return {"ok": false, "error": str(resp.get_exception().message)}
  var data = JSON.parse_string(resp.payload)
  if typeof(data) != TYPE_DICTIONARY: return {"ok": false, "error": "invalid_response"}
  return {"ok": true, "data": data}

func submit_turno(fixture_id: String, pibe_ids: Array) -> Dictionary:
  if not AuthManager.is_authenticated():
    return {"ok": false, "error": "not_authenticated"}
  var payload = JSON.stringify({"fixture_id": fixture_id, "pibe_ids": pibe_ids})
  var resp = await client.rpc_async(AuthManager.session, "submit_turno", payload)
  if resp.is_exception():
    return {"ok": false, "error": str(resp.get_exception().message)}
  var data = JSON.parse_string(resp.payload)
  if typeof(data) != TYPE_DICTIONARY: return {"ok": false, "error": "invalid_response"}
  return {"ok": true, "data": data}

func complete_tutorial(step: int, elapsed_ms: int = 0) -> Dictionary:
  # elapsed_ms is total tutorial duration captured by TutorialScreen on step 1;
  # passed unmodified on every step so the server logs the final value at step 6.
  # Server uses elapsed_ms only for telemetry (LAB-TUTORIAL-DURATION invariant).
  if not AuthManager.is_authenticated():
    return {"ok": false, "error": "not_authenticated"}
  var payload_dict := {"step": step}
  if elapsed_ms > 0:
    payload_dict["elapsed_ms"] = elapsed_ms
  var payload = JSON.stringify(payload_dict)
  var resp = await client.rpc_async(AuthManager.session, "complete_tutorial", payload)
  if resp.is_exception():
    return {"ok": false, "error": str(resp.get_exception().message)}
  var data = JSON.parse_string(resp.payload)
  if typeof(data) != TYPE_DICTIONARY: return {"ok": false, "error": "invalid_response"}
  return {"ok": true, "data": data}
```

## PlayerStore.gd additions
```gdscript
# Append after existing fields/signals. Reset all in clear().

signal roster_updated
signal resources_updated
signal aguantadero_updated
signal recruit_pool_updated
signal tutorial_advanced

# Phase 3 resources + rank
var rank: String = "pibe"
var plata: int = 0
var reputacion: int = 0
var vbc: int = 0
var aguante_contributed_total: int = 0

# Phase 3 tutorial
var tutorial_done: bool = false
var tutorial_step: int = 0
var cantico_unlocked: String = ""

# Phase 3 roster + aguantadero
var pibes: Array = []
var aguantadero: Dictionary = {}
var recruit_pool: Dictionary = {}
var roster_cap: int = 5

# Phase 3 faccion (D-16 label only)
var faccion: String = ""

func clear() -> void:
  # ... preserve existing reset lines, then:
  rank = "pibe"
  plata = 0
  reputacion = 0
  vbc = 0
  aguante_contributed_total = 0
  tutorial_done = false
  tutorial_step = 0
  cantico_unlocked = ""
  pibes.clear()
  aguantadero.clear()
  recruit_pool.clear()
  roster_cap = 5
  faccion = ""
  profile_cleared.emit()

# New helper — refresh from server
func refresh_resources_and_roster() -> void:
  var roster_resp = await NakamaService.get_roster()
  if roster_resp.get("ok", false):
    var d = roster_resp.get("data", {})
    pibes = d.get("pibes", [])
    roster_cap = int(d.get("roster_cap", 5))
    rank = str(d.get("rank", "pibe"))
    roster_updated.emit()
  var agu_resp = await NakamaService.get_aguantadero()
  if agu_resp.get("ok", false):
    aguantadero = agu_resp.get("data", {}).get("aguantadero", {})
    aguantadero_updated.emit()
  # Resources live on profile; load_from_server already reads profile,
  # but for delta refresh after collect_idle/submit_turno we re-call.
  # Pattern: caller updates plata/reputacion/vbc directly from RPC response
  # (collect_idle returns plata_credited; submit_turno returns rep_credited),
  # then emits resources_updated.
  resources_updated.emit()
```

## FlowRouter.gd additions
```gdscript
# Append after existing nav helpers. Use scene paths from new screen tscns.

func go_roster() -> void: go_to("res://scenes/RosterScreen.tscn")
func go_recruit() -> void: go_to("res://scenes/RecruitScreen.tscn")
func go_aguantadero() -> void: go_to("res://scenes/AguantaderoScreen.tscn")
func go_tutorial() -> void: go_to("res://scenes/TutorialScreen.tscn")

func go_pibe_detail(pibe_id: String) -> void:
  # Pass pibe_id via singleton var (Godot scene-args limitation).
  PlayerStore.set_meta("nav_pibe_id", pibe_id)
  go_to("res://scenes/PibeDetailScreen.tscn")

func go_profession_assign(pibe_id: String) -> void:
  PlayerStore.set_meta("nav_pibe_id", pibe_id)
  go_to("res://scenes/ProfessionAssignScreen.tscn")

# Post-pibe-create gate (ONB-05 tutorial entry).
func go_post_pibe_create() -> void:
  if PlayerStore.tutorial_done:
    go_home()
  else:
    go_tutorial()

# Tutorial step orchestrator — called by TutorialScreen step CTAs.
# elapsed_ms is the tutorial duration captured by TutorialScreen on step 1,
# forwarded to NakamaService.complete_tutorial for the LAB-TUTORIAL-DURATION
# server-side telemetry log line.
func tutorial_advance(step: int, elapsed_ms: int = 0) -> void:
  var resp = await NakamaService.complete_tutorial(step, elapsed_ms)
  if resp.get("ok", false):
    var data = resp.get("data", {})
    PlayerStore.tutorial_step = int(data.get("step", step))
    PlayerStore.tutorial_done = bool(data.get("tutorial_done", false))
    if data.has("reward"):
      var reward = data.get("reward", {})
      PlayerStore.cantico_unlocked = str(reward.get("cantico", ""))
    PlayerStore.tutorial_advanced.emit()
    if PlayerStore.tutorial_done:
      go_home()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 4 autoload extensions (AppTheme, NakamaService, PlayerStore, FlowRouter) + 9 reusable components</name>
  <read_first>
    - scripts/autoloads/AppTheme.gd (read all — extend with Phase 3 tokens; OVERRIDE FONT_HEADING 22→20 per UI-SPEC §0)
    - scripts/autoloads/NakamaService.gd (read all — append 10 wrappers after existing get_current_window / register_fcm_token block)
    - scripts/autoloads/PlayerStore.gd (read all — append signals + fields + clear() reset + refresh_resources_and_roster helper)
    - scripts/autoloads/FlowRouter.gd (read all — append 6 new go_ helpers + tutorial_advance orchestrator + go_post_pibe_create gate)
    - scripts/autoloads/AuthManager.gd (verify is_authenticated() + session API)
    - scripts/components/ClubCard.gd + scenes/components/ClubCard.tscn (template for PibeCard + RecruitCard)
    - scripts/components/NavButton.gd (template for ResourceWidget + ProfessionIcon — exported var + setter + _refresh pattern)
    - scripts/components/ChipButton.gd (template for RankBadge + TraitChip + EnergiaBar — cached StyleBoxFlat pattern)
    - .planning/phases/03-core-loop-laboral/03-UI-SPEC.md §3b (typography 4-size scale with [OVERRIDE]) + §4 (color palettes) + §6.1-6.10 (component dimensions/layouts)
    - .planning/phases/03-core-loop-laboral/03-PATTERNS.md §"AppTheme.gd" lines 1218-1244 + §"NakamaService.gd" lines 733-773 + §"PlayerStore.gd" lines 777-817 + §"FlowRouter.gd" lines 821-842 + §"PibeCard.gd" lines 1052-1117 + §"ResourceWidget.gd" lines 1129-1166 + §"RankBadge/TraitChip/EnergiaBar/ProfessionIcon" lines 1170-1206 + §"SkillProgressRing" lines 1210-1214 + §"No Analog Found" lines 1474-1484
    - .planning/phases/03-core-loop-laboral/03-RESEARCH.md §Godot Roster + Aguantadero UI lines 1117-1199
  </read_first>
  <files>
    scripts/autoloads/AppTheme.gd,
    scripts/autoloads/NakamaService.gd,
    scripts/autoloads/PlayerStore.gd,
    scripts/autoloads/FlowRouter.gd,
    scripts/components/PibeCard.gd, scenes/components/PibeCard.tscn,
    scripts/components/RecruitCard.gd, scenes/components/RecruitCard.tscn,
    scripts/components/ResourceWidget.gd, scenes/components/ResourceWidget.tscn,
    scripts/components/RankBadge.gd, scenes/components/RankBadge.tscn,
    scripts/components/TraitChip.gd, scenes/components/TraitChip.tscn,
    scripts/components/EnergiaBar.gd, scenes/components/EnergiaBar.tscn,
    scripts/components/ProfessionIcon.gd, scenes/components/ProfessionIcon.tscn,
    scripts/components/SkillProgressRing.gd, scenes/components/SkillProgressRing.tscn,
    scripts/components/TurnoModal.gd, scenes/TurnoModal.tscn
  </files>
  <action>
    1) **AppTheme.gd extension** — append Phase 3 tokens from <interfaces> §AppTheme.gd. If existing FONT_HEADING := 22 is present, CHANGE its value to 20 and add comment `# [OVERRIDE Phase 1] 22 → 20; serves dual Heading + Numeric Counter role per UI-SPEC §3b.`. Add `get_resource_color(name)` and `get_energia_color(value)` helper functions.

    2) **NakamaService.gd extension** — append 10 wrappers from <interfaces> §NakamaService.gd. Every wrapper follows the existing `get_current_window` template: auth guard, build payload, await `rpc_async`, exception check, `typeof != TYPE_DICTIONARY` guard (WR-09), return `{ok, data}` or `{ok:false, error}`. The `assign_profession` profession arg accepts `null` (rest mode) — JSON encodes as `null`. `collect_idle` accepts optional pibe_id filter. `complete_tutorial(step, elapsed_ms=0)` includes `elapsed_ms` in the payload when > 0 (telemetry for LAB-TUTORIAL-DURATION; server logs at step 6).

    3) **PlayerStore.gd extension** — append signals + fields per <interfaces> §PlayerStore.gd. Extend `clear()` to reset new fields. Add `refresh_resources_and_roster()` helper that calls get_roster + get_aguantadero in sequence and emits the corresponding signals.

    4) **FlowRouter.gd extension** — append 7 new go_ helpers + `go_post_pibe_create()` gate + `tutorial_advance(step, elapsed_ms=0)` orchestrator from <interfaces> §FlowRouter.gd. `tutorial_advance` forwards `elapsed_ms` to NakamaService.complete_tutorial; TutorialScreen (plan 03.04b) computes it from `tutorial_start_at_ms`. The `set_meta("nav_pibe_id", pibe_id)` pattern lets target screens read the pibe id via `PlayerStore.get_meta("nav_pibe_id")` (Godot scene-args limitation workaround).

    5) **9 components** — create each with .gd + .tscn pair following the analog from §read_first:

       **PibeCard.gd + .tscn** (UI-SPEC §6.1): PanelContainer 120px height, HBox with 80×80 AvatarPlaceholder (club tint), VBox containing Name+RankBadge HBox, rol/profession label, EnergiaBar mini, TraitChip×2 HBox. `set_pibe(pibe: Dictionary)` setter mirrors ClubCard's `set_club`. `tapped(pibe_id)` signal. In-turno state: left-edge 4px accent stripe.

       **RecruitCard.gd + .tscn** (UI-SPEC §6.2): PanelContainer 180px, HBox with 120×140 AvatarLarge, VBox with Name (Display 28), RolLabel, HBox of visible TraitChip + hidden TraitChip (label "?"), Plata cost label, "Reclutar" Button. Gated state (rank insufficient): `modulate.a = 0.4` + disabled CTA + gate label.

       **ResourceWidget.gd + .tscn** (UI-SPEC §6.3): VBoxContainer 80×60 fixed. HBox with 24×24 ColorRect tile (tinted via AppTheme.get_resource_color) + 16px Bold one-letter glyph Label ("$"/"A"/"R"/"V" placeholder), plus 20px Bold NumericValue Label. `set_value(int)` setter formats with `.` thousands separator (Spanish locale per UI-SPEC §3b note). `tooltip_text` set for accessibility ("Plata: {N}").

       **RankBadge.gd + .tscn** (UI-SPEC §6.4): PanelContainer corner 12, padding 8h/4v. Label 14 Bold all-caps. `set_rank(name: String)` setter swaps cached StyleBoxFlat with AppTheme rank color (RANK_PIBE/SOLDADO/CAPO/MESA/LIDER). Promotion flash state: 1.2s scale tween 1.0→1.15→1.0 + accent border 2px.

       **TraitChip.gd + .tscn** (UI-SPEC §6.7): PanelContainer corner 14, border 1px from AppTheme TRAIT_POSITIVE/NEGATIVE/NEUTRAL. Label 14 Bold. `set_trait(id: String, sentiment: String, label: String)` setter. Hidden variant: label "?" + modulate 0.6 + border #3D3D3D.

       **EnergiaBar.gd + .tscn** (UI-SPEC §6.5): ProgressBar with custom StyleBox. Track bg #2D2D2D. Fill color from `AppTheme.get_energia_color(value)` — swaps at integer thresholds 0/30/70. Regular variant 16px height; mini variant 8px (used in PibeCard).

       **ProfessionIcon.gd + .tscn** (UI-SPEC §6.8): PanelContainer 24×24 (inline) or 40×40 (assign screen rows). Bg color from AppTheme PROF_TRAPITO/VENDEDOR/PATOVICA/REMISERO/HABLAR_CANA/SIN_LABURO. Inner ColorRect placeholder for monochrome icon.

       **SkillProgressRing.gd + .tscn** (UI-SPEC §6.6): Control 40×40 with `_draw()` override calling `draw_arc()`. Background ring stroke #3D3D3D, foreground stroke #FBBF24 (RES_PLATA gold), 4px width. Center Label 16px Bold for level number. **No analog exists** (PATTERNS §No Analog Found) — refer to Godot 4.3 `Control._draw()` docs.

       **TurnoModal.gd + .tscn** (UI-SPEC §5.7 + §6 modal section): PanelContainer modal overlay. Header with Title + Subtitle. Body ScrollContainer with VBox of TurnoPibeRow (Checkbox + 40×40 mini avatar + Name + EnergiaBar mini + "−40" cost label). Footer with "Aporte estimado: +{N×50} Aguante · +{N×20} Rep" + Cancelar/Confirmar buttons. `show_modal(current_window)` method populates from PlayerStore.pibes filtered by `energia ≥ 30 && (en_turno_until == null || en_turno_until < now)`. Confirm CTA calls `NakamaService.submit_turno(fixture_id, pibe_ids)`. Already-in-turno pibes listed greyed below eligible block.

    **Vocabulary audit** (UI-SPEC §8.5 blacklist): no .gd file may contain "ataque", "raid", "pelea", "atacar", "robar". Use "hacer turno", "estar en la cancha", "aguantar", "laburar" instead.

    All components inherit Control or PanelContainer. All buttons use existing Phase 1 press scale-tween (already in Theme.tres — no per-component implementation needed).
  </action>
  <verify>
    <automated>cd /c/Users/el_lu/BarraBrava && (godot --headless --check-only --quit 2>&1 | grep -iE "error|fail" | grep -v "^$" | wc -l | grep -q '^0$' || echo "GODOT_CHECK_UNAVAILABLE_FALLBACK") && grep -q "RES_PLATA" scripts/autoloads/AppTheme.gd && grep -q "RES_VBC" scripts/autoloads/AppTheme.gd && grep -q "RANK_LIDER" scripts/autoloads/AppTheme.gd && grep -q "TRAIT_POSITIVE" scripts/autoloads/AppTheme.gd && grep -q "ENERGIA_FULL" scripts/autoloads/AppTheme.gd && grep -q "FONT_HEADING := 20" scripts/autoloads/AppTheme.gd && grep -q "func submit_turno" scripts/autoloads/NakamaService.gd && grep -q "func recruit_pibe" scripts/autoloads/NakamaService.gd && grep -q "func complete_tutorial" scripts/autoloads/NakamaService.gd && grep -q "elapsed_ms" scripts/autoloads/NakamaService.gd && grep -q "signal roster_updated" scripts/autoloads/PlayerStore.gd && grep -q "tutorial_done" scripts/autoloads/PlayerStore.gd && grep -q "func go_post_pibe_create" scripts/autoloads/FlowRouter.gd && grep -q "func tutorial_advance" scripts/autoloads/FlowRouter.gd && grep -q "elapsed_ms" scripts/autoloads/FlowRouter.gd && test -f scripts/components/PibeCard.gd && test -f scenes/components/PibeCard.tscn && test -f scripts/components/RecruitCard.gd && test -f scripts/components/ResourceWidget.gd && test -f scripts/components/RankBadge.gd && test -f scripts/components/TraitChip.gd && test -f scripts/components/EnergiaBar.gd && test -f scripts/components/ProfessionIcon.gd && test -f scripts/components/SkillProgressRing.gd && test -f scripts/components/TurnoModal.gd && test -f scenes/TurnoModal.tscn && test $(grep -rE "\\b(ataque|raid|pelea|atacar)\\b" scripts/components scripts/autoloads | grep -v '^#' | grep -v "scripts/components/.*\.gd:.*#" | wc -l) -eq 0 && echo "TASK1-PASS"</automated>
  </verify>
  <acceptance_criteria>
    - AppTheme.gd contains all Phase 3 tokens (RES_*, RANK_*, PROF_*, TRAIT_*, ENERGIA_*) AND FONT_HEADING := 20
    - NakamaService.gd contains all 10 new wrappers including complete_tutorial(step, elapsed_ms) with elapsed_ms in payload
    - PlayerStore.gd has new signals (roster_updated, resources_updated, aguantadero_updated, recruit_pool_updated, tutorial_advanced) and new fields
    - FlowRouter.gd has go_roster/go_recruit/go_aguantadero/go_tutorial/go_pibe_detail/go_profession_assign/go_post_pibe_create + tutorial_advance(step, elapsed_ms)
    - All 9 component pairs (.gd + .tscn) exist under scripts/components/ + scenes/components/ (TurnoModal lives in scenes/ root per UI-SPEC §5.7)
    - No banned vocabulary in components/ or autoloads/ source (verified by grep — comments allowed)
    - Godot project loads without script parse errors (check via `godot --headless --check-only --quit` if available, else fallback inspection)
    - `TASK1-PASS` printed
  </acceptance_criteria>
  <done>All autoload extensions compile (Godot parses .gd files at load); all 9 components instance-test in editor; banned-word grep returns 0 matches; project still boots from AuthScreen. Plan 03.04b consumes these to wire HomeScreen + 6 screens + tutorial state machine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Godot client → Nakama RPC | Untrusted client UI; all economy validation server-side (already enforced by 03.02 + 03.03 RPCs) |
| Client display copy → App Store reviewer | Static strings + dynamic insertions never use blacklisted vocabulary (UI-SPEC §8.5 audit) |
| User input (recruit/assign/upgrade taps) → RPC payload | All payloads built from server-side state (pibe.id from PlayerStore.pibes, pick_id from get_recruit_pool result) — never raw user-typed strings beyond Phase 1's display_name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-UIA-01 | Tampering | Client claims rank=lider to unlock hablar_cana profession color in ProfessionIcon | mitigate | Component renders color from AppTheme palette; ProfessionAssignScreen (plan 03.04b) enforces visible-but-disabled gating client-side; server `assign_profession` re-validates rank (D-07). |
| T-3-UIA-02 | Information disclosure | Trait_2 leaked client-side via RPC response inspection | mitigate | Server redacts trait_2 to boolean `true` (plan 03.02 get_recruit_pool). RecruitCard renders "?" — no value to leak. |
| T-3-UIA-03 | Regulatory (App Store) | Phase 3 copy includes combat vocabulary | mitigate | UI-SPEC §8.5 blacklist enforced via grep audit in Task 1 verify gate. Plan 03.04b extends audit across screens. |
| T-3-UIA-04 | DoS (network) | TurnoModal Confirm CTA tapped repeatedly | mitigate | CTA disabled-during-RPC pattern carried from Phase 1 _on_delete lines 92-101. Documented as required pattern for all components touching RPCs. |
</threat_model>

<verification>
After Task 1 completes:

1. **Editor parse check:** Open Godot 4.3. No script parse errors in Output panel. All autoload symbols resolve.

2. **Boot smoke-test:** Project launches to AuthScreen. Register/login flow unchanged (no screen wiring yet — plan 03.04b adds the tutorial gate).

3. **Component instancing:** Each .tscn opens in Godot editor without missing-node warnings. Visual sanity check: RankBadge colors match palette; EnergiaBar fill switches at 0/30/70 boundaries; ResourceWidget formats numbers with Spanish locale.

4. **Vocabulary audit:** `grep -rE "\\b(ataque|raid|pelea|atacar|robar)\\b" scripts/components scripts/autoloads | grep -v '^#'` returns 0 matches.

5. **Typography compliance:** AppTheme.FONT_HEADING == 20. Components do not hard-code 22 anywhere.

6. **Color palette compliance:** AppTheme exports RANK_*, PROF_*, TRAIT_*, ENERGIA_*, RES_* constants. Helper functions `get_resource_color`/`get_energia_color` resolve the lookups for downstream consumers.
</verification>

<success_criteria>
- All 4 autoload extensions land + parse cleanly.
- All 9 components instance + render correctly.
- complete_tutorial wrapper accepts optional `elapsed_ms` (plumbing for LAB-TUTORIAL-DURATION telemetry).
- ZERO banned vocabulary in autoloads or components source.
- 4-size typography compliance (14/16/20/28) — FONT_HEADING := 20 confirmed.
- Project boots from AuthScreen — no regression.
- Goja AST: still 28 RPCs (no server-side changes in this plan).
- Ready for plan 03.04b (screen wiring + tutorial state machine + human checkpoint).
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-loop-laboral/03-04a-SUMMARY.md` per template at $HOME/.claude/get-shit-done/templates/summary.md. Include:
- Confirmation that all 22 files (4 autoloads + 18 component pairs) compile.
- Banned-word grep result (should be 0).
- Note that FONT_HEADING was reduced from 22 to 20 (UI-SPEC §0 OVERRIDE).
- Confirmation that `complete_tutorial` wrapper plumbs `elapsed_ms` for telemetry.
- Pointer to plan 03.04b (screens + tutorial + human-verify checkpoint).
</output>
