---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - nakama/package.json
  - nakama/tsconfig.json
  - nakama/build.mjs
  - nakama/.gitignore
  - nakama/src/main.ts
  - nakama/src/clubs.ts
  - nakama/src/rpc/get_clubs.ts
  - nakama/src/rpc/create_pibe.ts
  - nakama/src/rpc/delete_account.ts
  - nakama/src/rpc/request_password_reset.ts
  - nakama/src/rpc/confirm_password_reset.ts
  - nakama/src/util/validation.ts
  - nakama/src/util/email.ts
  - nakama/data/clubs.json
  - nakama/local.yml
  - nakama/smoke-test.sh
  - Dockerfile.nakama
  - scripts/autoloads/NakamaClient.gd
  - .planning/phases/01-foundation/INFRA-NOTES.md
  - .planning/phases/01-foundation/01-VALIDATION.md
autonomous: false
requirements:
  - TEC-09
  - TEC-10
  - CLB-01
  - ONB-02
  - ONB-04
  - PRV-03
tags:
  - nakama
  - typescript
  - runtime
  - rpc
  - clubs-seed
  - rate-limiting
  - resend

user_setup:
  - service: resend
    why: "Password reset emails enviados desde el RPC server"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys (ya seteado en Plan 01)"
      - name: RESEND_FROM_EMAIL
        source: "noreply@{tu-dominio} — debe matchear el dominio verificado en Resend"
      - name: PASSWORD_RESET_BASE_URL
        source: "URL pública GitHub Pages donde se hostea el reset HTML (Plan 05)"
    dashboard_config:
      - task: "Confirmar que dominio Resend está Verified (no Verifying)"
        location: "resend.com/domains"
      - task: "Agregar las 3 env vars al servicio Railway de Nakama"
        location: "Railway service -> Variables"
  - service: nakama-server-key-sync
    why: "Sincronizar NAKAMA_SERVER_KEY entre Railway env var y NakamaClient.gd hardcoded constant (CHK-07 fix)"
    dashboard_config:
      - task: "Copiar el valor de NAKAMA_SERVER_KEY desde Railway service Variables (NO compartir con Claude — pegarlo directamente en el archivo)"
        location: "Railway service -> Variables -> NAKAMA_SERVER_KEY -> Copy"
      - task: "Editar scripts/autoloads/NakamaClient.gd y reemplazar NAKAMA_SERVER_KEY_DEFAULT = \"defaultkey\" por NAKAMA_SERVER_KEY_DEFAULT = \"<valor pegado>\""
        location: "Editor local — NO commitear este archivo si el repo es público. Si es público, mover la constante a nakama_local.cfg gitignored y leerla con ConfigFile en NakamaClient._ready"
      - task: "Documentar en INFRA-NOTES.md que el server key del cliente coincide con Railway (sin pegar el valor en plano — solo confirmación)"
        location: ".planning/phases/01-foundation/INFRA-NOTES.md sección Wave 2"
      - task: "Re-correr smoke-test.sh con NAKAMA_KEY exportado al valor real para confirmar match"
        location: "terminal local: NAKAMA_KEY=$REAL_KEY bash nakama/smoke-test.sh"

must_haves:
  truths:
    - "Nakama TypeScript runtime compila a JavaScript y carga al boot del server"
    - "InitModule siembra ~133 clubs en Nakama Storage en colección 'clubs' (idempotente — no duplica en restart)"
    - "RPC get_clubs retorna lista paginada de clubs filtrable por division"
    - "RPC create_pibe valida nombre server-side (deny list + length), asigna stats fijos 50/50/50/50, persiste en Storage colección 'pibes'"
    - "RPC delete_account elimina la cuenta Nakama y todos los Storage Objects del usuario"
    - "RPC request_password_reset envía email vía Resend y retorna respuesta uniforme (no revela si email existe)"
    - "RPC confirm_password_reset valida token, actualiza password, invalida token"
    - "Rate limiting está configurado en Nakama: register endpoint = 10/min/IP, RPCs = 60/min/user (TEC-10 baseline)"
    - "smoke-test.sh registra usuario test, llama get_clubs, llama create_pibe, todo retorna 200"
    - "NakamaClient.gd autoload tiene server key sincronizado con Railway NAKAMA_SERVER_KEY (no más 'defaultkey' placeholder)"
    - "01-VALIDATION.md frontmatter actualizado: nyquist_compliant=true y wave_0_complete=true (CHK-03 fix)"
  artifacts:
    - path: "nakama/package.json"
      provides: "Build pipeline TypeScript runtime (esbuild)"
      contains: "esbuild"
    - path: "nakama/tsconfig.json"
      provides: "TypeScript config con tipos Nakama"
      contains: "nkruntime"
    - path: "nakama/src/main.ts"
      provides: "Entrypoint InitModule + registro de RPCs"
      contains: "InitModule"
    - path: "nakama/data/clubs.json"
      provides: "Seed de ~133 clubs con lunfardo names, paleta, escudo arquetipo, barrio"
      contains: "lunfardo_name"
    - path: "nakama/src/rpc/get_clubs.ts"
      provides: "RPC paginated club list"
      contains: "get_clubs"
    - path: "nakama/src/rpc/create_pibe.ts"
      provides: "RPC server-side pibe creation con stats fijos"
      contains: "create_pibe"
    - path: "nakama/src/rpc/delete_account.ts"
      provides: "RPC self-service account deletion (PRV-03)"
      contains: "delete_account"
    - path: "nakama/local.yml"
      provides: "Nakama server config con rate limiting habilitado"
      contains: "rate_limit"
    - path: "nakama/smoke-test.sh"
      provides: "Script bash end-to-end: register -> clubs -> create_pibe"
      contains: "/v2/rpc/get_clubs"
  key_links:
    - from: "Dockerfile.nakama"
      to: "nakama/build/ (output esbuild)"
      via: "COPY nakama/build/ /nakama/data/modules/"
      pattern: "COPY nakama/build"
    - from: "nakama/src/main.ts"
      to: "nakama/src/rpc/*.ts"
      via: "initializer.registerRpc('name', handler)"
      pattern: "registerRpc"
    - from: "nakama/src/rpc/request_password_reset.ts"
      to: "Resend API"
      via: "nk.httpRequest('https://api.resend.com/emails', 'post', ...)"
      pattern: "api.resend.com"
    - from: "scripts/autoloads/NakamaClient.gd"
      to: "Railway NAKAMA_SERVER_KEY env var"
      via: "valor manual pegado por dev (CHK-07 fix) — debe coincidir"
      pattern: "NAKAMA_SERVER_KEY_DEFAULT"
---

<objective>
Wave 2 — Construir el runtime TypeScript de Nakama: setup nakama-project-template con esbuild, autorar `clubs.json` con ~133 clubs en lunfardo, escribir InitModule que siembra clubs idempotentemente, e implementar 5 RPCs server-authoritative: get_clubs, create_pibe, delete_account, request_password_reset, confirm_password_reset, además de configurar rate limiting baseline (TEC-10). Al final, redeployar Nakama con los módulos compilados, sincronizar el server key entre Railway y NakamaClient.gd (CHK-07), actualizar VALIDATION.md frontmatter a `nyquist_compliant: true` (CHK-03), y validar end-to-end con smoke-test.sh.

**Propósito:** Sin runtime custom, Nakama es solo un servidor de auth genérico. Las decisiones D-05/06/07/08/11 del CONTEXT.md (clubs seedeados, stats fijos, búsqueda/filtro) requieren código server-side. Sin RPCs no hay create_pibe (ONB-04), no hay catálogo de clubs (ONB-02, CLB-01), no hay account deletion (PRV-03). Plan 04 (pantallas) depende 100% de estos RPCs.

**Output:** `nakama/build/index.js` compilado y deployado en Railway. Logs Nakama muestran "Clubs seeded: 133+" en primer boot. smoke-test.sh verde con el server key real. NakamaClient.gd actualizado. VALIDATION.md marca el wave 0 completo y la nyquist compliance verde.

**Decisiones críticas locked-in del CONTEXT.md respetadas:**
- D-05/06: clubs.json estático con lunfardo_name + colors + shield_archetype + barrio_hq
- D-07: 5 divisiones AFA completas (~133 clubs)
- D-11: stats fijos al crear pibe (50/50/50/50 — Aguante, Velocidad, Astucia, Carisma per UI-SPEC §PibeCreatorScreen)
- D-03: password reset via Resend (NO SendGrid — killed free tier May 2025 per RESEARCH §6)
- D-10: NO faction selection en onboarding — create_pibe NO recibe ni guarda faction_id

**No incluye:** UI screens (eso es Plan 04), GPS, FCM push, analytics, IAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/INFRA-NOTES.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@Dockerfile.nakama
@scripts/autoloads/NakamaClient.gd

<interfaces>
<!-- Patterns de RESEARCH.md §4, §5, §6 que el executor debe replicar verbatim. -->

### nakama/package.json (esbuild + types)
```json
{
  "name": "barrabrava-nakama-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "nakama-runtime": "^1.32.0",
    "typescript": "^5.4.0"
  }
}
```

### nakama/tsconfig.json
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "module": "esnext",
    "moduleResolution": "node",
    "lib": ["ES2017"],
    "strict": true,
    "noImplicitAny": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "outDir": "build",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"]
}
```

### nakama/build.mjs (esbuild bundler)
```js
import { build } from 'esbuild';
import { readFileSync } from 'fs';

const clubsJson = readFileSync('./data/clubs.json', 'utf-8');

await build({
  entryPoints: ['src/main.ts'],
  outfile: 'build/index.js',
  bundle: true,
  format: 'iife',
  target: 'es2017',
  platform: 'neutral',
  define: {
    '__CLUBS_JSON__': JSON.stringify(clubsJson),
  },
  external: ['nakama-runtime'],
});

console.log('Built nakama/build/index.js');
```

### nakama/local.yml (Nakama config con rate limiting)
```yaml
name: barrabrava-nakama
data_dir: "./data"
logger:
  level: "INFO"
session:
  encryption_key: "${NAKAMA_SESSION_ENCRYPTION_KEY}"
  refresh_encryption_key: "${NAKAMA_SESSION_REFRESH_ENCRYPTION_KEY}"
  token_expiry_sec: 3600
  refresh_token_expiry_sec: 2592000
socket:
  server_key: "${NAKAMA_SERVER_KEY}"
runtime:
  js_entrypoint: "build/index.js"
  env:
    - "RESEND_API_KEY=${RESEND_API_KEY}"
    - "RESEND_FROM_EMAIL=${RESEND_FROM_EMAIL}"
    - "PASSWORD_RESET_BASE_URL=${PASSWORD_RESET_BASE_URL}"
limit:
  registration_per_ip_per_min: 10
  rpc_per_user_per_min: 60
```

### nakama/src/main.ts (InitModule + RPC registry)
```typescript
/// <reference path="../node_modules/nakama-runtime/index.d.ts" />

declare const __CLUBS_JSON__: string;

import { rpcGetClubs } from './rpc/get_clubs';
import { rpcCreatePibe } from './rpc/create_pibe';
import { rpcDeleteAccount } from './rpc/delete_account';
import { rpcRequestPasswordReset } from './rpc/request_password_reset';
import { rpcConfirmPasswordReset } from './rpc/confirm_password_reset';

const SYSTEM_USER_ID = '00000000-0000-0000-0000-000000000000';
const CLUBS_SEED_VERSION = 'v1';

interface Club {
  id: string;
  lunfardo_name: string;
  division: string;
  division_rank: number;
  colors: { primary: string; secondary: string };
  shield_archetype: string;
  barrio_hq: string;
  city: string;
}

function seedClubs(nk: nkruntime.Nakama, logger: nkruntime.Logger): void {
  const seedKey = `clubs_seeded_${CLUBS_SEED_VERSION}`;
  try {
    const existing = nk.storageRead([
      { collection: 'meta', key: seedKey, userId: SYSTEM_USER_ID },
    ]);
    if (existing.length > 0) {
      logger.info('Clubs already seeded (version=%s), skipping', CLUBS_SEED_VERSION);
      return;
    }
  } catch (e) {
    // first run, continue
  }

  const clubs: Club[] = JSON.parse(__CLUBS_JSON__);
  const writes: nkruntime.StorageWriteRequest[] = clubs.map(club => ({
    collection: 'clubs',
    key: club.id,
    userId: SYSTEM_USER_ID,
    value: club,
    permissionRead: 2,
    permissionWrite: 0,
  }));
  nk.storageWrite(writes);

  nk.storageWrite([{
    collection: 'meta',
    key: seedKey,
    userId: SYSTEM_USER_ID,
    value: { seeded: true, count: clubs.length, at: Date.now() },
    permissionRead: 0,
    permissionWrite: 0,
  }]);
  logger.info('Clubs seeded: %d (version=%s)', clubs.length, CLUBS_SEED_VERSION);
}

const InitModule: nkruntime.InitModule = (
  ctx: nkruntime.Context,
  logger: nkruntime.Logger,
  nk: nkruntime.Nakama,
  initializer: nkruntime.Initializer
) => {
  logger.info('BarraBrava runtime starting...');

  seedClubs(nk, logger);

  initializer.registerRpc('get_clubs', rpcGetClubs);
  initializer.registerRpc('create_pibe', rpcCreatePibe);
  initializer.registerRpc('delete_account', rpcDeleteAccount);
  initializer.registerRpc('request_password_reset', rpcRequestPasswordReset);
  initializer.registerRpc('confirm_password_reset', rpcConfirmPasswordReset);

  logger.info('BarraBrava runtime ready: 5 RPCs registered');
};

!InitModule && InitModule.bind(null);
```

### nakama/src/rpc/get_clubs.ts
[same as before]

### nakama/src/rpc/create_pibe.ts
[same as before — stats hardcoded 50/50/50/50, no faction, validatePibeName called]

### nakama/src/rpc/delete_account.ts
[same as before — iterates pibes + players, calls accountDeleteId]

### nakama/src/rpc/request_password_reset.ts (Resend integration)
[same as before — anti-enumeration return ok:true always]

### nakama/src/rpc/confirm_password_reset.ts
[same as before — token validate, linkEmail, storageDelete]

### nakama/src/util/validation.ts
[same as before — deny list with profanity + admin/moderador/staff]

### nakama/src/util/email.ts (Resend API call)
[same as before]

### nakama/data/clubs.json — schema exacto RESEARCH §5
[same as before — ≥130 entries across 5 AFA divisions]

### smoke-test.sh (RESEARCH §10)
[same as before — healthcheck, register, get_clubs Primera, get_clubs Todos, create_pibe, profanity reject, delete_account]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold nakama TypeScript runtime + clubs.json seed</name>
  <files>
    nakama/package.json
    nakama/tsconfig.json
    nakama/build.mjs
    nakama/.gitignore
    nakama/local.yml
    nakama/data/clubs.json
    nakama/src/main.ts
    nakama/src/util/validation.ts
    nakama/src/util/email.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (secciones 4, 5, 6)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-05, D-06, D-07, D-11)
    - CLAUDE.md (Tono cultural — lunfardo, apolítico, sin nombres reales de líderes)
    - Dockerfile.nakama (verificar que la línea COPY del runtime está comentada — se descomenta en Task 4)
  </read_first>
  <action>
    [Mismas instrucciones que Plan 03 original — crear nakama/ con package.json, tsconfig.json, build.mjs, .gitignore, local.yml, data/clubs.json (≥130 entries 5 divisiones AFA), src/main.ts, src/util/validation.ts, src/util/email.ts.

    Ver `<interfaces>` arriba para contenido literal. Distribución mínima de clubs.json:
    - Primera División: 26 clubs
    - Primera Nacional: 38 clubs
    - B Metro: 17 clubs
    - Federal A: ~32 clubs
    - C Metro: ~20 clubs
    - Total ≥130.

    Después de crear archivos, correr `cd nakama && npm install`.

    Commit: `feat(01): scaffold nakama TypeScript runtime + clubs.json seed (~133 clubs)`.]
  </action>
  <verify>
    <automated>test -f nakama/package.json && test -f nakama/tsconfig.json && test -f nakama/build.mjs && test -f nakama/local.yml && test -f nakama/data/clubs.json && test -f nakama/src/main.ts && test -f nakama/src/util/validation.ts && test -f nakama/src/util/email.ts && grep -q "esbuild" nakama/package.json && grep -q "nakama-runtime" nakama/package.json && grep -q "registerRpc" nakama/src/main.ts && grep -q "seedClubs" nakama/src/main.ts && grep -q "rate_limit\|registration_per_ip" nakama/local.yml && python3 -c "import json,sys; d=json.load(open('nakama/data/clubs.json')); divs={c['division'] for c in d}; assert len(d)>=130, f'Only {len(d)} clubs'; required={'Primera','Nacional','B Metro','Federal A','C Metro'}; missing=required-divs; assert not missing, f'Missing divisions: {missing}'; print(f'OK {len(d)} clubs, divisions={divs}')"</automated>
  </verify>
  <acceptance_criteria>
    - Scaffolding completo + clubs.json ≥130 entries 5 divisiones + 5 RPCs registrados en main.ts
    - validatePibeName con deny list (puta, admin, moderador, etc.)
    - local.yml con rate limits (10/min IP register, 60/min user RPC)
    - Commit local realizado
  </acceptance_criteria>
  <done>
    Scaffold listo. Task 2 implementa los 5 RPCs individuales.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementar 5 RPCs server-side + esbuild typecheck + build</name>
  <files>
    nakama/src/rpc/get_clubs.ts
    nakama/src/rpc/create_pibe.ts
    nakama/src/rpc/delete_account.ts
    nakama/src/rpc/request_password_reset.ts
    nakama/src/rpc/confirm_password_reset.ts
    nakama/build/index.js
  </files>
  <read_first>
    - nakama/src/main.ts
    - nakama/src/util/validation.ts
    - nakama/src/util/email.ts
    - .planning/phases/01-foundation/01-RESEARCH.md (§6 password reset + §4 Storage permissions)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-10 NO faction, D-11 stats fijos)
  </read_first>
  <action>
    [Mismas instrucciones que Plan 03 original — crear los 5 RPCs copy literal de `<interfaces>`:
    - get_clubs.ts: paginación + filtros division + search
    - create_pibe.ts: stats hardcoded 50/50/50/50, validatePibeName, no faction
    - delete_account.ts: cleanup colecciones pibes + players, accountDeleteId
    - request_password_reset.ts: anti-enumeration (ok:true siempre), token 30min TTL en colección reset_tokens
    - confirm_password_reset.ts: validate token + linkEmail + storageDelete token

    Correr `cd nakama && npm run typecheck && npm run build`. Verificar build/index.js > 20KB.

    Commit: `feat(01): implement 5 server RPCs + esbuild bundle`.]
  </action>
  <verify>
    <automated>test -f nakama/src/rpc/get_clubs.ts && test -f nakama/src/rpc/create_pibe.ts && test -f nakama/src/rpc/delete_account.ts && test -f nakama/src/rpc/request_password_reset.ts && test -f nakama/src/rpc/confirm_password_reset.ts && grep -q "aguante: 50" nakama/src/rpc/create_pibe.ts && grep -q "velocidad: 50" nakama/src/rpc/create_pibe.ts && grep -q "astucia: 50" nakama/src/rpc/create_pibe.ts && grep -q "carisma: 50" nakama/src/rpc/create_pibe.ts && grep -q "validatePibeName" nakama/src/rpc/create_pibe.ts && ! grep -q "faction" nakama/src/rpc/create_pibe.ts && grep -q "accountDeleteId" nakama/src/rpc/delete_account.ts && grep -q "ok: true" nakama/src/rpc/request_password_reset.ts && grep -q "api.resend.com\|sendResetEmail" nakama/src/rpc/request_password_reset.ts && grep -q "linkEmail" nakama/src/rpc/confirm_password_reset.ts && cd nakama && npm run typecheck && npm run build && test -f build/index.js && test $(wc -c < build/index.js) -gt 20000 && grep -q "get_clubs" build/index.js && grep -q "create_pibe" build/index.js && grep -q "delete_account" build/index.js && test $(grep -c "lunfardo_name" build/index.js) -ge 130</automated>
  </verify>
  <acceptance_criteria>
    - 5 RPC files compilados a un solo bundle build/index.js > 20KB
    - clubs embedded (≥130 occurrences "lunfardo_name")
    - Stats hardcoded 50/50/50/50 en create_pibe
    - No "faction" en create_pibe (D-10)
    - Anti-enumeration en request_password_reset
    - Commit local realizado
  </acceptance_criteria>
  <done>
    Runtime TypeScript compilado. Listo para deploy en Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Actualizar Dockerfile.nakama + redeploy Railway + smoke-test.sh + sync NakamaClient.gd server key (CHK-07) + update VALIDATION.md (CHK-03)</name>
  <files>
    Dockerfile.nakama
    nakama/smoke-test.sh
    scripts/autoloads/NakamaClient.gd
    .planning/phases/01-foundation/INFRA-NOTES.md
    .planning/phases/01-foundation/01-VALIDATION.md
  </files>
  <read_first>
    - Dockerfile.nakama (descomentar línea COPY runtime)
    - nakama/build/index.js (verificar existe)
    - scripts/autoloads/NakamaClient.gd (Plan 02 — buscar NAKAMA_SERVER_KEY_DEFAULT)
    - .planning/phases/01-foundation/INFRA-NOTES.md (Railway URL)
    - .planning/phases/01-foundation/01-VALIDATION.md (frontmatter actual: nyquist_compliant=false, wave_0_complete=false)
    - .planning/phases/01-foundation/01-RESEARCH.md (§10 smoke test)
  </read_first>
  <action>
    **1. Actualizar `Dockerfile.nakama`** a multi-stage build:
    ```dockerfile
    FROM node:20-alpine AS runtime-builder
    WORKDIR /workspace
    COPY nakama/package.json nakama/tsconfig.json nakama/build.mjs ./
    COPY nakama/src ./src
    COPY nakama/data ./data
    RUN npm install --no-audit --no-fund
    RUN npm run build

    FROM heroiclabs/nakama:3.21.0
    COPY --from=runtime-builder /workspace/build/ /nakama/data/modules/
    COPY nakama/local.yml /nakama/data/local.yml
    EXPOSE 7350 7351
    ```

    **2. Crear `nakama/smoke-test.sh`** — copy literal del bloque `<interfaces>` (sección smoke-test). Hacer ejecutable con `chmod +x`.

    **3. CHK-07 fix — Sincronizar NakamaClient.gd server key:**

    Este task ES un user_setup item (declarado en frontmatter user_setup). El executor Claude NO puede leer el valor secreto del Railway env var. Las acciones del executor son:

    a. **Pausar antes de modificar NakamaClient.gd** y pedir al usuario que ejecute estos pasos manualmente:
       1. Abrir Railway dashboard → service Nakama → Variables → copiar el valor de NAKAMA_SERVER_KEY
       2. Confirmar al executor: "tengo el server key copiado" (NO compartir el valor)

    b. El executor entonces edita `scripts/autoloads/NakamaClient.gd` para crear un patrón limpio que el usuario complete. Reemplazar la constante:
       ```gdscript
       const NAKAMA_SERVER_KEY_DEFAULT := "defaultkey"  # OVERRIDE via project settings or env at runtime
       ```
       Por:
       ```gdscript
       # CHK-07 fix: server key MUST match Railway NAKAMA_SERVER_KEY env var.
       # Strategy: read from gitignored nakama_local.cfg if present (safe for public repos),
       # fallback to compile-time const (safe for private repos — manually update).
       const NAKAMA_SERVER_KEY_DEFAULT := "defaultkey"  # ← REPLACE LOCALLY post-deploy; DO NOT commit real value if repo is public
       const NAKAMA_LOCAL_CFG := "user://nakama_local.cfg"

       func _load_runtime_overrides() -> void:
           # If the dev placed nakama_local.cfg in user:// or beside the executable, use it
           var cfg = ConfigFile.new()
           if cfg.load(NAKAMA_LOCAL_CFG) == OK:
               nakama_server_key = cfg.get_value("nakama", "server_key", nakama_server_key)
               nakama_host = cfg.get_value("nakama", "host", nakama_host)
       ```
       Y en `_ready()`, llamar `_load_runtime_overrides()` ANTES de `create_client`.

    c. Si el repo es PRIVADO (verificar con `git remote get-url origin` y user choice), aceptar pegar el server key real directamente en `NAKAMA_SERVER_KEY_DEFAULT` y commit. Documentar la decisión en INFRA-NOTES.md.

    d. Si el repo es PÚBLICO, NO commitear el valor real. El usuario debe crear localmente `user://nakama_local.cfg` (no commit) con:
       ```ini
       [nakama]
       server_key="VALOR_REAL_DEL_KEY"
       ```
       El `_load_runtime_overrides` lo lee al boot.

    **4. CHK-03 fix — Actualizar `.planning/phases/01-foundation/01-VALIDATION.md` frontmatter:**

    Modificar el frontmatter cambiando:
    ```yaml
    nyquist_compliant: false
    wave_0_complete: false
    ```
    Por:
    ```yaml
    nyquist_compliant: true
    wave_0_complete: true
    ```
    Razón: las plans 01-05 incluyen `<automated>` verify commands en cada task (Nyquist), y Wave 0 ahora cubre los items declarados (smoke-test.sh se crea en este task, build-android-debug.yml ya existe en Plan 01, Nakama Railway operativo, clubs.json seedeado).

    Actualizar también el campo `Approval` al final del documento de `pending` a `signed off via Plan 03 Task 3 (2026-05-15) — see plan must_haves`.

    **5. Trigger redeploy en Railway:**
    ```
    git add Dockerfile.nakama nakama/smoke-test.sh scripts/autoloads/NakamaClient.gd .planning/phases/01-foundation/01-VALIDATION.md
    git commit -m "feat(01): wire nakama runtime into Docker + smoke test + sync server key + validation signoff"
    git push origin main
    ```

    Esperar ~3-5 min al deploy. Verificar logs Railway buscando:
    - `BarraBrava runtime starting...`
    - `Clubs seeded: 133` (o N exacto)
    - `BarraBrava runtime ready: 5 RPCs registered`

    **6. Correr smoke-test.sh** con el server key REAL del Railway:
    ```
    export NAKAMA_HOST="..."  # del INFRA-NOTES.md
    export NAKAMA_KEY="..."   # del Railway dashboard (NO commitear)
    bash nakama/smoke-test.sh
    ```
    Esperar `ALL SMOKE TESTS PASSED`.

    **7. Actualizar `INFRA-NOTES.md`** sección Wave 2:
    ```markdown
    ## Wave 2 — Runtime deployado

    - **Fecha primer deploy con runtime:** {hoy}
    - **Clubs seedeados:** {N}
    - **RPCs registrados:** get_clubs, create_pibe, delete_account, request_password_reset, confirm_password_reset
    - **Smoke test:** `bash nakama/smoke-test.sh` → ALL SMOKE TESTS PASSED en {fecha}
    - **Rate limits activos:** 10 registers/IP/min, 60 RPCs/user/min
    - **Server key sync (CHK-07):** confirmado match Railway env var ↔ NakamaClient.gd ({método: hardcoded private repo / nakama_local.cfg gitignored})
    - **VALIDATION.md (CHK-03):** nyquist_compliant=true, wave_0_complete=true firmado en este deploy
    ```
  </action>
  <verify>
    <automated>grep -q "FROM heroiclabs/nakama:3.21.0" Dockerfile.nakama && grep -q "COPY --from=runtime-builder" Dockerfile.nakama && grep -q "node:20-alpine AS runtime-builder" Dockerfile.nakama && test -x nakama/smoke-test.sh && grep -q "/v2/rpc/get_clubs" nakama/smoke-test.sh && grep -q "/v2/rpc/create_pibe" nakama/smoke-test.sh && grep -q "/v2/rpc/delete_account" nakama/smoke-test.sh && grep -q "Wave 2 — Runtime deployado" .planning/phases/01-foundation/INFRA-NOTES.md && grep -q "Server key sync" .planning/phases/01-foundation/INFRA-NOTES.md && grep -q "_load_runtime_overrides\|NAKAMA_LOCAL_CFG" scripts/autoloads/NakamaClient.gd && grep -q "nyquist_compliant: true" .planning/phases/01-foundation/01-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/01-foundation/01-VALIDATION.md && NAKAMA_HOST=$(grep "URL pública Nakama" .planning/phases/01-foundation/INFRA-NOTES.md | head -1 | sed 's|.*https*://||' | tr -d ' \r/') && test -n "$NAKAMA_HOST" && curl -fsS --max-time 10 "https://${NAKAMA_HOST}/healthcheck" > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile.nakama multi-stage (node builder + nakama runtime)
    - smoke-test.sh executable + cubre healthcheck/register/get_clubs/create_pibe/profanity/delete
    - Railway deploy verde + logs "Clubs seeded" + "5 RPCs registered"
    - smoke-test.sh con NAKAMA_KEY real termina "ALL SMOKE TESTS PASSED"
    - NakamaClient.gd tiene `_load_runtime_overrides` que lee `nakama_local.cfg` (CHK-07)
    - INFRA-NOTES.md documenta el método de sync del server key
    - VALIDATION.md frontmatter: `nyquist_compliant: true`, `wave_0_complete: true` (CHK-03)
    - VALIDATION.md `Approval: signed off via Plan 03 Task 3`
    - Commit + push a main realizados
  </acceptance_criteria>
  <done>
    Runtime deployado. Server key sincronizado. VALIDATION.md actualizado. Plan 04 puede consumir los 5 RPCs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificación humana — Runtime Wave 2 operativo + server key sync + VALIDATION.md signed off</name>
  <what-built>
    - Nakama TypeScript runtime con 5 RPCs y seed de ~133 clubs
    - Dockerfile.nakama multi-stage
    - smoke-test.sh corre end-to-end con server key REAL y pasa
    - INFRA-NOTES.md actualizado con estado Wave 2 + server key sync method
    - Rate limiting baseline configurado (TEC-10)
    - NakamaClient.gd con _load_runtime_overrides para sync server key (CHK-07)
    - VALIDATION.md nyquist_compliant=true + wave_0_complete=true (CHK-03)
  </what-built>
  <how-to-verify>
    1. **Railway dashboard**: Último deploy = "Success" verde. Logs muestran `BarraBrava runtime starting...`, `Clubs seeded: N`, `5 RPCs registered`.

    2. **Nakama Console** (`https://{tu-railway-url}:7351`):
       - Login con admin / NAKAMA_CONSOLE_PASSWORD
       - Storage → Collections: aparecen `clubs` (~133 entries) y `meta` (`clubs_seeded_v1`)
       - Inspeccionar 3-5 clubs random — verificar schema completo (lunfardo_name, colors, barrio_hq, shield_archetype, division)

    3. **Smoke test con server key REAL**:
       ```
       export NAKAMA_HOST="{tu-railway-host-sin-https}"
       export NAKAMA_KEY="{el server key real de Railway, NO defaultkey}"
       bash nakama/smoke-test.sh
       ```
       Output esperado: `ALL SMOKE TESTS PASSED`.

       **Si `defaultkey` funciona pero el real no:** revisar que el deploy aplicó la nueva env var (Railway a veces requiere restart manual).

    4. **CHK-07 verificación — server key match cliente ↔ server:**
       a. Decidir tu strategy: repo público → `nakama_local.cfg` gitignored. Repo privado → hardcoded directly OK.
       b. Si gitignored:
          - Confirmar `nakama_local.cfg` está en `.gitignore` (agregar si falta: `echo "nakama_local.cfg" >> .gitignore`)
          - Crear archivo local en la ruta que Godot mapea como `user://`. En desktop dev: ~/.local/share/godot/app_userdata/BarraBrava/nakama_local.cfg
          - Contenido:
            ```ini
            [nakama]
            server_key="VALOR_REAL_DE_RAILWAY"
            ```
       c. Si hardcoded:
          - Editar `scripts/autoloads/NakamaClient.gd`
          - Reemplazar `const NAKAMA_SERVER_KEY_DEFAULT := "defaultkey"` por el valor real
          - Commit y push (solo si repo privado)
       d. Rebuild APK + reinstalar en device, login, confirmar que `[NakamaClient] initialized` no muestra warnings de auth.

    5. **CHK-03 verificación — VALIDATION.md signed off:**
       ```
       head -10 .planning/phases/01-foundation/01-VALIDATION.md
       ```
       Debe mostrar:
       - `nyquist_compliant: true`
       - `wave_0_complete: true`

    6. **Validar parodia legal** — review rápido de 10-15 entries de clubs.json para verificar que NO hay nombres reales de clubes AFA ni de líderes barra reales.

    7. **Verificación de rate limiting (opcional):** 11 registros consecutivos desde mismo IP → registro #11 debería retornar HTTP 429 o equivalente.

    Si todo verde, escribí "approved". Si reportás issues, describelos.
  </how-to-verify>
  <resume-signal>
    "approved" o descripción de issues
  </resume-signal>
  <acceptance_criteria>
    - Usuario confirmó "approved"
    - smoke-test.sh termina "ALL SMOKE TESTS PASSED" con server key REAL (no defaultkey)
    - VALIDATION.md frontmatter muestra nyquist_compliant=true + wave_0_complete=true
    - NakamaClient.gd tiene server key sync method (gitignored config or hardcoded private)
    - clubs.json review legal pasado
  </acceptance_criteria>
  <done>
    Wave 2 verificada. Backend completo. Server key sync resuelto (CHK-07). VALIDATION.md firmado (CHK-03). Plan 04 puede comenzar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (Godot) → Nakama RPCs | Cliente envía payloads no confiables; server valida TODO |
| Nakama RPC → Storage | Storage permission levels (0/1/2) controlan access pattern |
| Nakama → Resend API | Outbound HTTPS con API key |
| Internet → registration endpoint | Anti-bot via rate limit |
| Reset token storage | Token vive máximo 30 min; eliminado tras uso |
| Server key | Compartido entre Railway env var y NakamaClient.gd (gitignored o hardcoded private) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-RT-01 | Tampering | create_pibe stats from client | mitigate | Stats HARDCODED en server |
| T-1-RT-02 | Information Disclosure | request_password_reset reveals if email exists | mitigate | RPC retorna `{ok:true}` SIEMPRE |
| T-1-RT-03 | Spoofing | Pibe name impersonation (admin, mod) | mitigate | validatePibeName DENY_WORDS |
| T-1-RT-04 | Tampering | Pibe name injection (XSS/SQL) | mitigate | Regex strict + Nakama Storage escape |
| T-1-RT-05 | Denial of Service | Bot mass registration | mitigate | local.yml registration_per_ip_per_min: 10 |
| T-1-RT-06 | Denial of Service | RPC flood per user | mitigate | local.yml rpc_per_user_per_min: 60 |
| T-1-RT-07 | Elevation of Privilege | Replay password reset token | mitigate | Token eliminado tras uso + TTL 30min |
| T-1-RT-08 | Information Disclosure | Server logs leak password reset token | mitigate | Token NUNCA logueado plain |
| T-1-RT-09 | Tampering | clubs.json modified at runtime | mitigate | permissionRead:2 permissionWrite:0 |
| T-1-RT-10 | Repudiation | Account deletion not logged | mitigate | logger.info before delete |
| T-1-RT-11 | Information Disclosure | Server key leaked en repo público | mitigate | CHK-07 fix: nakama_local.cfg gitignored si repo público; hardcoded solo si privado |
</threat_model>

<verification>
1. `cd nakama && npm run typecheck` exit 0
2. `cd nakama && npm run build` produce `build/index.js` > 20KB
3. `grep -c "lunfardo_name" nakama/build/index.js` >= 130
4. clubs.json válido + 5 divisiones + ≥130 entries
5. `bash nakama/smoke-test.sh` con server key real → "ALL SMOKE TESTS PASSED"
6. Stats hardcoded 50/50/50/50 en create_pibe
7. No "faction" en create_pibe (D-10)
8. Rate limits en local.yml (CHK-07 también: server key sync)
9. Railway logs `Clubs seeded` + `5 RPCs registered`
10. VALIDATION.md frontmatter: nyquist_compliant=true, wave_0_complete=true
11. NakamaClient.gd implements `_load_runtime_overrides` (CHK-07)
</verification>

<success_criteria>
- TypeScript runtime compila sin errores
- esbuild produce un solo bundle
- Multi-stage Dockerfile + Railway deploy verde
- Logs muestran seedeo idempotente
- 5 RPCs responden correctamente
- Rate limiting baseline activo (TEC-10)
- smoke-test.sh end-to-end verde con server key real
- Server key sync method documentado (CHK-07)
- VALIDATION.md signed off (CHK-03)
</success_criteria>

<output>
Crear `.planning/phases/01-foundation/01-03-SUMMARY.md` con:
- Cantidad de clubs seedeados
- Hash o tamaño de build/index.js
- Output del smoke-test.sh con server key real
- Link al deploy Railway
- Variables agregadas (RESEND_*, PASSWORD_RESET_BASE_URL)
- Método elegido para server key sync (gitignored cfg vs hardcoded private)
- Confirmación VALIDATION.md signed off
- Próximos pasos Plan 04
</output>
