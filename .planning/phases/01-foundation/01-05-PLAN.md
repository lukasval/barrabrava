---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01
  - 03
files_modified:
  - web/index.html
  - web/privacy/index.html
  - web/privacy/en.html
  - web/terms/index.html
  - web/reset-password/index.html
  - web/reset-password/script.js
  - web/reset-password/style.css
  - web/styles/base.css
  - web/.nojekyll
  - .github/workflows/deploy-web.yml
  - scripts/autoloads/AppConfig.gd
  - scripts/screens/AuthScreen.gd
  - project.godot
  - .planning/phases/01-foundation/AAIP-REGISTRATION.md
  - .planning/phases/01-foundation/LEGAL-NOTES.md
autonomous: false
requirements:
  - PRV-01
  - PRV-02
  - PRV-05
  - CLB-02
tags:
  - privacy
  - legal
  - aaip
  - web
  - github-pages
  - password-reset
  - consent

user_setup:
  - service: github-pages
    why: "Hosting estático para Privacy Policy + Password Reset HTML (zero cost)"
    dashboard_config:
      - task: "Habilitar GitHub Pages para el repo: Settings → Pages → Source = GitHub Actions"
        location: "github.com/{owner}/barrabrava/settings/pages"
      - task: "(Opcional) Configurar custom domain `legal.barrabrava.com.ar` con CNAME al repo Pages"
        location: "DNS provider del dominio + GitHub Pages custom domain field"
  - service: aaip
    why: "Cumplimiento Ley 25.326 Protección de Datos Personales (Argentina) — required pre-launch"
    dashboard_config:
      - task: "Iniciar registro de base de datos en AAIP vía TAD (Trámites a Distancia)"
        location: "argentina.gob.ar/aaip → 'Registro Nacional de Bases de Datos'"
      - task: "Completar formulario con: responsable (CUIT solo dev), datos recolectados (email, password hash, user_id Nakama, pibe_name, club_id), finalidad, transferencia internacional (Railway país disponible + Resend EE.UU.)"
        location: "TAD formulario online"
      - task: "Pagar tasa AAIP (~ARS $5000-10000 según escala)"
        location: "TAD payment step"
      - task: "Guardar número de trámite + screenshot en .planning/phases/01-foundation/AAIP-REGISTRATION.md"
        location: "TAD confirmation page"

must_haves:
  truths:
    - "Privacy Policy en español accesible vía URL pública sin login (PRV-01)"
    - "Privacy Policy lista: datos recolectados, finalidad, retention, derechos ARCO, contacto del responsable, transferencia internacional Railway"
    - "Password reset HTML page hospeda el formulario que toma `?token=...` y llama RPC confirm_password_reset usando Bearer token de device session (NO server key expuesto en JS público — CHK-06 fix)"
    - "AuthScreen del cliente Godot abre el browser del sistema en el URL de privacy cuando el usuario tapea 'leé las reglas del juego' (PRV-01)"
    - "Disclaimer CLB-02 (ya implementado en SplashScreen Plan 04) tiene su contenido legal completo documentado en LEGAL-NOTES.md con justificación de parodia"
    - "AAIP-REGISTRATION.md documenta el número de trámite + fecha + screenshot path (PRV-02)"
    - "No se guarda geolocation en Phase 1 — PlayerStore + Nakama Storage objects NO contienen `lat`, `lng`, `gps`, `location` (PRV-05)"
    - "GitHub Actions workflow `deploy-web.yml` publica `web/` a GitHub Pages en cada push a main"
    - "App Godot lee `privacy_url` desde un AppConfig autoload (no hardcoded), permite override por entorno"
    - "PRV-04 (consent dialogs analytics/push) NOT in scope — deferred a Phase 2 cuando se agreguen GameAnalytics + FCM. Documentado en LEGAL-NOTES.md sin task asociado en este plan (CHK-08 fix)"
  artifacts:
    - path: "web/privacy/index.html"
      provides: "Privacy Policy en español (page principal)"
      contains: "Política de Privacidad"
    - path: "web/privacy/en.html"
      provides: "Privacy Policy English"
      contains: "Privacy Policy"
    - path: "web/terms/index.html"
      provides: "Terms of Service / EULA en español"
      contains: "Términos"
    - path: "web/reset-password/index.html"
      provides: "Form HTML que envía nueva password al RPC confirm_password_reset"
      contains: "confirm_password_reset"
    - path: "web/reset-password/script.js"
      provides: "Cliente JS que obtiene Bearer token via device auth y llama RPC. NO server key expuesto (CHK-06)"
      contains: "authenticate/device"
    - path: ".github/workflows/deploy-web.yml"
      provides: "GitHub Actions deploy to GitHub Pages"
      contains: "actions/deploy-pages"
    - path: "scripts/autoloads/AppConfig.gd"
      provides: "Singleton con URLs públicas"
      contains: "PRIVACY_URL"
    - path: ".planning/phases/01-foundation/AAIP-REGISTRATION.md"
      provides: "Registro de trámite AAIP"
      contains: "Número de trámite"
    - path: ".planning/phases/01-foundation/LEGAL-NOTES.md"
      provides: "Justificación legal + PRV-04 deferral note"
      contains: "parodia"
  key_links:
    - from: "scripts/autoloads/AppConfig.gd"
      to: "scripts/screens/AuthScreen.gd"
      via: "AuthScreen lee AppConfig.PRIVACY_URL en _ready"
      pattern: "AppConfig\\.PRIVACY_URL"
    - from: "web/reset-password/script.js"
      to: "Nakama /v2/account/authenticate/device + /v2/rpc/confirm_password_reset"
      via: "fetch POST device auth → Bearer token → RPC call (NO server key in JS)"
      pattern: "authenticate/device"
    - from: ".github/workflows/deploy-web.yml"
      to: "GitHub Pages"
      via: "actions/upload-pages-artifact + actions/deploy-pages"
      pattern: "deploy-pages"
---

<objective>
Wave 3 — Cerrar los outputs legales y de hosting Phase 1: Privacy Policy en español (PRV-01), AAIP registration trámite iniciado y documentado (PRV-02), Terms of Service mínimos, página HTML del password reset que recibe el `?token=...` del email Resend y llama el RPC `confirm_password_reset` del Plan 03 usando un Bearer token obtenido vía device auth (sin exponer el server key en JS público — fix CHK-06), GitHub Pages workflow que publica todo, AppConfig autoload en Godot para parametrizar las URLs públicas, y verificación explícita de PRV-05 (no GPS).

**PRV-04 está fuera de scope (CHK-08 fix):** Los consent dialogs para analytics + push notifications se diferían a Phase 2 explícitamente en CONTEXT.md (analytics y FCM no están en Phase 1). Este plan documenta esa deferral en LEGAL-NOTES.md pero NO entrega código de consent dialog. Por eso `PRV-04` se elimina de `requirements` frontmatter.

**Propósito:** Sin Privacy Policy pública accesible, App Store y Play Store rechazan el binario. Sin AAIP iniciado, hay riesgo legal (Ley 25.326). Sin la página HTML de reset, el RPC del Plan 03 está aislado — pero la versión original del Plan 05 exponía el server key en JS público (CHK-06 BLOCKER), permitiendo que un atacante con acceso a GitHub Pages JS leyera el key y firmara RPCs arbitrarios. Esta revisión usa device auth (que Nakama permite anónimamente) para obtener un Bearer token ephemeral antes de llamar al RPC.

**Output:**
- `https://{owner}.github.io/barrabrava/privacy/` accesible público sin login
- `https://{owner}.github.io/barrabrava/reset-password/?token=xxx` formulario funcional que NO expone NAKAMA_SERVER_KEY
- `AAIP-REGISTRATION.md` con número de trámite real + screenshot
- AuthScreen Godot abre el browser apuntando al privacy URL al tap del link
- Smoke test: en device físico, recibir email reset → tap link → completar nueva password → poder loguearse con la nueva

**Decisiones críticas:**
- D-15 revisado: el privacy URL apunta al mismo dominio público de prod desde Phase 1; Railway = región LATAM disponible (no São Paulo, ver CONTEXT.md D-15)
- Privacy en español PRIMARIO; inglés solo para store submission
- AAIP es proceso async — esta plan inicia y documenta el inicio, no espera completion
- Password reset HTML usa device auth + fetch + Bearer token (NO server key exposed — CHK-06 fix)

**No incluye:** App Store / Play Store submission (Phase 7), legal review por abogado real (TODO en LEGAL-NOTES.md), consent dialogs PRV-04 (Phase 2 — analytics + FCM aún no existen).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/INFRA-NOTES.md
@.planning/phases/01-foundation/DEFERRED-IOS-CI.md

<interfaces>
<!-- Patterns de RESEARCH.md §6 §7 + AFA fiction disclaimer + Ley 25.326. -->

### web/privacy/index.html (Privacy Policy ES — esqueleto)
[same as before — 10 sections covering Responsable, Datos, Finalidad, Transferencia, Conservación, ARCO, Seguridad, Menores, Cambios, Contacto]

### web/reset-password/index.html
```html
<!DOCTYPE html>
<html lang="es-AR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="referrer" content="no-referrer" />
  <title>Resetear contraseña — BarraBrava</title>
  <link rel="stylesheet" href="/barrabrava/styles/base.css" />
  <link rel="stylesheet" href="/barrabrava/reset-password/style.css" />
</head>
<body>
  <main class="reset">
    <h1>Resetear contraseña</h1>
    <p id="intro">Poné tu nueva contraseña abajo. Mínimo 8 caracteres.</p>
    <form id="reset-form" autocomplete="off">
      <label for="pw">Nueva contraseña</label>
      <input type="password" id="pw" name="pw" minlength="8" required />
      <label for="pw2">Confirmar contraseña</label>
      <input type="password" id="pw2" name="pw2" minlength="8" required />
      <button type="submit" id="submit-btn">Cambiar contraseña</button>
    </form>
    <p id="status" role="status" aria-live="polite"></p>
    <p class="link"><a href="/barrabrava/">← Volver al inicio</a></p>
  </main>
  <script src="/barrabrava/reset-password/script.js" defer></script>
</body>
</html>
```

### web/reset-password/script.js — CHK-06 SECURE REDESIGN
```javascript
// Reset password page — CHK-06 redesigned to AVOID exposing NAKAMA_SERVER_KEY in public JS.
//
// Strategy:
// 1. Get token from URL ?token=<reset-token>
// 2. Perform an unauthenticated device auth call: POST /v2/account/authenticate/device
//    with id = "reset-helper-" + token (Nakama allows anonymous device auth WITHOUT server
//    key Basic auth — the endpoint accepts no-auth requests by default; the public host is
//    injected by deploy workflow but no key is ever shipped to the browser).
// 3. The response contains a short-lived Bearer token (NakamaSession.token).
// 4. Use the Bearer token to call /v2/rpc/confirm_password_reset with the actual reset token
//    in the payload.
//
// Result: the only thing leaked publicly is the host name. No server key in JS.

(function () {
  "use strict";

  // CONFIG — replaced at deploy time by .github/workflows/deploy-web.yml
  const NAKAMA_HOST = "REPLACE_WITH_NAKAMA_HOST";

  const params = new URLSearchParams(window.location.search);
  const token = params.get("token") || "";
  const form = document.getElementById("reset-form");
  const status = document.getElementById("status");
  const intro = document.getElementById("intro");
  const submitBtn = document.getElementById("submit-btn");

  if (!token) {
    intro.textContent = "Link inválido — no se encontró el token.";
    form.style.display = "none";
    return;
  }

  function setStatus(text, kind) {
    status.textContent = text;
    status.dataset.kind = kind || "info";
  }

  // Step 1: Anonymous device auth — Nakama accepts this without a server key when
  // the endpoint is configured publicly (default). The "id" is just a per-flow unique string.
  async function fetchBearerToken() {
    const deviceId = "reset-helper-" + token + "-" + Date.now();
    const resp = await fetch(
      "https://" + NAKAMA_HOST + "/v2/account/authenticate/device?create=true",
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ id: deviceId }),
      }
    );
    if (!resp.ok) {
      throw new Error("device-auth-failed:" + resp.status);
    }
    const data = await resp.json();
    if (!data || !data.token) {
      throw new Error("device-auth-no-token");
    }
    return data.token;
  }

  async function callResetRpc(bearer) {
    // Nakama expects RPC payload as a JSON-encoded string in the body.
    const payload = JSON.stringify({ token: token, new_password: document.getElementById("pw").value });
    const body = JSON.stringify(payload);
    const resp = await fetch(
      "https://" + NAKAMA_HOST + "/v2/rpc/confirm_password_reset?unwrap",
      {
        method: "POST",
        headers: {
          "Authorization": "Bearer " + bearer,
          "Content-Type": "application/json",
        },
        body: body,
      }
    );
    if (!resp.ok) {
      const txt = await resp.text();
      throw new Error(txt || ("rpc-" + resp.status));
    }
    return true;
  }

  form.addEventListener("submit", async function (ev) {
    ev.preventDefault();
    const pw = document.getElementById("pw").value;
    const pw2 = document.getElementById("pw2").value;
    if (pw.length < 8) {
      setStatus("Mínimo 8 caracteres, chabón.", "error");
      return;
    }
    if (pw !== pw2) {
      setStatus("Las contraseñas no coinciden.", "error");
      return;
    }
    submitBtn.disabled = true;
    setStatus("Procesando...", "info");

    try {
      const bearer = await fetchBearerToken();
      await callResetRpc(bearer);
      setStatus("Contraseña actualizada. Volvé a la app y entrá con tu nueva contraseña.", "success");
      form.style.display = "none";
    } catch (e) {
      const msg = String(e && e.message || e);
      if (msg.indexOf("expired") !== -1) {
        setStatus("Link vencido. Pedí uno nuevo desde la app.", "error");
      } else if (msg.indexOf("Invalid") !== -1 || msg.indexOf("invalid") !== -1) {
        setStatus("Link inválido o ya usado.", "error");
      } else if (msg.indexOf("device-auth-failed") !== -1) {
        setStatus("Sin conexión con el servidor. Probá de nuevo en unos segundos.", "error");
      } else {
        setStatus("Algo salió mal: " + msg.slice(0, 200), "error");
      }
      submitBtn.disabled = false;
    }
  });
})();
```

**Security note (CHK-06 closure):** This script ships NO secret to the browser. The only public string is `NAKAMA_HOST` (the same hostname users already see in network traffic — non-secret by design). The Bearer token is obtained per-session via the anonymous device auth endpoint and discarded after the RPC call. The deploy workflow only injects `NAKAMA_HOST`, NEVER `NAKAMA_SERVER_KEY`.

### web/reset-password/style.css
[same as before — dark theme matching app]

### web/styles/base.css
[same as before]

### web/index.html (landing simple)
[same as before — links to privacy + terms]

### web/.nojekyll
File vacío.

### .github/workflows/deploy-web.yml — REDACTED CHK-06 (no server key)
```yaml
name: Deploy Web to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'web/**'
      - '.github/workflows/deploy-web.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Inject Nakama host into reset-password script (NAKAMA_HOST is non-secret)
        run: |
          sed -i "s|REPLACE_WITH_NAKAMA_HOST|${{ secrets.NAKAMA_PUBLIC_HOST }}|g" web/reset-password/script.js
          echo "Verifying host injection (should be 0 remaining placeholders):"
          test $(grep -c "REPLACE_WITH_NAKAMA_HOST" web/reset-password/script.js) -eq 0
          echo "Verifying NO server key got injected (defensive — CHK-06):"
          ! grep -q "NAKAMA_SERVER_KEY\|defaultkey" web/reset-password/script.js
          ! grep -qE "Basic\s+[A-Za-z0-9+/=]+" web/reset-password/script.js

      - name: Inject deploy date into privacy policies
        run: |
          TODAY=$(date +%Y-%m-%d)
          sed -i "s|{YYYY-MM-DD del deploy}|$TODAY|g" web/privacy/index.html || true
          sed -i "s|{YYYY-MM-DD del deploy}|$TODAY|g" web/privacy/en.html || true

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/

      - id: deployment
        uses: actions/deploy-pages@v4
```

**Secret needed:** `NAKAMA_PUBLIC_HOST` (the Railway hostname WITHOUT scheme, NON-SECRET — used as deploy-time templating only). NO `NAKAMA_SERVER_KEY` secret is referenced by this workflow.

### scripts/autoloads/AppConfig.gd
[same as before — PRIVACY_URL, GPS_ENABLED=false, assert]

### scripts/screens/AuthScreen.gd edit
Already handled in Plan 04 Task 1 — AuthScreen reads `AppConfig.PRIVACY_URL` dynamically.

### project.godot
[add AppConfig as first autoload]

### .planning/phases/01-foundation/AAIP-REGISTRATION.md
[same template as before]

### .planning/phases/01-foundation/LEGAL-NOTES.md — extended with PRV-04 deferral note
```markdown
[same content as before, with explicit PRV-04 deferral section]

## PRV-04 — Consent Dialogs (deferred a Phase 2 — CHK-08 confirmation)

PRV-04 originalmente listado en Plan 05 requirements frontmatter. **REMOVIDO** porque:
- Analytics (GameAnalytics) NO está integrado en Phase 1
- Push notifications (FCM) NO están integradas en Phase 1
- Sin esos servicios, NO HAY datos opt-in que solicitar consentimiento

Cuando Phase 2 (Heartbeat AFA) o Phase 6 (Polish) integren analytics o FCM, agregar:
- **GameAnalytics opt-in**: dialog explícito al primer launch post-update con copy "¿Querés ayudar a mejorar el juego compartiendo estadísticas anónimas?". Default: OFF.
- **FCM push notifications**: dialog al primer login post-update con copy "¿Querés que te avisemos cuándo tu club juega un clásico?". Default: OFF.

Until then, AppConfig.gd enforza:
- `ANALYTICS_ENABLED = false`
- `PUSH_NOTIFICATIONS_ENABLED = false`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estructura web/ + Privacy Policy + Terms + Reset Password HTML (CHK-06 secure redesign) + GitHub Pages workflow</name>
  <files>
    web/index.html
    web/privacy/index.html
    web/privacy/en.html
    web/terms/index.html
    web/reset-password/index.html
    web/reset-password/script.js
    web/reset-password/style.css
    web/styles/base.css
    web/.nojekyll
    .github/workflows/deploy-web.yml
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (PRV-01..03 + PRV-05)
    - .planning/phases/01-foundation/01-03-SUMMARY.md (RPC confirm_password_reset payload shape)
    - .planning/phases/01-foundation/01-RESEARCH.md §6 password reset flow
    - .planning/phases/01-foundation/INFRA-NOTES.md (Railway host actual)
    - .planning/phases/01-foundation/01-VALIDATION.md (T-1-WEB-02 mitigation expected)
  </read_first>
  <action>
    **1. `web/.nojekyll`** — archivo vacío.

    **2. `web/styles/base.css`** — dark theme compartido (copy literal de `<interfaces>` Plan 05 original).

    **3. `web/index.html`** — landing con links a privacy + terms.

    **4. `web/privacy/index.html`** — Privacy Policy ES con las 10 secciones (Responsable, Datos, Finalidad, Transferencia, Conservación, ARCO, Seguridad, Menores, Cambios, Contacto). Placeholders `{nombre dev}`, `{CUIT}`, `{ciudad provincia}`, `{NRO-TRAMITE-AAIP}` quedan literales hasta Task 3.

    Nota sobre Railway región: la sección 4 (Transferencia Internacional) ya NO dice "São Paulo (Brasil)". Reemplazar por: "Los servidores de la App (Nakama + PostgreSQL) están alojados en **Railway, región LATAM** (proveedor con presencia regional adecuada). Argentina considera a Brasil, EE.UU. y otros países con nivel adecuado de protección. La región exacta queda especificada en AAIP-REGISTRATION.md."

    **5. `web/privacy/en.html`** — traducción literal de privacy/index.html.

    **6. `web/terms/index.html`** — Terms of Service ES (8 secciones: Aceptación, Naturaleza del Servicio, Cuentas y Conducta, UGC, Modificaciones, Limitación de Responsabilidad, Ley Aplicable, Contacto). Declara explícitamente "parodia ficticia" + "no afiliado a la AFA".

    **7. `web/reset-password/index.html`** — copy literal del bloque `<interfaces>`. Incluye `<meta name="referrer" content="no-referrer">` (T-1-WEB-02 mitigation defensive).

    **8. `web/reset-password/script.js`** — **CHK-06 SECURE REDESIGN**. Copy literal del bloque `<interfaces>` arriba. Cambios clave vs versión original:
       - Elimina `NAKAMA_KEY = "defaultkey"` (NO había server key en JS)
       - Elimina `auth = "Basic " + btoa(...)`
       - Agrega `fetchBearerToken()` que llama `/v2/account/authenticate/device?create=true` con un `id` único per-flow para obtener una NakamaSession.token
       - `callResetRpc(bearer)` envía `Authorization: Bearer <token>` al RPC
       - Si device auth falla, status muestra error específico

       **Defense in depth:** El JS también incluye un grep de su propio source (en el workflow CHK below) para confirmar que NO contiene "NAKAMA_SERVER_KEY", "defaultkey", o un patrón `Basic xxxxxxxx=`. Si alguno aparece, el deploy falla.

    **9. `web/reset-password/style.css`** — copy literal estilos dark theme.

    **10. `.github/workflows/deploy-web.yml`** — copy literal del bloque `<interfaces>` revisado. Cambios vs original:
       - Step "Inject Nakama host" mantiene la sustitución de `REPLACE_WITH_NAKAMA_HOST` (host es público, no secret)
       - **Agregado:** después de inject, verificación de seguridad:
         ```yaml
         echo "Verifying NO server key got injected (defensive — CHK-06):"
         ! grep -q "NAKAMA_SERVER_KEY\|defaultkey" web/reset-password/script.js
         ! grep -qE "Basic\s+[A-Za-z0-9+/=]+" web/reset-password/script.js
         ```
         Si cualquier de esos greps encuentra match → exit non-zero → deploy falla → CHK-06 stays protected.
       - El secret `NAKAMA_PUBLIC_HOST` sigue siendo necesario (host = no secret, pero usar GH secret evita commitearlo)
       - NO referenciar `NAKAMA_SERVER_KEY` como GitHub secret en este workflow (eso pertenece a Railway only).

    Commit: `feat(01): privacy/terms/reset-password HTML + secure deploy workflow (CHK-06: Bearer auth, no server key in JS)`.
  </action>
  <verify>
    <automated>test -f web/.nojekyll && test -f web/index.html && test -f web/privacy/index.html && test -f web/privacy/en.html && test -f web/terms/index.html && test -f web/reset-password/index.html && test -f web/reset-password/script.js && test -f web/reset-password/style.css && test -f web/styles/base.css && test -f .github/workflows/deploy-web.yml && grep -q "Política de Privacidad" web/privacy/index.html && grep -q "Ley 25.326" web/privacy/index.html && grep -q "ARCO\|Acceso.*Rectificación" web/privacy/index.html && grep -q "Privacy Policy" web/privacy/en.html && grep -q "Términos" web/terms/index.html && grep -q "parodia ficticia" web/terms/index.html && grep -q "confirm_password_reset" web/reset-password/script.js && grep -q "authenticate/device" web/reset-password/script.js && ! grep -q "NAKAMA_SERVER_KEY\|defaultkey" web/reset-password/script.js && ! grep -qE "Basic\s*\+\s*btoa" web/reset-password/script.js && grep -q "REPLACE_WITH_NAKAMA_HOST" web/reset-password/script.js && grep -q "actions/deploy-pages" .github/workflows/deploy-web.yml && grep -q "secrets.NAKAMA_PUBLIC_HOST" .github/workflows/deploy-web.yml && ! grep -q "secrets.NAKAMA_SERVER_KEY" .github/workflows/deploy-web.yml && grep -q "Bearer" web/reset-password/script.js</automated>
  </verify>
  <acceptance_criteria>
    - 10 secciones en privacy/index.html (Responsable, Datos, Finalidad, Transferencia, Conservación, ARCO, Seguridad, Menores, Cambios, Contacto)
    - Privacy cita Ley 25.326 + transferencia internacional Railway (sin mencionar São Paulo específico)
    - Terms declara "parodia ficticia" + jurisdicción CABA + sin afiliación AFA
    - Reset password form tiene fields pw + pw2 + button
    - **script.js usa device auth + Bearer token (CHK-06 secure)**
    - **script.js NO contiene "NAKAMA_SERVER_KEY", "defaultkey", ni "Basic + btoa(...)" pattern**
    - script.js incluye `<meta referrer no-referrer>` (T-1-WEB-02 defensive)
    - deploy-web.yml inyecta solo NAKAMA_PUBLIC_HOST + post-inject verifica ausencia de server key (defense in depth)
    - **deploy-web.yml NO referencia secrets.NAKAMA_SERVER_KEY**
    - Commit local realizado
  </acceptance_criteria>
  <done>
    Assets web listos. CHK-06 cerrado: ningún JS público contiene ni el server key ni Basic auth con server key. Task 2 wires Godot client. Task 3 documenta AAIP. Task 4 verifica end-to-end.
  </done>
</task>

<task type="auto">
  <name>Task 2: AppConfig autoload + AAIP-REGISTRATION.md + LEGAL-NOTES.md (con PRV-04 deferral) + verificación PRV-05</name>
  <files>
    scripts/autoloads/AppConfig.gd
    project.godot
    .planning/phases/01-foundation/AAIP-REGISTRATION.md
    .planning/phases/01-foundation/LEGAL-NOTES.md
  </files>
  <read_first>
    - scripts/screens/AuthScreen.gd (Plan 04 — ya lee AppConfig.PRIVACY_URL)
    - project.godot (Plan 04 — sección [autoload])
    - scripts/autoloads/AppTheme.gd (patrón de autoload)
    - .planning/phases/01-foundation/01-CONTEXT.md (responsable info + D-15 revisado)
  </read_first>
  <action>
    **1. `scripts/autoloads/AppConfig.gd`** — singleton con URLs públicas + feature flags:
    ```gdscript
    extends Node

    # Centralized public URLs and runtime config.
    # Phase 1 — values hardcoded; future phases can override via env or remote config.

    const SITE_BASE := "https://{OWNER}.github.io/barrabrava"

    const PRIVACY_URL := SITE_BASE + "/privacy/"
    const PRIVACY_URL_EN := SITE_BASE + "/privacy/en.html"
    const TERMS_URL := SITE_BASE + "/terms/"
    const RESET_PASSWORD_BASE := SITE_BASE + "/reset-password/"
    const SUPPORT_EMAIL := "soporte@barrabrava.com.ar"
    const PRIVACY_EMAIL := "privacidad@barrabrava.com.ar"

    # Feature flags Phase 1 (PRV-04 enforcement — analytics/push deferred to Phase 2).
    const ANALYTICS_ENABLED := false   # GameAnalytics opt-in dialog vendrá en Phase 2
    const PUSH_NOTIFICATIONS_ENABLED := false   # FCM en Phase 2
    const GPS_ENABLED := false   # PRV-05 — locked OFF en Phase 1

    func _ready() -> void:
        # PRV-05 enforcement: GPS must remain disabled in Phase 1
        assert(not GPS_ENABLED, "PRV-05: GPS must remain disabled in Phase 1")
        # PRV-04 enforcement: until consent dialogs ship, analytics/push must stay OFF
        assert(not ANALYTICS_ENABLED, "PRV-04: ANALYTICS disabled until consent dialog ships (Phase 2)")
        assert(not PUSH_NOTIFICATIONS_ENABLED, "PRV-04: PUSH disabled until consent dialog ships (Phase 2)")
        print("[AppConfig] site=%s analytics=%s push=%s gps=%s" % [SITE_BASE, ANALYTICS_ENABLED, PUSH_NOTIFICATIONS_ENABLED, GPS_ENABLED])
    ```

    Sustituir `{OWNER}` con el GitHub username/org real (leer de `git remote get-url origin`).

    **2. Editar `project.godot`** [autoload] para agregar `AppConfig` como **primera** línea:
    ```
    [autoload]
    AppConfig="*res://scripts/autoloads/AppConfig.gd"
    NakamaClient="*res://scripts/autoloads/NakamaClient.gd"
    AuthManager="*res://scripts/autoloads/AuthManager.gd"
    AppTheme="*res://scripts/autoloads/AppTheme.gd"
    PlayerStore="*res://scripts/autoloads/PlayerStore.gd"
    FlowRouter="*res://scripts/autoloads/FlowRouter.gd"
    ```

    **3. `.planning/phases/01-foundation/AAIP-REGISTRATION.md`** — template para que el usuario complete:
    ```markdown
    # AAIP Registration — Ley 25.326

    **Estado:** pendiente — usuario inicia trámite en Task 3
    **Fecha de inicio:** {YYYY-MM-DD}
    **Número de trámite TAD:** {N°}
    **CUIT declarado:** {CUIT}
    **Tipo de base:** Privada — datos personales de usuarios

    ## Datos declarados

    | Categoría | Datos | Finalidad |
    |-----------|-------|-----------|
    | Identificación | Email | Autenticación |
    | Credenciales | Hash bcrypt password | Autenticación |
    | Perfil | user_id Nakama, pibe_name, club_id, timestamps | Funcionalidad del juego |
    | Transferencia internacional | Sí — Railway (región LATAM disponible — NO São Paulo, ver CONTEXT.md D-15) + EE.UU. (Resend) | Hosting + comunicaciones |

    ## Documentos adjuntos

    - {Path al screenshot del formulario TAD enviado}
    - {Path al comprobante de pago de tasa AAIP}
    - {Path a la confirmación de número de trámite}

    ## Próximos pasos

    - Esperar revisión AAIP (~2-4 semanas)
    - Si solicitan correcciones, actualizar este archivo
    - Una vez aprobado, actualizar `web/privacy/index.html` y `en.html` reemplazando `{NRO-TRAMITE-AAIP}` por el número final
    - Una vez aprobado, actualizar este archivo con `Estado: aprobado` y fecha

    ## Referencias

    - [AAIP — Registro de Bases de Datos](https://www.argentina.gob.ar/aaip/datospersonales/registrar-base-de-datos)
    - Ley 25.326 — Protección de Datos Personales
    ```

    **4. `.planning/phases/01-foundation/LEGAL-NOTES.md`** — incluir disclaimer parodia + transferencia internacional + **PRV-04 deferral explícito (CHK-08 fix)**:
    ```markdown
    # Legal Notes — Phase 1

    ## Disclaimer de Parodia (CLB-02)

    **Copy en App (SplashScreen):**
    > "Esto es una parodia de fútbol argentino. Cualquier parecido con personas reales es coincidencia."

    **Justificación:**
    - BarraBrava NO utiliza nombres oficiales de clubes AFA
    - Nombres inventados en lunfardo, evocadores pero no idénticos
    - Escudos = arquetipos genéricos (8 formas predefinidas)
    - Colores pueden recordar paletas conocidas — uso evocativo parodia
    - NO nombres reales de líderes barra existentes
    - NO eventos reales violentos específicos
    - NO contenido político partidario

    **Riesgo residual:** trademark holders podrían disputar. Mitigation:
    - Review IP lawyer ANTES soft launch (Phase 7)
    - Protocolo de cambio de nombre por cease & desist
    - Changelog de nombres usados

    ## Transferencia Internacional (Ley 25.326 art. 12)

    | País | Servicio | Datos | Nivel de adecuación AAIP |
    |------|----------|-------|-------------------------|
    | Región LATAM (Railway, NO São Paulo — ver D-15 revisado) | Nakama + Postgres | Todos los datos de usuario | Verificar según región exacta — usuario confirma en AAIP-REGISTRATION.md |
    | EE.UU. | Resend (emails) | Email + contenido reset | No adecuado por sí solo — base legal: ejecución del contrato |

    ## Contacto del Responsable
    - Email general: `soporte@barrabrava.com.ar`
    - Email privacidad: `privacidad@barrabrava.com.ar`

    ## PRV-04 — Consent Dialogs (deferred a Phase 2 — CHK-08 closure)

    PRV-04 fue removido de Plan 05 `requirements` frontmatter porque:
    - Analytics (GameAnalytics) NO está integrado en Phase 1
    - Push notifications (FCM) NO están integradas en Phase 1
    - Sin esos servicios, NO HAY datos opt-in que solicitar consentimiento

    **Enforcement Phase 1:** `AppConfig.gd` mantiene `ANALYTICS_ENABLED=false` y `PUSH_NOTIFICATIONS_ENABLED=false` con `assert()` en `_ready()` que enforza ambos como false. Cualquier intento futuro de poner true SIN agregar consent dialog primero rompe el boot.

    **Cuando Phase 2 integre analytics:**
    - **GameAnalytics opt-in**: dialog explícito al primer launch post-update con copy "¿Querés ayudar a mejorar el juego compartiendo estadísticas anónimas?". Default: OFF.
    - **FCM push notifications**: dialog al primer login post-update con copy "¿Querés que te avisemos cuándo tu club juega un clásico?". Default: OFF.

    ## PRV-05 — Sin Tracking de GPS

    - `grep -ri "Geolocation\|location_request\|gps\|latitude\|longitude" scripts/ scenes/` → 0 matches
    - `AppConfig.GPS_ENABLED = false` con `assert()` en `_ready()`

    ## TODOs pre-launch (Phase 7)

    - [ ] IP lawyer review (1h)
    - [ ] AAIP aprobación final + actualizar número en privacy/index.html
    - [ ] Verificar `privacidad@barrabrava.com.ar` reciba mails
    - [ ] Traducir terms/index.html a inglés
    - [ ] Updatear COPPA / GDPR / CCPA si app expande fuera de LATAM
    ```

    **5. Verificación PRV-05** correr:
    ```
    grep -ri "Geolocation\|location_request\|gps_enabled\|requestLocation\|CLLocationManager\|ACCESS_FINE_LOCATION" scripts/ scenes/ web/ nakama/src/ 2>/dev/null || echo "PASS"
    ```

    **6. Verificación AppConfig assert:**
    ```
    godot --headless --check-only --path . 2>&1 | tee /tmp/check.log
    ```

    Commit: `feat(01): AppConfig autoload + AAIP/LEGAL docs + PRV-05 enforce + PRV-04 deferral note (CHK-08)`.
  </action>
  <verify>
    <automated>test -f scripts/autoloads/AppConfig.gd && grep -q "PRIVACY_URL" scripts/autoloads/AppConfig.gd && grep -q "GPS_ENABLED := false" scripts/autoloads/AppConfig.gd && grep -q "ANALYTICS_ENABLED := false" scripts/autoloads/AppConfig.gd && grep -q "PUSH_NOTIFICATIONS_ENABLED := false" scripts/autoloads/AppConfig.gd && grep -q "assert(not GPS_ENABLED" scripts/autoloads/AppConfig.gd && grep -q "assert(not ANALYTICS_ENABLED" scripts/autoloads/AppConfig.gd && grep -q "AppConfig=\"\*res://scripts/autoloads/AppConfig.gd\"" project.godot && test -f .planning/phases/01-foundation/AAIP-REGISTRATION.md && grep -q "Ley 25.326" .planning/phases/01-foundation/AAIP-REGISTRATION.md && test -f .planning/phases/01-foundation/LEGAL-NOTES.md && grep -q "parodia" .planning/phases/01-foundation/LEGAL-NOTES.md && grep -q "CLB-02" .planning/phases/01-foundation/LEGAL-NOTES.md && grep -q "PRV-05" .planning/phases/01-foundation/LEGAL-NOTES.md && grep -q "PRV-04" .planning/phases/01-foundation/LEGAL-NOTES.md && grep -q "CHK-08" .planning/phases/01-foundation/LEGAL-NOTES.md && ! grep -ri "Geolocation\|location_request\|gps_enabled\|requestLocation\|CLLocationManager\|ACCESS_FINE_LOCATION" scripts/ scenes/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - AppConfig.gd con URLs públicas + 3 feature flags (analytics, push, gps todos=false)
    - AppConfig.gd `_ready()` con 3 asserts (gps, analytics, push)
    - project.godot [autoload] tiene AppConfig como primera línea
    - AAIP-REGISTRATION.md con secciones: estado, fecha, número, datos declarados (Railway región LATAM, NO São Paulo), documentos, próximos pasos
    - LEGAL-NOTES.md con sección explícita "PRV-04 — Consent Dialogs (deferred a Phase 2 — CHK-08 closure)"
    - LEGAL-NOTES.md confirma transferencia internacional sin mencionar São Paulo específico
    - `grep -ri "Geolocation..."` retorna 0 matches (PRV-05)
    - Commit local realizado
  </acceptance_criteria>
  <done>
    Cliente Godot wireado a URLs públicas. Docs legales con PRV-04 deferral documentado. PRV-05 enforced (assert runtime + ausencia código). CHK-08 closed.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User setup — Iniciar trámite AAIP + configurar secret NAKAMA_PUBLIC_HOST + (opcional) custom domain</name>
  <what-built>
    Setup administrativo manual (cuenta AAIP, secret en GH settings, DNS).
  </what-built>
  <how-to-verify>
    [Same as Plan 05 original Task 3 — incluye:
    - Paso 1: configurar secret NAKAMA_PUBLIC_HOST (host, no key)
    - Paso 2: habilitar GitHub Pages
    - Paso 3 (opcional): custom domain legal.barrabrava.com.ar
    - Paso 4: iniciar trámite AAIP via TAD
    - Paso 5: actualizar privacy policies con número AAIP real

    **CHK-06 specific check en Paso 1:** confirmar que el secret se llama `NAKAMA_PUBLIC_HOST` (host) y NO `NAKAMA_SERVER_KEY` (key). El workflow deploy-web.yml SOLO necesita el host. Si por error se configuró el server key, eliminarlo del repo Settings → Secrets.]
  </how-to-verify>
  <resume-signal>
    "approved — AAIP trámite #{NRO}" o descripción de issues
  </resume-signal>
  <acceptance_criteria>
    - Secret `NAKAMA_PUBLIC_HOST` configurado (NO `NAKAMA_SERVER_KEY` en GH secrets para este repo)
    - GitHub Pages habilitado
    - URLs públicas accesibles
    - AppConfig.gd actualizado con SITE_BASE correcto
    - Trámite AAIP iniciado + número documentado
    - web/privacy actualizado con número AAIP
    - Usuario reportó "approved"
  </acceptance_criteria>
  <done>
    Infra y trámite legal en marcha. CHK-06 doubly confirmed (no server key in GH secrets). Task 4 verifica end-to-end.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificación humana — Reset password end-to-end (CHK-06) + privacy reachable + PRV-04/05 enforcement</name>
  <what-built>
    - Privacy + Terms publicados en GitHub Pages
    - Reset password HTML funcional usando Bearer token (CHK-06 secure)
    - AppConfig autoload wirea URLs + feature flags
    - AAIP iniciado
    - PRV-05 enforced (sin GPS)
    - PRV-04 deferral documentado (CHK-08)
  </what-built>
  <how-to-verify>
    **Test 1 — Privacy reachable from app (PRV-01):** [Same as before]

    **Test 2 — Password reset flow end-to-end (D-03, CHK-02 + CHK-06):**
    1. App: registrar con email REAL
    2. Logout (o cerrar app)
    3. Reabrir → AuthScreen tab Entrar → tap "¿Olvidaste tu contraseña?" → ForgotPasswordScreen
    4. Ingresar el email REAL → tap "Enviar reseteo" → ver status uniforme
    5. Revisar inbox: email Resend con link al HTML
    6. Tap el link → browser abre `https://{owner}.github.io/barrabrava/reset-password/?token=XXX`
    7. **CHK-06 inspect: View Source de la página** (en browser dev tools)
       - Confirmar que `script.js` NO contiene la string "defaultkey", "NAKAMA_SERVER_KEY", ni "Basic" + base64
       - Confirmar que SI contiene "authenticate/device" y "Bearer"
    8. Ingresar nueva password en ambos campos → tap "Cambiar contraseña"
    9. **Verificación network tab del browser:**
       - Primer fetch: `POST /v2/account/authenticate/device` → 200 → response contiene token
       - Segundo fetch: `POST /v2/rpc/confirm_password_reset` con header `Authorization: Bearer <token-de-step-1>` → 200
    10. Status: "Contraseña actualizada..."
    11. Volver a la app, login con vieja password → fail
    12. Login con nueva password → success → HomeScreen

    **Test 3 — Token expiration & reuse (T-1-RT-07):**
    1. Pedir reset password nuevo
    2. Usar el token una vez → success
    3. Re-usar el mismo token desde el browser → debe mostrar "Link inválido o ya usado"

    **Test 4 — PRV-05 enforcement (no GPS):**
    1. `unzip -p BarraBrava-debug.apk AndroidManifest.xml | strings | grep -i permission` → NO `ACCESS_*_LOCATION`
    2. Settings → Apps → BarraBrava → Permissions → NO ubicación solicitada
    3. `adb logcat | grep AppConfig` → `[AppConfig] site=... analytics=false push=false gps=false`

    **Test 5 — PRV-04 enforcement (CHK-08):**
    1. Verificar `AppConfig.gd` tiene 3 asserts (gps, analytics, push) que enforzan = false
    2. Verificar logs Godot al inicio muestran las 3 flags = false
    3. Verificar LEGAL-NOTES.md tiene sección "PRV-04 — Consent Dialogs (deferred a Phase 2 — CHK-08 closure)"

    **Test 6 — AAIP trámite documentado:**
    1. `.planning/phases/01-foundation/AAIP-REGISTRATION.md` tiene número + fecha (no placeholders)
    2. `web/privacy/index.html` cita el número AAIP real

    **Test 7 — Terms reachable:** [Same as before]

    **Test 8 — Disclaimer parodia (CLB-02):** [Same as before]

    **Test 9 (NEW) — CHK-06 deploy workflow defense:**
    1. En GitHub Actions, abrir el último run de "Deploy Web to GitHub Pages"
    2. Step "Inject Nakama host..." debe mostrar:
       - `Verifying NO server key got injected (defensive — CHK-06):` → pass
       - `test $(grep -c REPLACE_WITH_NAKAMA_HOST...) -eq 0` → pass
    3. Si el step falla, el deploy NO debe completar — la defensa funciona

    Si los 9 tests pasan, escribí "approved". Si reportás issues, descrítelos.
  </how-to-verify>
  <resume-signal>
    "approved" o descripción de issues
  </resume-signal>
  <acceptance_criteria>
    - Test 1 privacy reachable: pass
    - Test 2 reset password end-to-end usando Bearer (CHK-06): pass
    - Test 3 token reuse rejected: pass
    - Test 4 PRV-05 (no GPS): pass
    - Test 5 PRV-04 enforcement (CHK-08): pass
    - Test 6 AAIP documentado: pass
    - Test 7 Terms accesible: pass
    - Test 8 disclaimer CLB-02: pass
    - Test 9 CHK-06 deploy workflow defense: pass
    - Usuario reportó "approved"
  </acceptance_criteria>
  <done>
    Phase 1 outputs completos: app jugable + Privacy pública + AAIP iniciado + Password reset SECURE (CHK-06 Bearer auth, no server key) + Sin GPS + PRV-04 deferral documentado. Phase 1 ready para `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Email recipient → reset HTML page | Token capability con TTL 30 min |
| Reset HTML page → Nakama RPC | fetch HTTPS POST con Bearer token obtenido vía device auth (CHK-06 fix) |
| GitHub Pages → public HTTPS | Hosting estático servido por GitHub. CDN público — TODO el HTML/JS es leíble por cualquiera |
| AppConfig consts → cliente compilado | URLs públicas en binario Godot |
| AAIP TAD ↔ Argentine government | Transferencia datos del dev (no de usuarios) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-WEB-01 | Tampering | Reset HTML JS modified by MITM | mitigate | GitHub Pages HTTPS (TLS) |
| T-1-WEB-02 | Information Disclosure | Token leak via Referer | mitigate | `<meta name="referrer" content="no-referrer">` en index.html |
| T-1-WEB-03 | Elevation of Privilege | XSS via token query param | mitigate | Token solo en URLSearchParams + JSON body, nunca innerHTML |
| T-1-WEB-04 | Spoofing | Phishing clone | accept | Phase 1 acepta riesgo |
| T-1-WEB-05 | Denial of Service | Mass fetch al RPC | mitigate | Nakama rate limit 60/min + device auth requiere round-trip extra |
| T-1-WEB-06 | Repudiation | User claims no reset | mitigate | Server log + Resend log |
| T-1-WEB-07 | Information Disclosure | Privacy contains PII real del dev | accept | Necesario Ley 25.326 |
| T-1-WEB-08 | Tampering | Workflow deploys malicious web/ | mitigate | Branch protection main + no PR external auto-deploy |
| T-1-WEB-09 | Information Disclosure | Stale privacy after update | mitigate | Workflow auto on push + CDN TTL ~10min |
| T-1-WEB-10 | Elevation of Privilege | Privacy declarada ≠ datos reales | mitigate | LEGAL-NOTES checklist + PRV-05 grep |
| T-1-WEB-11 | Information Disclosure | Server key exposed in public JS | **MITIGATED (CHK-06)** | Script usa device auth → Bearer token, NO server key. Workflow grep verifica ausencia. Tres capas defensivas: source (no key in script.js), inject (only NAKAMA_PUBLIC_HOST), post-inject grep (fails build if "defaultkey"/"NAKAMA_SERVER_KEY"/"Basic <base64>" appear) |
</threat_model>

<verification>
1. Privacy + Terms + Reset HTML + script.js + style.css + base.css + .nojekyll + deploy-web.yml all exist
2. `grep -q "Ley 25.326" web/privacy/index.html`
3. `grep -q "confirm_password_reset" web/reset-password/script.js`
4. **`grep -q "authenticate/device" web/reset-password/script.js`** (CHK-06)
5. **`! grep -q "NAKAMA_SERVER_KEY\|defaultkey" web/reset-password/script.js`** (CHK-06)
6. **`! grep -qE "Basic\s+[A-Za-z0-9+/=]+" web/reset-password/script.js`** (CHK-06)
7. `grep -q "Bearer" web/reset-password/script.js` (CHK-06)
8. `grep -q "secrets.NAKAMA_PUBLIC_HOST" .github/workflows/deploy-web.yml`
9. `! grep -q "secrets.NAKAMA_SERVER_KEY" .github/workflows/deploy-web.yml` (CHK-06)
10. `grep -q "AppConfig" project.godot`
11. AppConfig.gd asserts: gps + analytics + push (PRV-05 + PRV-04 enforce)
12. `! grep -ri "ACCESS_FINE_LOCATION\|requestLocation\|CLLocationManager" scripts/ scenes/`
13. AAIP-REGISTRATION.md + LEGAL-NOTES.md exist
14. LEGAL-NOTES.md tiene sección "PRV-04 — Consent Dialogs (deferred a Phase 2 — CHK-08 closure)"
15. Manual: GitHub Pages deploy verde + URL accesible
16. Manual: end-to-end reset password con Bearer auth
</verification>

<success_criteria>
- Privacy Policy en español accesible vía URL pública sin login (PRV-01)
- Terms of Service declara parodia ficticia
- Reset password HTML form funcional usando Bearer token via device auth (CHK-06 secure — no server key exposed)
- Deploy workflow tiene defense-in-depth check que falla si server key aparece en JS
- GitHub Actions deploy-web.yml publica `web/` automáticamente
- AppConfig autoload centraliza URLs + 3 feature flags con asserts
- AuthScreen Godot abre browser al privacy URL
- AAIP trámite iniciado + número documentado (PRV-02)
- LEGAL-NOTES.md documenta disclaimer + transferencia internacional + PRV-04 deferral (CHK-08)
- PRV-04 explícitamente OUT of Phase 1 scope, enforced via assert
- PRV-05 enforced: 0 referencias GPS en codebase + assert runtime
- APK no declara permisos location
- End-to-end reset password con Bearer auth funcional en device
</success_criteria>

<output>
Crear `.planning/phases/01-foundation/01-05-SUMMARY.md` con:
- URL pública final del privacy
- Número de trámite AAIP + fecha
- Screenshot/log del deploy verde
- Network tab dump del reset flow (mostrando Bearer auth, no server key)
- Verificación PRV-05 + PRV-04 enforcement
- Confirmación CHK-06 closure (3 capas defensivas: source, inject, post-inject grep)
- Confirmación CHK-08 closure (PRV-04 removed from requirements, documented as deferred)
- TODOs Phase 7 (legal review, AAIP final, custom domain si no se hizo)
- Resumen final Phase 1 outputs
</output>
