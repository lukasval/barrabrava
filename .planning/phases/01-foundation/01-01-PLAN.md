---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .github/workflows/build-android-debug.yml
  - fly.toml
  - Dockerfile.nakama
  - .gitignore
  - README.md
  - export_presets.cfg
  - .planning/phases/01-foundation/INFRA-NOTES.md
  - .planning/phases/01-foundation/DEFERRED-IOS-CI.md
autonomous: false
requirements:
  - TEC-01
  - TEC-08
  - TEC-02
  - TEC-03
  - PRV-02
tags:
  - infrastructure
  - cicd
  - railway
  - flyio
  - nakama
  - godot
  - aaip

user_setup:
  - service: railway
    why: "Hosting Nakama + Postgres (placeholder hasta migrar a Fly.io)"
    env_vars:
      - name: DATABASE_URL
        source: "Railway Dashboard -> Postgres plugin -> Variables -> DATABASE_URL"
      - name: NAKAMA_SERVER_KEY
        source: "Generar string aleatorio min 16 chars (openssl rand -hex 16)"
      - name: NAKAMA_CONSOLE_USERNAME
        source: "Elegir nombre de admin (ej: admin)"
      - name: NAKAMA_CONSOLE_PASSWORD
        source: "Generar password fuerte (1Password / openssl rand -base64 24)"
      - name: NAKAMA_SESSION_ENCRYPTION_KEY
        source: "openssl rand -base64 32 (mín 32 chars)"
      - name: NAKAMA_SESSION_REFRESH_ENCRYPTION_KEY
        source: "openssl rand -base64 32 (mín 32 chars, distinto al anterior)"
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create -> copy (resend.com)"
    dashboard_config:
      - task: "Crear cuenta Railway + nuevo proyecto 'barrabrava-nakama'"
        location: "railway.app -> New Project -> Empty Project"
      - task: "Agregar plugin Postgres al proyecto"
        location: "Railway Project -> + New -> Database -> PostgreSQL"
      - task: "Conectar repositorio GitHub a Railway"
        location: "Railway Project -> + New -> GitHub Repo -> Authorize"
      - task: "Configurar todas las env vars listadas arriba"
        location: "Railway service -> Variables tab"
      - task: "Registrar dominio (sugerencia: barrabrava.com.ar) para Resend SMTP"
        location: "Registrar.ar / Namecheap / Cloudflare Registrar"
      - task: "Crear cuenta Resend + verificar dominio (DKIM/SPF DNS records)"
        location: "resend.com/domains -> Add Domain -> seguir instrucciones DNS"
      - task: "Iniciar registro AAIP (Ley 25.326) — proceso async, 2-4 semanas"
        location: "argentina.gob.ar/aaip -> Trámites a Distancia -> CUIT del dev"
  - service: github
    why: "Repositorio + CI/CD para Android debug builds"
    dashboard_config:
      - task: "Crear repo barrabrava (si no existe) + push initial code"
        location: "github.com/new"
      - task: "Crear branch protection rules en 'main' (require PR + CI green)"
        location: "Repo Settings -> Branches -> Add rule -> main"

must_haves:
  truths:
    - "Push a develop dispara build de Android debug APK en GitHub Actions"
    - "Pipeline CI termina verde y produce artefacto APK descargable"
    - "Nakama responde 200 en /healthcheck desde host público Railway"
    - "Postgres está corriendo y conectado a Nakama (logs muestran 'database connection established')"
    - "Repositorio tiene branches main + develop con protection en main"
    - "Registro AAIP fue iniciado (capture de pantalla del trámite en INFRA-NOTES.md)"
    - "DEFERRED-IOS-CI.md documenta que el workflow iOS IPA se difiere a Phase 7 (TEC-08 Phase 1 scope = solo Android)"
  artifacts:
    - path: ".github/workflows/build-android-debug.yml"
      provides: "GitHub Actions workflow para APK debug"
      contains: "barichello/godot-ci:4.3"
    - path: "fly.toml"
      provides: "Fly.io deployment manifest (preparado para migración futura)"
      contains: "primary_region = \"gru\""
    - path: "Dockerfile.nakama"
      provides: "Imagen Nakama 3.x con TypeScript runtime mount"
      contains: "FROM heroiclabs/nakama"
    - path: "export_presets.cfg"
      provides: "Godot export config para Android (debug) solamente — iOS deferred a Phase 7"
      contains: "platform=\"Android\""
    - path: ".planning/phases/01-foundation/INFRA-NOTES.md"
      provides: "Bitácora de infra: URLs Railway, fecha inicio AAIP, dominio registrado"
      contains: "Railway URL"
    - path: ".planning/phases/01-foundation/DEFERRED-IOS-CI.md"
      provides: "Documento de decisión: iOS CI difiere a Phase 7 (acepta build manual local en Phase 1)"
      contains: "Phase 7"
  key_links:
    - from: ".github/workflows/build-android-debug.yml"
      to: "Godot 4.3 export pipeline"
      via: "barichello/godot-ci:4.3 Docker image + --export-debug command"
      pattern: "godot --headless --export-debug"
    - from: "Railway service (Nakama)"
      to: "Postgres (Railway plugin)"
      via: "DATABASE_URL env var"
      pattern: "postgresql://.*"
    - from: "Nakama instance"
      to: "Resend SMTP API"
      via: "RESEND_API_KEY env var (no usado en este plan, pero seteado para Plan 03)"
      pattern: "RESEND_API_KEY"
---

<objective>
Wave 0 — Establecer la infraestructura base de BarraBrava: repositorio Git con branches main/develop, despliegue de Nakama 3.x + PostgreSQL en Railway (cualquier región disponible — São Paulo no existe en Railway, placeholder hasta migrar a Fly.io), pipeline GitHub Actions para builds debug APK de Android, dominio registrado para Resend SMTP, y registro AAIP iniciado.

**Propósito:** Sin Nakama corriendo no hay servidor para conectar; sin CI no hay validación de que el cliente Godot compila; sin AAIP iniciado no hay compliance legal (Ley 25.326). Esto es la fundación que desbloquea todas las olas siguientes.

**Output:** Nakama health endpoint público respondiendo 200, GitHub Actions verde en push a develop, dominio registrado, AAIP en trámite, bitácora `INFRA-NOTES.md` con todas las URLs y credenciales (referenciadas, no en plano).

**Decisión importante de usuario (revisión D-15):** D-15 originalmente decía "Railway São Paulo desde Phase 1". RESEARCH.md confirmó que **Railway no tiene región São Paulo**. La decisión se revisa a: usar cualquier región Railway disponible como placeholder, manteniendo `fly.toml` con `primary_region = "gru"` preparado para la migración futura a Fly.io (que sí tiene gru = São Paulo). Esta revisión NO altera el intent original de D-15 (latencia baja para usuarios Argentina + transferencia internacional declarable a AAIP), solo el provider técnico. **CONTEXT.md D-15 wording debe actualizarse en este task para reflejar la revisión: "Nakama se despliega en Railway desde Phase 1 (región más cercana a Argentina disponible — gru no existe en Railway, queda como target para migración a Fly.io vía `fly.toml`)".**

**Decisión revisada TEC-08 scope:** TEC-08 originalmente abarcaba "App buildea para iOS y Android desde GitHub Actions". Para Phase 1 se reduce a **solo Android**. iOS CI se difiere formalmente a Phase 7 (Polish + Launch) porque (a) macOS runners en GHA cuestan ~10x más que Linux, (b) Fastlane (que pertenece al setup iOS) ya estaba diferido a Phase 7 en CONTEXT.md Claude's Discretion, (c) builds iOS Phase 1 son manuales en Mac local del dev. Crear `DEFERRED-IOS-CI.md` documenta esta decisión.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Patterns de RESEARCH.md que el executor debe replicar verbatim. -->

### Nakama Docker start command (Railway / Fly.io idéntico)
```sh
/bin/sh -ecx "exec /nakama/nakama \
  --database.address $DATABASE_URL \
  --session.encryption_key $NAKAMA_SESSION_ENCRYPTION_KEY \
  --session.refresh_encryption_key $NAKAMA_SESSION_REFRESH_ENCRYPTION_KEY \
  --console.username $NAKAMA_CONSOLE_USERNAME \
  --console.password $NAKAMA_CONSOLE_PASSWORD"
```

### Nakama pre-deploy migration
```sh
/bin/sh -ecx "/nakama/nakama migrate up --database.address $DATABASE_URL"
```

### GitHub Actions Android workflow (skeleton — adaptar nombre app)
```yaml
name: Android Debug Build
on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [main]

jobs:
  export-android-debug:
    name: Android Debug Export
    runs-on: ubuntu-22.04
    container:
      image: barichello/godot-ci:4.3
    steps:
      - uses: actions/checkout@v4
      - name: Setup export templates
        run: |
          mkdir -p ~/.local/share/godot/export_templates/
          mv /root/.local/share/godot/export_templates/4.3.stable \
             ~/.local/share/godot/export_templates/4.3.stable
      - name: Import project (pre-heat)
        run: godot --headless --editor --quit || true
      - name: Export APK (debug)
        run: |
          mkdir -p build/android
          godot --headless --verbose \
            --export-debug "Android Debug" \
            "build/android/BarraBrava-debug.apk"
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-debug-apk
          path: build/android/BarraBrava-debug.apk
```

### fly.toml plantilla (NO se deploya en este plan, pero queda en repo)
```toml
app = "barrabrava-nakama"
primary_region = "gru"

[build]
  dockerfile = "Dockerfile.nakama"

[http_service]
  internal_port = 7350
  force_https = true

[[services.ports]]
  handlers = ["http"]
  port = 80
[[services.ports]]
  handlers = ["tls", "http"]
  port = 443
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Setup manual de cuentas Railway + Resend + Dominio + AAIP</name>
  <files>N/A — setup externo</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (secciones: Backend Deployment, Auth Implementation, Privacy Policy + AAIP)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-15 actualizado: Railway placeholder)
  </read_first>
  <what-built>
    Aún no se construyó nada — este checkpoint es para que el USUARIO realice los pasos manuales que Claude no puede hacer (crear cuentas externas, comprar dominio, iniciar trámite AAIP).
  </what-built>
  <how-to-verify>
    Usuario debe completar y confirmar:
    1. **Railway account creada** y proyecto vacío "barrabrava-nakama" existe. URL del dashboard.
    2. **Postgres plugin agregado** al proyecto Railway. Variable `DATABASE_URL` aparece en Variables.
    3. **Repositorio GitHub** "barrabrava" creado (puede ser privado). Default branch = `main`. Branch `develop` creada.
    4. **Dominio registrado** (sugerencia: `barrabrava.com.ar`). Anotar dónde fue registrado.
    5. **Cuenta Resend creada** + dominio agregado en Resend Dashboard. DNS records (DKIM/SPF) configurados en el registrar del dominio. Estado: "Verifying" o "Verified".
    6. **AAIP registración iniciada** en argentina.gob.ar/aaip vía Trámites a Distancia. Tener captura de pantalla con número de trámite. **No bloquea desarrollo — async 2-4 semanas.**
    7. **Env vars generadas** (NO compartir con Claude — el usuario las pega en Railway directamente):
       - `NAKAMA_SERVER_KEY` = `openssl rand -hex 16`
       - `NAKAMA_CONSOLE_USERNAME` = `admin`
       - `NAKAMA_CONSOLE_PASSWORD` = `openssl rand -base64 24`
       - `NAKAMA_SESSION_ENCRYPTION_KEY` = `openssl rand -base64 32`
       - `NAKAMA_SESSION_REFRESH_ENCRYPTION_KEY` = `openssl rand -base64 32` (distinto)
       - `RESEND_API_KEY` = del Resend Dashboard
  </how-to-verify>
  <resume-signal>
    Escribir "ready" + pegar:
    - URL pública del proyecto Railway (ej: https://barrabrava-nakama-production.up.railway.app)
    - Dominio registrado (ej: barrabrava.com.ar)
    - Número de trámite AAIP (ej: EX-2026-XXXXXXXX-APN-DNPDP#AAIP)
    - Estado dominio Resend (Verifying / Verified)
  </resume-signal>
  <acceptance_criteria>
    - Usuario confirmó "ready"
    - Usuario proveyó URL Railway pública
    - Usuario proveyó dominio
    - Usuario proveyó número trámite AAIP
  </acceptance_criteria>
  <done>
    Todos los servicios externos están provisionados. Claude tiene la URL pública de Nakama para usar en los siguientes tasks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repositorio Git + .gitignore + README + Dockerfile.nakama + fly.toml + DEFERRED-IOS-CI.md + actualizar CONTEXT.md D-15</name>
  <files>
    .gitignore
    README.md
    Dockerfile.nakama
    fly.toml
    export_presets.cfg
    .planning/phases/01-foundation/INFRA-NOTES.md
    .planning/phases/01-foundation/DEFERRED-IOS-CI.md
    .planning/phases/01-foundation/01-CONTEXT.md
  </files>
  <read_first>
    - CLAUDE.md (constraints generales)
    - .planning/phases/01-foundation/01-RESEARCH.md (secciones: GitHub Actions CI/CD, Backend Deployment)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-14, D-15, D-16)
  </read_first>
  <action>
    Crear los siguientes archivos en la raíz del repositorio:

    **`.gitignore`** (Godot 4 + Node + macOS estándar). Contenido exacto:
    ```
    # Godot 4+ specific
    .godot/
    /android/
    /export/
    .import/
    *.translation

    # OS junk
    .DS_Store
    Thumbs.db
    desktop.ini

    # IDE
    .vscode/
    .idea/
    *.iml

    # Nakama TypeScript runtime (build outputs)
    nakama/build/
    nakama/node_modules/

    # Secrets (never commit)
    .env
    .env.local
    *.pem
    *.keystore

    # Build outputs
    build/
    *.apk
    *.aab
    *.ipa
    *.dSYM
    ```
    NO incluir `export_presets.cfg` en `.gitignore` (Godot CI lo necesita per D-14 y RESEARCH §3).

    **`README.md`** (skeleton mínimo en español, código en inglés). Debe incluir secciones: "## BarraBrava", "## Stack" (Godot 4.3 + Nakama 3.x + Postgres + Railway), "## Setup local" (placeholder), "## Deploy" (placeholder), "## Workflow GSD" (link a .planning/).

    **`Dockerfile.nakama`** — imagen Nakama 3.x pinneada para Railway. Contenido:
    ```dockerfile
    FROM heroiclabs/nakama:3.21.0

    # Copy TypeScript runtime build output (will be built in Plan 03)
    # For Wave 0, no runtime modules — Nakama starts with empty runtime
    # COPY nakama/build/ /nakama/data/modules/

    EXPOSE 7350 7351

    # Use Nakama default entrypoint (Railway sets start command via dashboard)
    ```
    Nota: El COPY de runtime modules está comentado — se descomenta en Plan 03 cuando exista el directorio build/. Esto permite que el deploy Wave 0 funcione SIN runtime custom.

    **`fly.toml`** — preparado para migración futura (usar plantilla `<interfaces>` exacta arriba). Esto NO se deploya en este plan; queda como artefacto para migración futura per RESEARCH §2.

    **`export_presets.cfg`** — preset "Android Debug" pre-configurado para que CI pueda exportar APK sin abrir editor. **Solo Android — iOS deferred a Phase 7 per DEFERRED-IOS-CI.md.** Contenido mínimo:
    ```ini
    [preset.0]

    name="Android Debug"
    platform="Android"
    runnable=true
    advanced_options=false
    dedicated_server=false
    custom_features=""
    export_filter="all_resources"
    include_filter=""
    exclude_filter=""
    export_path="build/android/BarraBrava-debug.apk"
    encryption_include_filters=""
    encryption_exclude_filters=""
    encrypt_pck=false
    encrypt_directory=false
    script_export_mode=2
    script_encryption_key=""

    [preset.0.options]

    custom_template/debug=""
    custom_template/release=""
    gradle_build/use_gradle_build=false
    architectures/armeabi-v7a=false
    architectures/arm64-v8a=true
    architectures/x86_64=false
    package/unique_name="ar.com.barrabrava"
    package/name="BarraBrava"
    package/signed=true
    package/app_category=2
    package/retain_data_on_uninstall=false
    package/exclude_from_recents=false
    launcher_icons/main_192x192=""
    launcher_icons/adaptive_foreground_432x432=""
    launcher_icons/adaptive_background_432x432=""
    graphics/opengl_debug=false
    xr_features/xr_mode=0
    screen/immersive_mode=true
    screen/support_small=false
    screen/support_normal=true
    screen/support_large=true
    screen/support_xlarge=true
    user_data_backup/allow=true
    command_line/extra_args=""
    apk_expansion/enable=false
    apk_expansion/SALT=""
    apk_expansion/public_key=""
    permissions/internet=true
    ```
    (Otras permisos quedan en `false` — solo `internet=true` y `signed=true` para debug keystore automático de Godot CI per RESEARCH §3 keystore note.)

    **`.planning/phases/01-foundation/INFRA-NOTES.md`** — bitácora viva. Estructura:
    ```markdown
    # Phase 1 — Infrastructure Notes

    > Bitácora de URLs, credenciales (REFERENCED, no secretos en plano), y estado de trámites.

    ## Railway
    - **URL pública Nakama:** {RAILWAY_URL del Task 1 resume-signal}
    - **Proyecto:** barrabrava-nakama
    - **Región:** {región elegida por usuario — NOTA: Railway no tiene São Paulo, D-15 revisado en CONTEXT.md}
    - **Server key:** stored en Railway Variables (NAKAMA_SERVER_KEY)
    - **Console URL:** {RAILWAY_URL}:7351

    ## Dominio
    - **Dominio:** {DOMAIN del Task 1}
    - **Registrado en:** {donde lo registró el usuario}
    - **DNS records Resend:** Verifying / Verified (per setup Resend)

    ## Resend
    - **Dominio enviador:** noreply@{DOMAIN}
    - **API key:** stored en Railway Variables (RESEND_API_KEY)
    - **Free tier:** 3000 emails/mes, 100/día

    ## AAIP (Ley 25.326)
    - **Número de trámite:** {AAIP_NUM del Task 1}
    - **Iniciado:** {fecha de hoy}
    - **Estado esperado:** En curso (2-4 semanas)

    ## Branch Strategy
    - `main` — producción (auto-deploy a Railway)
    - `develop` — staging / integración (auto-build CI sin deploy)

    ## TEC-08 scope (Phase 1)
    - **Android APK debug:** GitHub Actions automated (build-android-debug.yml)
    - **iOS IPA:** MANUAL en Mac local del dev hasta Phase 7 (ver DEFERRED-IOS-CI.md)

    ## Próximos pasos
    - Plan 02: Setup proyecto Godot
    - Plan 03: Build TypeScript runtime + redeploy Nakama con módulos
    ```

    Sustituir `{...}` con los valores reales del Task 1 resume-signal.

    **`.planning/phases/01-foundation/DEFERRED-IOS-CI.md`** — verificar que existe (creado durante revisión CHK-01). Si no existe, crearlo con contenido que documente:
    - Decisión: iOS CI workflow difiere a Phase 7
    - Razones (3): macOS runner cost, Fastlane ya diferido, Phase 1 iOS builds son manuales
    - Phase 1 manual iOS build instructions
    - Phase 7 deliverables (build-ios-release.yml, Fastlane match, App Store Connect API)
    - Reversal trigger (>5 manual iOS builds/week → revisitar)

    **Actualizar `.planning/phases/01-foundation/01-CONTEXT.md` D-15** — revisar el wording. Localizar la línea actual:
    > "**D-15:** Nakama se despliega en **Railway São Paulo desde Phase 1** (no Docker local). El entorno de desarrollo apunta a Railway desde el día 1. Evita divergencia dev/prod."

    Reemplazar por:
    > "**D-15 (revisado 2026-05-15):** Nakama se despliega en **Railway desde Phase 1** (no Docker local). RESEARCH.md confirmó que Railway NO tiene región São Paulo — se usa la región disponible más cercana a Argentina como placeholder. `fly.toml` se prepara con `primary_region = \"gru\"` para migración futura a Fly.io (que sí tiene São Paulo). El entorno de desarrollo apunta a Railway desde el día 1. Evita divergencia dev/prod. **Intent original preservado** (latencia baja LATAM + transferencia internacional declarable AAIP)."

    Después de crear los archivos, hacer commit local (NO push aún) con mensaje: `chore(01): scaffold infra config + .gitignore + Docker + fly.toml + export_presets + defer iOS CI (D-15 revised)`. Confirmar branches: `main` ya existe, crear `develop` con `git branch develop` y push ambas: `git push -u origin main && git push -u origin develop`.
  </action>
  <verify>
    <automated>test -f .gitignore && test -f Dockerfile.nakama && test -f fly.toml && test -f export_presets.cfg && test -f .planning/phases/01-foundation/INFRA-NOTES.md && test -f .planning/phases/01-foundation/DEFERRED-IOS-CI.md && grep -q "heroiclabs/nakama" Dockerfile.nakama && grep -q "primary_region = \"gru\"" fly.toml && grep -q "Android Debug" export_presets.cfg && ! grep -q "platform=\"iOS\"" export_presets.cfg && grep -q "Railway" .planning/phases/01-foundation/INFRA-NOTES.md && grep -q "Phase 7" .planning/phases/01-foundation/DEFERRED-IOS-CI.md && grep -q "D-15 (revisado" .planning/phases/01-foundation/01-CONTEXT.md && git branch | grep -E "develop|main" | wc -l | grep -q "2"</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `.gitignore` existe y contiene `.godot/`, `*.apk`, `nakama/node_modules/`
    - Archivo `Dockerfile.nakama` existe, tiene `FROM heroiclabs/nakama:3.21.0` (versión pinneada, NO :latest)
    - Archivo `fly.toml` existe con `primary_region = "gru"` exacto
    - Archivo `export_presets.cfg` existe con preset "Android Debug" y `architectures/arm64-v8a=true`
    - `export_presets.cfg` NO contiene preset iOS (deferred a Phase 7)
    - `INFRA-NOTES.md` existe con valores reales del Task 1 (no placeholders `{...}`)
    - `INFRA-NOTES.md` declara explícitamente que iOS builds son manuales en Phase 1
    - `DEFERRED-IOS-CI.md` existe con razones, instrucciones manuales, y plan Phase 7
    - `01-CONTEXT.md` D-15 reemplazado con wording "(revisado 2026-05-15)" + nota Railway no tiene São Paulo
    - Branch `develop` existe localmente Y en remoto (verify: `git branch -r | grep develop`)
    - Commit local realizado con mensaje exacto especificado en action
  </acceptance_criteria>
  <done>
    Repositorio tiene scaffold de infra config. Branches main/develop ambas pushed. INFRA-NOTES.md tiene URLs reales del Task 1. CONTEXT.md D-15 revisado. DEFERRED-IOS-CI.md documenta la reducción de scope TEC-08.
  </done>
</task>

<task type="auto">
  <name>Task 3: GitHub Actions workflow para Android debug APK</name>
  <files>
    .github/workflows/build-android-debug.yml
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (sección 3: GitHub Actions CI/CD for Godot 4.3 — copiar YAML exacto)
    - export_presets.cfg (verificar nombre exacto del preset "Android Debug")
    - .planning/phases/01-foundation/DEFERRED-IOS-CI.md (confirmar que iOS NO se incluye)
  </read_first>
  <action>
    Crear `.github/workflows/build-android-debug.yml` usando el bloque `<interfaces>` arriba como base. Adaptaciones:

    1. Reemplazar `BarraBrava-debug.apk` por el path exacto especificado en `export_presets.cfg` → `export_path="build/android/BarraBrava-debug.apk"`.
    2. El workflow debe disparar en push a `develop` y `main`, y en PR a `main` (per D-16).
    3. Usar `actions/checkout@v4` (no v3, no v5).
    4. Usar `actions/upload-artifact@v4`.
    5. Imagen Docker pinneada exacta: `barichello/godot-ci:4.3` (NO :latest — TEC-08 + RESEARCH §3 anti-handroll).
    6. Pre-heat step REQUERIDO antes de export (RESEARCH §3 gotcha: sin pre-heat, headless export crashea en imports faltantes). Comando exacto: `godot --headless --editor --quit || true` (el `|| true` porque a veces el comando retorna 1 incluso si succeeded — verificado por la comunidad Godot).
    7. Subir artefacto APK con nombre `android-debug-apk` y retention 7 días.
    8. **No crear build-ios-debug.yml ni equivalente.** iOS se difiere a Phase 7 per DEFERRED-IOS-CI.md.

    Workflow completo:
    ```yaml
    name: Android Debug Build

    on:
      push:
        branches: [develop, main]
      pull_request:
        branches: [main]

    jobs:
      export-android-debug:
        name: Android Debug Export
        runs-on: ubuntu-22.04
        container:
          image: barichello/godot-ci:4.3
        steps:
          - name: Checkout code
            uses: actions/checkout@v4
            with:
              lfs: true

          - name: Setup export templates
            run: |
              mkdir -p ~/.local/share/godot/export_templates/
              mv /root/.local/share/godot/export_templates/4.3.stable \
                 ~/.local/share/godot/export_templates/4.3.stable

          - name: Import project (pre-heat)
            run: godot --headless --editor --quit || true

          - name: Export APK (debug)
            run: |
              mkdir -p build/android
              godot --headless --verbose \
                --export-debug "Android Debug" \
                "build/android/BarraBrava-debug.apk"

          - name: Verify APK exists
            run: |
              test -f build/android/BarraBrava-debug.apk
              ls -lh build/android/

          - name: Upload APK artifact
            uses: actions/upload-artifact@v4
            with:
              name: android-debug-apk
              path: build/android/BarraBrava-debug.apk
              retention-days: 7
    ```

    **Nota Wave 0:** En este punto NO existe `project.godot` aún (eso es Plan 02). El workflow va a fallar en el step "Export APK" hasta que Plan 02 cree el proyecto Godot. Esto es ACEPTABLE y esperado — el workflow queda commiteado, y la primera corrida verde será al final de Plan 02.

    Commit con mensaje: `ci(01): add Android debug APK build workflow (GHA + barichello/godot-ci:4.3)`.

    Push a `develop` para validar que el workflow se parsea correctamente (aunque falle en export por falta de project.godot — eso es esperado).
  </action>
  <verify>
    <automated>test -f .github/workflows/build-android-debug.yml && grep -q "barichello/godot-ci:4.3" .github/workflows/build-android-debug.yml && grep -q "Android Debug" .github/workflows/build-android-debug.yml && grep -q "actions/checkout@v4" .github/workflows/build-android-debug.yml && grep -q "actions/upload-artifact@v4" .github/workflows/build-android-debug.yml && grep -q "godot --headless --editor --quit" .github/workflows/build-android-debug.yml && ! test -f .github/workflows/build-ios-debug.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build-android-debug.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `.github/workflows/build-android-debug.yml` existe
    - YAML válido (parsea sin error con Python yaml.safe_load)
    - Contiene `barichello/godot-ci:4.3` (versión pinneada)
    - Contiene preset name exacto "Android Debug" (match con export_presets.cfg)
    - Triggers `develop` y `main` configurados
    - Steps incluyen: checkout v4, pre-heat con `godot --headless --editor --quit`, export, verify APK existe, upload artifact v4
    - **NO existe `build-ios-debug.yml`** (deferred a Phase 7 per DEFERRED-IOS-CI.md)
    - Workflow pusheado a `develop`
    - GitHub Actions registró el workflow (visible en repo Actions tab — usuario verifica visualmente en próximo checkpoint)
  </acceptance_criteria>
  <done>
    Workflow CI Android existe, parsea correctamente, está pusheado. Failing-as-expected hasta Plan 02 cree project.godot. Workflow iOS confirmado ausente (Phase 7 scope).
  </done>
</task>

<task type="auto">
  <name>Task 4: Configurar Railway service + deploy Nakama vacío + healthcheck verde</name>
  <files>
    .planning/phases/01-foundation/INFRA-NOTES.md
  </files>
  <read_first>
    - Dockerfile.nakama (verificar versión pinneada)
    - .planning/phases/01-foundation/INFRA-NOTES.md (URL Railway del Task 1)
    - .planning/phases/01-foundation/01-RESEARCH.md (sección 2: start command + migrate up)
  </read_first>
  <action>
    Configurar el servicio Nakama en Railway usando el `Dockerfile.nakama` ya commiteado, y disparar el primer deploy.

    **Pasos (vía Railway dashboard del usuario o CLI `railway` si está instalado):**

    1. En el proyecto Railway, conectar el servicio Nakama al repositorio GitHub (branch `main`).
    2. **Build settings**:
       - Builder: `Dockerfile`
       - Dockerfile path: `Dockerfile.nakama`
    3. **Pre-deploy command** (Railway service settings):
       ```
       /bin/sh -ecx "/nakama/nakama migrate up --database.address $DATABASE_URL"
       ```
    4. **Start command** (Railway service settings):
       ```
       /bin/sh -ecx "exec /nakama/nakama --database.address $DATABASE_URL --session.encryption_key $NAKAMA_SESSION_ENCRYPTION_KEY --session.refresh_encryption_key $NAKAMA_SESSION_REFRESH_ENCRYPTION_KEY --console.username $NAKAMA_CONSOLE_USERNAME --console.password $NAKAMA_CONSOLE_PASSWORD"
       ```
    5. **Networking**: Generar dominio público en Railway (genera xxx.up.railway.app). Configurar port 7350 como public.
    6. Si Claude tiene acceso a Railway CLI (`railway` instalado y `railway login` ya hecho por el usuario en checkpoint anterior), ejecutar:
       ```
       railway up --service nakama
       railway logs --service nakama
       ```
       Si NO tiene acceso CLI, dejar instrucciones en INFRA-NOTES.md "Próximos pasos manuales" y avanzar — el deploy se valida en el próximo checkpoint.

    7. Esperar a que el deploy termine. Verificar healthcheck:
       ```
       curl -f https://{RAILWAY_URL}/healthcheck
       ```
       Debe retornar `200 OK` con body `{}` o similar (Nakama healthcheck format).

    8. Actualizar `INFRA-NOTES.md` con:
       - Fecha del primer deploy exitoso
       - Output del `curl /healthcheck` (debe ser 200)
       - URL de Nakama Console: `https://{RAILWAY_URL}:7351` (puerto 7351 — admin)
       - Status: "Nakama vacío deployado, sin runtime custom (TypeScript modules vienen en Plan 03)"

    **Importante:** En esta etapa Nakama corre sin TypeScript runtime modules — solo el binario base. Eso está bien para Wave 0; los módulos se compilan y deployan en Plan 03 (Wave 2).
  </action>
  <verify>
    <automated>RAILWAY_URL=$(grep "URL pública Nakama" .planning/phases/01-foundation/INFRA-NOTES.md | head -1 | sed 's/.*: //' | tr -d ' '); test -n "$RAILWAY_URL" && curl -fsS --max-time 10 "$RAILWAY_URL/healthcheck" -o /tmp/nakama-health.json && test $? -eq 0 && echo "Healthcheck OK"</automated>
  </verify>
  <acceptance_criteria>
    - `curl -f https://{RAILWAY_URL}/healthcheck` retorna HTTP 200
    - `INFRA-NOTES.md` tiene línea "Status: Nakama vacío deployado"
    - `INFRA-NOTES.md` tiene fecha del primer deploy
    - `INFRA-NOTES.md` tiene URL exacta de Nakama Console (puerto 7351)
    - Logs Railway no muestran errores fatales (verificar manualmente con usuario en próximo checkpoint)
  </acceptance_criteria>
  <done>
    Nakama responde en su URL pública. Postgres conectado (Nakama no arrancaría sin DB). Plan 03 puede empezar a deployar runtime modules.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verificación humana — Infra Wave 0 lista</name>
  <what-built>
    - Repositorio GitHub con `main` + `develop`
    - `.gitignore`, `Dockerfile.nakama`, `fly.toml`, `export_presets.cfg`, `README.md`, `INFRA-NOTES.md`, `DEFERRED-IOS-CI.md` committed
    - GitHub Actions workflow Android debug en `.github/workflows/build-android-debug.yml` (failing-as-expected hasta Plan 02)
    - CONTEXT.md D-15 revisado para reflejar Railway región no-São-Paulo
    - Railway: Nakama + Postgres corriendo. Healthcheck verde.
    - Resend: dominio verificado.
    - AAIP: trámite iniciado (async).
  </what-built>
  <how-to-verify>
    1. **GitHub repo**: visitá tu repo en github.com/{user}/barrabrava. Verificá que existen branches `main` y `develop`.
    2. **GitHub Actions tab**: visitá Actions → debería verse "Android Debug Build" workflow registrado. La última corrida probablemente está roja (no existe `project.godot` aún) — eso es ESPERADO. Confirmá que el workflow se reconoce (no hay errores de sintaxis YAML). **Confirmá también que NO existe ningún workflow iOS** — eso es Phase 7.
    3. **Railway dashboard**: visitá tu proyecto. Servicio Nakama estado = "Active". Postgres = "Active". Logs de Nakama no muestran errors fatales.
    4. **Healthcheck Nakama**: en terminal local, correr:
       ```
       curl -i https://{tu-railway-url}/healthcheck
       ```
       Esperás: `HTTP/2 200`.
    5. **Nakama Console**: visitá `https://{tu-railway-url}:7351` en browser. Login con `NAKAMA_CONSOLE_USERNAME` + `NAKAMA_CONSOLE_PASSWORD`. Confirmá que entra al dashboard.
    6. **Resend**: visitá resend.com/domains. Tu dominio debería estar en "Verified" (o aún "Verifying" si DNS lento — esperar 24h max).
    7. **AAIP**: confirmá que tenés captura del número de trámite. No bloquea avance.
    8. **INFRA-NOTES.md**: abrí `.planning/phases/01-foundation/INFRA-NOTES.md` y revisá que tiene tus URLs reales (no placeholders). Confirmá que tiene la sección "TEC-08 scope (Phase 1)" declarando Android-only.
    9. **DEFERRED-IOS-CI.md**: revisá que existe en el phase folder y entendés la decisión.
    10. **CONTEXT.md D-15**: revisá que la decisión está marcada como "(revisado 2026-05-15)" reflejando Railway no-São-Paulo.

    Si algo falla, describí qué falló. Si todo OK, escribí "approved".
  </how-to-verify>
  <resume-signal>
    Escribí "approved" si todo verde, o describí qué falló y Claude itera.
  </resume-signal>
  <acceptance_criteria>
    - Usuario confirmó "approved"
    - Si reportó issues, fueron resueltos antes del approved
  </acceptance_criteria>
  <done>
    Wave 0 verificada por humano. Plan 02 puede comenzar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Railway/Nakama | Tráfico no autenticado entra al endpoint público; Nakama valida server key + auth tokens |
| GitHub Actions → repo secrets | CI tiene acceso a secrets configurados (none en Wave 0 — debug builds, no signing) |
| Developer machine → Railway env vars | Credenciales sensibles se setean vía Railway dashboard, NUNCA en código |
| Resend API → email recipients | Emails salen autenticados via DKIM/SPF del dominio verificado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-INF-01 | Information Disclosure | Railway env vars en logs | mitigate | Verificar que start command usa `-ecx` (echo SIN expand) — Railway no loguea valores de env vars; revisar logs post-deploy buscando keys/passwords leaked |
| T-1-INF-02 | Spoofing | Nakama server key default | mitigate | `NAKAMA_SERVER_KEY` generado con `openssl rand -hex 16` (mín 16 chars random per RESEARCH §2); NUNCA usar "defaultkey" en producción Railway |
| T-1-INF-03 | Elevation of Privilege | Nakama Console exposed | mitigate | Console en puerto 7351, password fuerte (`openssl rand -base64 24`); evaluar en Phase 2 si limitar por IP allowlist Railway (out of scope Wave 0) |
| T-1-INF-04 | Tampering | Dockerfile FROM `:latest` | mitigate | Imagen pinneada: `heroiclabs/nakama:3.21.0` (NO `:latest`) — reproducibilidad + anti-supply-chain |
| T-1-INF-05 | Denial of Service | Bot scanning health endpoint | accept | `/healthcheck` es público por diseño Nakama; rate limiting nivel app viene en Plan 03 (TEC-10 baseline) |
| T-1-INF-06 | Information Disclosure | Resend API key leaked en repo | mitigate | API key SOLO en Railway env var (`RESEND_API_KEY`); `.env` y `.env.local` en `.gitignore`; revisar git log no contiene secrets antes de push |
| T-1-INF-07 | Spoofing | Email phishing usando dominio sin SPF | mitigate | Resend requiere DKIM + SPF records verificados antes de permitir envío; dominio NO se considera verificado hasta DNS propagation completo |
</threat_model>

<verification>
1. `curl -f https://{RAILWAY_URL}/healthcheck` retorna 200
2. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build-android-debug.yml'))"` parsea OK
3. `git branch -r | grep -E "origin/(main|develop)"` muestra ambas branches
4. `grep -E "(heroiclabs/nakama:3\.[0-9]+\.[0-9]+|barichello/godot-ci:4\.3)" Dockerfile.nakama .github/workflows/build-android-debug.yml` muestra versiones pinneadas (NO :latest)
5. `grep -L "RESEND_API_KEY\|NAKAMA_SERVER_KEY\|DATABASE_URL" *.{toml,yml,md,cfg} 2>/dev/null` — ningún archivo en repo contiene valores en plano (solo referencias por nombre)
6. INFRA-NOTES.md contiene URL Railway real (no placeholder `{...}`)
7. `! test -f .github/workflows/build-ios-debug.yml && ! test -f .github/workflows/build-ios-release.yml` (TEC-08 Phase 1 scope = Android-only)
8. `test -f .planning/phases/01-foundation/DEFERRED-IOS-CI.md` (decisión documentada)
9. `grep -q "D-15 (revisado" .planning/phases/01-foundation/01-CONTEXT.md` (CONTEXT.md actualizado)
</verification>

<success_criteria>
- Push a develop registra workflow Android Debug Build en GitHub Actions (puede estar rojo si project.godot no existe — Plan 02 lo arregla)
- Nakama responde 200 en /healthcheck público
- Postgres conectado y migrate up exitoso (logs Railway lo muestran)
- AAIP trámite iniciado (número registrado en INFRA-NOTES.md)
- Resend domain en estado Verifying o Verified
- Branches main + develop existen local y remoto
- Cero secrets commiteados al repo (auditado con grep)
- TEC-08 Phase 1 scope = Android-only; iOS CI difiere a Phase 7 con documento DEFERRED-IOS-CI.md
- CONTEXT.md D-15 actualizado: Railway región no es São Paulo (no existe ahí)
</success_criteria>

<output>
Después de completar, crear `.planning/phases/01-foundation/01-01-SUMMARY.md` con:
- URLs públicas (Railway, dominio, AAIP)
- Estado de cada servicio externo
- Workflow CI status (Android only, iOS deferred)
- Próximos pasos para Plan 02
</output>
