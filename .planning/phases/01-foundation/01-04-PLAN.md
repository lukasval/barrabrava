---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 02
  - 03
files_modified:
  - scenes/SplashScreen.tscn
  - scripts/screens/SplashScreen.gd
  - scenes/AuthScreen.tscn
  - scripts/screens/AuthScreen.gd
  - scenes/ForgotPasswordScreen.tscn
  - scripts/screens/ForgotPasswordScreen.gd
  - scenes/ClubPickerScreen.tscn
  - scripts/screens/ClubPickerScreen.gd
  - scenes/components/ClubCard.tscn
  - scripts/components/ClubCard.gd
  - scenes/components/ChipButton.tscn
  - scripts/components/ChipButton.gd
  - scenes/PibeCreatorScreen.tscn
  - scripts/screens/PibeCreatorScreen.gd
  - scenes/TutorialScreen.tscn
  - scripts/screens/TutorialScreen.gd
  - scenes/HomeScreen.tscn
  - scripts/screens/HomeScreen.gd
  - scenes/components/NavButton.tscn
  - scripts/components/NavButton.gd
  - scripts/autoloads/FlowRouter.gd
  - scripts/autoloads/PlayerStore.gd
  - scripts/utils/Tween.gd
  - project.godot
  - assets/theme/Theme.tres
autonomous: false
requirements:
  - ONB-01
  - ONB-02
  - ONB-04
  - CLB-01
  - CLB-02
  - PRV-03
  - TEC-09
tags:
  - godot
  - ui
  - screens
  - onboarding
  - club-picker
  - pibe-creator
  - home
  - forgot-password

must_haves:
  truths:
    - "SplashScreen aparece al inicio, muestra disclaimer de parodia (CLB-02) en first-launch, y persiste el flag `disclaimer_seen=true` en user://app.cfg para no mostrarlo en relanzamientos"
    - "AuthScreen permite registro y login con email/password contra Nakama (ONB-01); errores se muestran con copy lunfardo exacto del UI-SPEC §AuthScreen"
    - "AuthScreen tab Entrar incluye link 'Olvidaste tu contraseña?' que navega a ForgotPasswordScreen (D-03 entry point)"
    - "ForgotPasswordScreen acepta email y llama RPC request_password_reset; muestra confirmación uniforme aunque el email no exista (anti-enumeration)"
    - "Auto-login: si AuthManager restaura sesión válida y el jugador ya tiene pibe (player_profile en Storage), FlowRouter salta directo a HomeScreen"
    - "ClubPickerScreen lista los ~133 clubs desde RPC get_clubs, filtra por división con ChipButton scroll horizontal y busca por texto (CLB-01, ONB-02)"
    - "PibeCreatorScreen acepta nombre, muestra los 4 stats fijos 50/50/50/50, llama RPC create_pibe, y maneja error 'Ese nombre no va' del server (ONB-04)"
    - "TutorialScreen muestra copy de bienvenida y CTA único 'Dale, empezamos' → HomeScreen"
    - "HomeScreen muestra pibe name + club name del save, empty-state copy, y bottom nav con 4 NavButtons (Inicio activo)"
    - "HomeScreen incluye botón 'Borrar mi cuenta' que llama RPC delete_account, limpia user://session.cfg y user://app.cfg, vuelve a SplashScreen (PRV-03)"
    - "Todas las transiciones de pantalla usan fade 150ms in / 150ms out vía Tween util"
    - "Theme global aplica colores y tipografía Nunito sin overrides locales en ningún Control de las pantallas"
  artifacts:
    - path: "scenes/SplashScreen.tscn"
      provides: "First-launch + disclaimer screen"
      contains: "SplashScreen"
    - path: "scripts/screens/SplashScreen.gd"
      provides: "Logic: lee app.cfg, decide AuthScreen vs HomeScreen, espera AuthManager"
      contains: "disclaimer_seen"
    - path: "scenes/AuthScreen.tscn"
      provides: "TabContainer Entrar/Registrarse + forgot-password link"
      contains: "TabContainer"
    - path: "scripts/screens/AuthScreen.gd"
      provides: "Llama AuthManager.login/register, maneja errores, ruta a ForgotPasswordScreen"
      contains: "AuthManager.login"
    - path: "scenes/ForgotPasswordScreen.tscn"
      provides: "Email input + 'Enviar reseteo' button — D-03 UI entry point"
      contains: "ForgotPasswordScreen"
    - path: "scripts/screens/ForgotPasswordScreen.gd"
      provides: "Llama AuthManager.request_password_reset(email)"
      contains: "request_password_reset"
    - path: "scenes/ClubPickerScreen.tscn"
      provides: "Search + filter chips + scrollable club list"
      contains: "ScrollContainer"
    - path: "scripts/screens/ClubPickerScreen.gd"
      provides: "Llama get_clubs RPC, instancia ClubCards, maneja selección"
      contains: "get_clubs"
    - path: "scenes/components/ClubCard.tscn"
      provides: "Reusable ClubCard (crest placeholder + name + division)"
      contains: "ClubCard"
    - path: "scenes/PibeCreatorScreen.tscn"
      provides: "Name input + stats display + CTA"
      contains: "PibeCreatorScreen"
    - path: "scripts/screens/PibeCreatorScreen.gd"
      provides: "Llama create_pibe RPC, maneja validación server-side"
      contains: "create_pibe"
    - path: "scenes/TutorialScreen.tscn"
      provides: "Welcome screen single CTA"
      contains: "TutorialScreen"
    - path: "scenes/HomeScreen.tscn"
      provides: "Empty-state hub + bottom nav + delete account"
      contains: "HomeScreen"
    - path: "scripts/screens/HomeScreen.gd"
      provides: "Carga player profile + club name, gatilla delete_account"
      contains: "delete_account"
    - path: "scripts/autoloads/FlowRouter.gd"
      provides: "Centralized scene transitions (fade) + onboarding flow"
      contains: "go_to"
    - path: "scripts/autoloads/PlayerStore.gd"
      provides: "Cache de player profile (pibe + club) en memoria post-login"
      contains: "pibe_name"
  key_links:
    - from: "scripts/screens/SplashScreen.gd"
      to: "scripts/autoloads/AuthManager.gd"
      via: "AuthManager.session_ready signal + is_authenticated()"
      pattern: "AuthManager\\."
    - from: "scripts/screens/AuthScreen.gd"
      to: "scripts/autoloads/AuthManager.gd"
      via: "await AuthManager.login(email, password)"
      pattern: "AuthManager\\.(login|register)"
    - from: "scripts/screens/AuthScreen.gd"
      to: "scripts/screens/ForgotPasswordScreen.gd"
      via: "FlowRouter.go_forgot_password() on link tap"
      pattern: "go_forgot_password"
    - from: "scripts/screens/ForgotPasswordScreen.gd"
      to: "RPC request_password_reset"
      via: "AuthManager.request_password_reset(email)"
      pattern: "request_password_reset"
    - from: "scripts/screens/ClubPickerScreen.gd"
      to: "RPC get_clubs"
      via: "NakamaClient.client.rpc_async(session, 'get_clubs', payload)"
      pattern: "rpc_async.*get_clubs"
    - from: "scripts/screens/PibeCreatorScreen.gd"
      to: "RPC create_pibe"
      via: "NakamaClient.client.rpc_async(session, 'create_pibe', payload)"
      pattern: "rpc_async.*create_pibe"
    - from: "scripts/screens/HomeScreen.gd"
      to: "RPC delete_account"
      via: "NakamaClient.client.rpc_async(session, 'delete_account', '')"
      pattern: "rpc_async.*delete_account"
    - from: "scripts/autoloads/FlowRouter.gd"
      to: "All Screen.tscn files"
      via: "get_tree().change_scene_to_file(path) + Tween fade"
      pattern: "change_scene_to_file"
---

<objective>
Wave 3 — Construir las 7 pantallas Godot del onboarding completo per UI-SPEC §Component Inventory (Splash, Auth, ForgotPassword, ClubPicker, PibeCreator, Tutorial, Home), conectándolas a los 5 RPCs server-side del Plan 03 y al AuthManager autoload del Plan 02. Incluye 2 nuevos autoloads (FlowRouter para transiciones + PlayerStore para cache de pibe/club), 3 componentes reutilizables (ClubCard, ChipButton, NavButton), y wiring completo del flow Splash → Auth → ClubPicker → PibeCreator → Tutorial → Home con auto-login determinístico. **ForgotPasswordScreen es el UI entry point de D-03** (password recovery) — sin él, el servidor RPC + email HTML del Plan 05 son inalcanzables desde la app.

**Propósito:** Cerrar el goal del Phase 1 (per ROADMAP §Phase 1 Success criteria 2-4): "Player puede registrarse, elegir club entre ~130, crear su pibe, ver pantalla de inicio funcional, y recuperar su contraseña si la olvida." Wave 0-2 prepararon infra + runtime; Wave 3 hace que la app sea jugable end-to-end en device real. Plan 05 cierra el resto (privacy policy hosting + AAIP + reset password HTML).

**Output:** APK debug instalado en device físico permite: registrarse con email nuevo → ver lista filtrable de clubs → elegir club → ponerle nombre al pibe → ver tutorial bienvenida → llegar a HomeScreen con pibe name visible. Relanzar app después → auto-login → directo a HomeScreen. Borrar cuenta desde HomeScreen → vuelve a SplashScreen con disclaimer NO mostrado (porque disclaimer_seen flag persiste). Re-registro con mismo email rechazado con copy lunfardo correcto. **Tap "Olvidaste tu contraseña?" en AuthScreen → ForgotPasswordScreen → email enviado → recibo email Resend con link al HTML Plan 05.**

**Decisiones críticas locked-in respetadas:**
- D-03: password recovery vía email reset, UI entry point en AuthScreen → ForgotPasswordScreen
- D-04: Splash → Auth, sin landing page elaborada
- D-09: orden de flujo Registro → Club picker → Nombre pibe → Tutorial → Home
- D-10: NO faction selection (skipped en flow)
- D-11: stats fijos 50/50/50/50 mostrados read-only en PibeCreatorScreen
- D-12: avatar placeholder (ColorRect silueta) en PibeCreatorScreen + HomeScreen
- D-13: TutorialScreen = single welcome screen, NO tutorial completo "primera salida"
- T-1-04 mitigation: pibe stats vienen del response del RPC, NUNCA se envían desde el cliente

**No incluye:** Privacy policy HTML hosting (Plan 05), reset password HTML page (Plan 05), AAIP docs (Plan 05), analytics, FCM, GPS, art real (todo placeholder).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@project.godot
@scripts/autoloads/NakamaClient.gd
@scripts/autoloads/AuthManager.gd
@scripts/autoloads/AppTheme.gd
@assets/theme/Theme.tres

<interfaces>
<!-- Patterns from UI-SPEC §Component Inventory + RESEARCH §1 §6 — copy literal. -->

### scripts/autoloads/FlowRouter.gd
```gdscript
extends Node

# Centralized scene navigation with 150ms fade in/out (UI-SPEC §Screen Flow).
# Auto-load order: NakamaClient, AuthManager, AppTheme, PlayerStore, FlowRouter (last).

const TRANSITION_MS := 150

signal transition_started(target_path)
signal transition_finished(target_path)

var _fade_layer: CanvasLayer
var _fade_rect: ColorRect
var _transitioning := false

func _ready() -> void:
    _setup_fade_layer()

func _setup_fade_layer() -> void:
    _fade_layer = CanvasLayer.new()
    _fade_layer.layer = 100
    _fade_rect = ColorRect.new()
    _fade_rect.color = Color(0, 0, 0, 0)
    _fade_rect.mouse_filter = Control.MOUSE_FILTER_IGNORE
    _fade_rect.anchor_right = 1.0
    _fade_rect.anchor_bottom = 1.0
    _fade_layer.add_child(_fade_rect)
    get_tree().root.call_deferred("add_child", _fade_layer)

func go_to(scene_path: String) -> void:
    if _transitioning:
        return
    _transitioning = true
    transition_started.emit(scene_path)
    _fade_rect.mouse_filter = Control.MOUSE_FILTER_STOP
    var t = create_tween()
    t.tween_property(_fade_rect, "color:a", 1.0, TRANSITION_MS / 1000.0)
    await t.finished
    get_tree().change_scene_to_file(scene_path)
    await get_tree().process_frame
    var t2 = create_tween()
    t2.tween_property(_fade_rect, "color:a", 0.0, TRANSITION_MS / 1000.0)
    await t2.finished
    _fade_rect.mouse_filter = Control.MOUSE_FILTER_IGNORE
    _transitioning = false
    transition_finished.emit(scene_path)

# Convenience helpers — keep all scene paths in one place.
func go_splash() -> void: go_to("res://scenes/SplashScreen.tscn")
func go_auth() -> void: go_to("res://scenes/AuthScreen.tscn")
func go_forgot_password() -> void: go_to("res://scenes/ForgotPasswordScreen.tscn")
func go_club_picker() -> void: go_to("res://scenes/ClubPickerScreen.tscn")
func go_pibe_creator() -> void: go_to("res://scenes/PibeCreatorScreen.tscn")
func go_tutorial() -> void: go_to("res://scenes/TutorialScreen.tscn")
func go_home() -> void: go_to("res://scenes/HomeScreen.tscn")
```

### scripts/autoloads/AuthManager.gd — request_password_reset method (extend Plan 02 AuthManager)
```gdscript
# Added in Plan 04 — request_password_reset is the bridge AuthScreen/ForgotPasswordScreen → RPC.
func request_password_reset(email: String) -> Dictionary:
    var payload = JSON.stringify({"email": email.strip_edges()})
    # Use an unauthenticated server-key call (Nakama allows it for public RPCs).
    # We don't have a session here, so use rpc_async with null session.
    var resp = await NakamaClient.client.rpc_async(null, "request_password_reset", payload)
    if resp.is_exception():
        return {"ok": false, "error": str(resp.get_exception().message)}
    return {"ok": true}
```
Note: This method is added by Plan 04 Task 2 (forgot password task) to the existing AuthManager.gd. The server RPC (Plan 03) returns `ok: true` uniformly for anti-enumeration — client must respect this and show a uniform confirmation regardless.

### scripts/screens/AuthScreen.gd
```gdscript
extends Control

@onready var tabs: TabContainer = $VBox/TabContainer
@onready var login_email: LineEdit = $VBox/TabContainer/Entrar/Email
@onready var login_password: LineEdit = $VBox/TabContainer/Entrar/Password
@onready var login_button: Button = $VBox/TabContainer/Entrar/Submit
@onready var login_error: Label = $VBox/TabContainer/Entrar/ErrorLabel
@onready var forgot_link: RichTextLabel = $VBox/TabContainer/Entrar/ForgotLink
@onready var reg_email: LineEdit = $VBox/TabContainer/Registrarse/Email
@onready var reg_password: LineEdit = $VBox/TabContainer/Registrarse/Password
@onready var reg_confirm: LineEdit = $VBox/TabContainer/Registrarse/Confirm
@onready var reg_button: Button = $VBox/TabContainer/Registrarse/Submit
@onready var reg_error: Label = $VBox/TabContainer/Registrarse/ErrorLabel
@onready var privacy_link: RichTextLabel = $VBox/TabContainer/Registrarse/PrivacyLink

func _ready() -> void:
    login_button.pressed.connect(_on_login)
    reg_button.pressed.connect(_on_register)
    privacy_link.meta_clicked.connect(_on_privacy_clicked)
    forgot_link.meta_clicked.connect(_on_forgot_clicked)
    forgot_link.text = "[url=forgot]¿Olvidaste tu contraseña?[/url]"
    privacy_link.text = "[url=%s]Antes de arrancar, leé las reglas del juego[/url]" % AppConfig.PRIVACY_URL
    login_error.visible = false
    reg_error.visible = false

func _on_login() -> void:
    login_error.visible = false
    login_button.disabled = true
    var res = await AuthManager.login(login_email.text.strip_edges(), login_password.text)
    login_button.disabled = false
    if not res.ok:
        login_error.text = _humanize_error(res.error)
        login_error.visible = true
        return
    var profile = await PlayerStore.load_from_server()
    if profile.ok and PlayerStore.has_profile():
        FlowRouter.go_home()
    else:
        FlowRouter.go_club_picker()

func _on_register() -> void:
    reg_error.visible = false
    if reg_password.text != reg_confirm.text:
        reg_error.text = "Las contraseñas no coinciden, chabón."
        reg_error.visible = true
        return
    if reg_password.text.length() < 8:
        reg_error.text = "Mínimo 8 caracteres, chabón."
        reg_error.visible = true
        return
    reg_button.disabled = true
    var res = await AuthManager.register(reg_email.text.strip_edges(), reg_password.text)
    reg_button.disabled = false
    if not res.ok:
        reg_error.text = _humanize_error(res.error)
        reg_error.visible = true
        return
    FlowRouter.go_club_picker()

func _humanize_error(err: String) -> String:
    var lower = err.to_lower()
    if "exist" in lower or "already" in lower:
        return "Ese mail ya está en la vuelta. Entrá con tu cuenta."
    if "invalid" in lower or "password" in lower or "credentials" in lower:
        return "Código equivocado, chabón. Probá de vuelta."
    if "network" in lower or "timeout" in lower or "connection" in lower:
        return "Sin conexión, chabón. Fijate el WiFi."
    return "Algo salió mal. Probá de nuevo."

func _on_privacy_clicked(meta: Variant) -> void:
    OS.shell_open(str(meta))

func _on_forgot_clicked(_meta: Variant) -> void:
    FlowRouter.go_forgot_password()
```

### scripts/screens/ForgotPasswordScreen.gd
```gdscript
extends Control

@onready var email_input: LineEdit = $VBox/EmailInput
@onready var submit_button: Button = $VBox/Submit
@onready var status_label: Label = $VBox/Status
@onready var back_link: RichTextLabel = $VBox/BackLink

func _ready() -> void:
    submit_button.text = "Enviar reseteo"
    submit_button.pressed.connect(_on_submit)
    back_link.text = "[url=back]← Volver a Entrar[/url]"
    back_link.meta_clicked.connect(func(_m): FlowRouter.go_auth())
    status_label.visible = false
    email_input.placeholder_text = "tu@correo.com"

func _on_submit() -> void:
    var email := email_input.text.strip_edges()
    if email.length() == 0 or email.find("@") == -1:
        status_label.text = "Poné un email válido, chabón."
        status_label.visible = true
        return
    submit_button.disabled = true
    status_label.visible = false
    var res = await AuthManager.request_password_reset(email)
    submit_button.disabled = false
    # Anti-enumeration: show uniform success even if server says error
    # (server returns ok:true regardless per Plan 03 T-1-RT-02)
    status_label.text = "Si ese email está en la base, te llega un link en unos minutos. Revisá spam también."
    status_label.visible = true
    email_input.editable = false
    submit_button.disabled = true
```

### scripts/screens/SplashScreen.gd
```gdscript
extends Control

const APP_CFG := "user://app.cfg"
const MIN_SPLASH_MS := 800  # avoid jarring flash

@onready var disclaimer_label: Label = $VBox/Disclaimer
@onready var progress: ProgressBar = $VBox/ProgressBar
@onready var loading_label: Label = $VBox/LoadingLabel

func _ready() -> void:
    var cfg = ConfigFile.new()
    var has_seen = false
    if cfg.load(APP_CFG) == OK:
        has_seen = cfg.get_value("first_launch", "disclaimer_seen", false)
    disclaimer_label.visible = not has_seen
    _wait_and_route(has_seen)

func _wait_and_route(disclaimer_was_seen: bool) -> void:
    var start := Time.get_ticks_msec()
    var deadline := start + 3000
    while Time.get_ticks_msec() < deadline:
        if AuthManager.session != null or _has_attempted_restore():
            break
        await get_tree().create_timer(0.1).timeout
    if not disclaimer_was_seen:
        var cfg = ConfigFile.new()
        cfg.set_value("first_launch", "disclaimer_seen", true)
        cfg.save(APP_CFG)
    var elapsed := Time.get_ticks_msec() - start
    if elapsed < MIN_SPLASH_MS:
        await get_tree().create_timer((MIN_SPLASH_MS - elapsed) / 1000.0).timeout
    if AuthManager.is_authenticated():
        var res = await PlayerStore.load_from_server()
        if res.ok and PlayerStore.has_profile():
            FlowRouter.go_home()
        else:
            FlowRouter.go_club_picker()
    else:
        FlowRouter.go_auth()

func _has_attempted_restore() -> bool:
    return false
```

### scripts/autoloads/PlayerStore.gd
```gdscript
extends Node

signal profile_loaded
signal profile_cleared

var pibe_id: String = ""
var pibe_name: String = ""
var club_id: String = ""
var club_name: String = ""

func has_profile() -> bool:
    return pibe_id != ""

func clear() -> void:
    pibe_id = ""
    pibe_name = ""
    club_id = ""
    club_name = ""
    profile_cleared.emit()

func load_from_server() -> Dictionary:
    if not AuthManager.is_authenticated():
        return {"ok": false, "error": "Not authenticated"}
    var session = AuthManager.session
    var read_req = [
        {"collection": "players", "key": "profile", "user_id": session.user_id},
    ]
    var resp = await NakamaClient.client.read_storage_objects_async(session, read_req)
    if resp.is_exception():
        return {"ok": false, "error": str(resp.get_exception().message)}
    if resp.objects.size() == 0:
        return {"ok": false, "error": "no_profile"}
    var profile = JSON.parse_string(resp.objects[0].value)
    pibe_id = profile.get("pibe_id", "")
    club_id = profile.get("club_id", "")
    var pibe_resp = await NakamaClient.client.read_storage_objects_async(session, [
        {"collection": "pibes", "key": pibe_id, "user_id": session.user_id},
    ])
    if not pibe_resp.is_exception() and pibe_resp.objects.size() > 0:
        var pibe = JSON.parse_string(pibe_resp.objects[0].value)
        pibe_name = pibe.get("name", "")
    var club_resp = await NakamaClient.client.read_storage_objects_async(session, [
        {"collection": "clubs", "key": club_id, "user_id": "00000000-0000-0000-0000-000000000000"},
    ])
    if not club_resp.is_exception() and club_resp.objects.size() > 0:
        var club = JSON.parse_string(club_resp.objects[0].value)
        club_name = club.get("lunfardo_name", "")
    profile_loaded.emit()
    return {"ok": true}
```

### scripts/screens/ClubPickerScreen.gd
```gdscript
extends Control

const DIVISIONS := ["Todos", "Primera", "Nacional", "B Metro", "Federal A", "C Metro"]

@onready var search: LineEdit = $VBox/Search
@onready var chips_box: HBoxContainer = $VBox/ChipsScroll/Chips
@onready var scroll: ScrollContainer = $VBox/ClubScroll
@onready var list_box: VBoxContainer = $VBox/ClubScroll/List
@onready var cta: Button = $VBox/CTA
@onready var empty_state: VBoxContainer = $VBox/EmptyState

const ClubCardScene = preload("res://scenes/components/ClubCard.tscn")
const ChipButtonScene = preload("res://scenes/components/ChipButton.tscn")

var _current_division := "Todos"
var _selected_club_id := ""
var _selected_card
var _all_clubs: Array = []
var _search_debounce: float = 0.0

func _ready() -> void:
    cta.disabled = true
    cta.text = "Me banco este club"
    cta.pressed.connect(_on_cta)
    search.text_changed.connect(_on_search_changed)
    empty_state.visible = false
    _build_chips()
    _load_clubs()

func _build_chips() -> void:
    for div in DIVISIONS:
        var chip = ChipButtonScene.instantiate()
        chip.label_text = div
        chip.is_selected = (div == _current_division)
        chip.pressed.connect(func(): _on_chip_pressed(div, chip))
        chips_box.add_child(chip)

func _on_chip_pressed(div: String, chip: Node) -> void:
    _current_division = div
    for c in chips_box.get_children():
        c.is_selected = (c == chip)
    _render_clubs()

func _on_search_changed(_t: String) -> void:
    _search_debounce = 0.2
    set_process(true)

func _process(delta: float) -> void:
    if _search_debounce > 0.0:
        _search_debounce -= delta
        if _search_debounce <= 0.0:
            _render_clubs()
            set_process(false)

func _load_clubs() -> void:
    var session = AuthManager.session
    var payload = JSON.stringify({"division": "Todos", "page": 1})
    var resp = await NakamaClient.client.rpc_async(session, "get_clubs", payload)
    if resp.is_exception():
        push_error("[ClubPicker] get_clubs failed: %s" % resp.get_exception().message)
        return
    var data = JSON.parse_string(resp.payload)
    _all_clubs = data.get("clubs", [])
    var page = 2
    while data.get("has_more", false) and page < 10:
        var p = JSON.stringify({"division": "Todos", "page": page})
        var r = await NakamaClient.client.rpc_async(session, "get_clubs", p)
        if r.is_exception(): break
        data = JSON.parse_string(r.payload)
        _all_clubs.append_array(data.get("clubs", []))
        page += 1
    _render_clubs()

func _render_clubs() -> void:
    for c in list_box.get_children():
        c.queue_free()
    _selected_card = null
    _selected_club_id = ""
    cta.disabled = true
    var q := search.text.strip_edges().to_lower()
    var filtered: Array = []
    for club in _all_clubs:
        if _current_division != "Todos" and club.division != _current_division:
            continue
        if q.length() > 0:
            var name = str(club.get("lunfardo_name", "")).to_lower()
            var barrio = str(club.get("barrio_hq", "")).to_lower()
            if not (q in name or q in barrio):
                continue
        filtered.append(club)
    empty_state.visible = filtered.size() == 0
    scroll.visible = filtered.size() > 0
    for club in filtered:
        var card = ClubCardScene.instantiate()
        card.set_club(club)
        card.tapped.connect(func(): _on_club_tapped(card, club))
        list_box.add_child(card)

func _on_club_tapped(card: Node, club: Dictionary) -> void:
    if _selected_card != null:
        _selected_card.set_selected(false)
    _selected_card = card
    card.set_selected(true)
    _selected_club_id = str(club.get("id", ""))
    cta.disabled = false

func _on_cta() -> void:
    if _selected_club_id == "":
        return
    PlayerStore.club_id = _selected_club_id
    FlowRouter.go_pibe_creator()
```

### scripts/components/ClubCard.gd
```gdscript
extends PanelContainer

signal tapped

@onready var crest: ColorRect = $H/Crest
@onready var name_label: Label = $H/V/Name
@onready var division_label: Label = $H/V/Division

var club_data: Dictionary = {}

func set_club(club: Dictionary) -> void:
    club_data = club
    name_label.text = str(club.get("lunfardo_name", ""))
    division_label.text = str(club.get("division", ""))
    var colors = club.get("colors", {})
    var primary_hex = str(colors.get("primary", "#888888"))
    crest.color = Color(primary_hex)

func set_selected(sel: bool) -> void:
    var sb := get_theme_stylebox("panel").duplicate() as StyleBoxFlat
    sb.border_width_left = 2 if sel else 0
    sb.border_width_top = 2 if sel else 0
    sb.border_width_right = 2 if sel else 0
    sb.border_width_bottom = 2 if sel else 0
    sb.border_color = AppTheme.ACCENT
    add_theme_stylebox_override("panel", sb)

func _gui_input(event: InputEvent) -> void:
    if event is InputEventScreenTouch and event.pressed:
        tapped.emit()
    elif event is InputEventMouseButton and event.pressed and event.button_index == MOUSE_BUTTON_LEFT:
        tapped.emit()
```

### scripts/screens/PibeCreatorScreen.gd
```gdscript
extends Control

@onready var name_input: LineEdit = $VBox/NameInput
@onready var preview: Label = $VBox/Preview
@onready var error_label: Label = $VBox/ErrorLabel
@onready var hint_label: Label = $VBox/HintLabel
@onready var cta: Button = $VBox/CTA
@onready var stat_aguante: Label = $VBox/Stats/AguanteValue
@onready var stat_velocidad: Label = $VBox/Stats/VelocidadValue
@onready var stat_astucia: Label = $VBox/Stats/AstuciaValue
@onready var stat_carisma: Label = $VBox/Stats/CarismaValue

func _ready() -> void:
    name_input.max_length = 20
    error_label.visible = false
    hint_label.visible = false
    cta.text = "Así se llama mi pibe"
    cta.disabled = true
    stat_aguante.text = "50"
    stat_velocidad.text = "50"
    stat_astucia.text = "50"
    stat_carisma.text = "50"
    name_input.text_changed.connect(_on_text_changed)
    cta.pressed.connect(_on_submit)

func _on_text_changed(t: String) -> void:
    preview.text = t.strip_edges()
    error_label.visible = false
    hint_label.visible = t.length() >= 19
    cta.disabled = t.strip_edges().length() < 2

func _on_submit() -> void:
    error_label.visible = false
    cta.disabled = true
    var session = AuthManager.session
    var payload = JSON.stringify({
        "name": name_input.text.strip_edges(),
        "club_id": PlayerStore.club_id,
    })
    var resp = await NakamaClient.client.rpc_async(session, "create_pibe", payload)
    cta.disabled = false
    if resp.is_exception():
        var msg = resp.get_exception().message
        if "Ese nombre" in msg or "name" in msg.to_lower() or "Máximo" in msg:
            error_label.text = "Ese nombre no va. Elegí otro."
        elif "already exists" in msg.to_lower():
            error_label.text = "Ya tenés un pibe creado, chabón."
        else:
            error_label.text = "Algo salió mal. Probá de nuevo."
        error_label.visible = true
        return
    var pibe = JSON.parse_string(resp.payload)
    PlayerStore.pibe_id = str(pibe.get("id", ""))
    PlayerStore.pibe_name = str(pibe.get("name", ""))
    PlayerStore.club_id = str(pibe.get("club_id", ""))
    await PlayerStore.load_from_server()
    FlowRouter.go_tutorial()
```

### scripts/screens/TutorialScreen.gd
```gdscript
extends Control

@onready var cta: Button = $VBox/CTA

func _ready() -> void:
    cta.text = "Dale, empezamos"
    cta.pressed.connect(func(): FlowRouter.go_home())
```

### scripts/screens/HomeScreen.gd
```gdscript
extends Control

@onready var pibe_label: Label = $TopBar/PibeName
@onready var club_label: Label = $TopBar/ClubName
@onready var empty_heading: Label = $Content/Empty/Heading
@onready var empty_body: Label = $Content/Empty/Body
@onready var delete_button: Button = $Content/DeleteAccount

func _ready() -> void:
    if PlayerStore.pibe_name == "":
        await PlayerStore.load_from_server()
    pibe_label.text = PlayerStore.pibe_name if PlayerStore.pibe_name != "" else "Pibe"
    club_label.text = PlayerStore.club_name if PlayerStore.club_name != "" else "—"
    empty_heading.text = "Tu barra te espera."
    empty_body.text = "Empezá a laburar."
    delete_button.text = "Borrar mi cuenta"
    delete_button.pressed.connect(_on_delete)

func _on_delete() -> void:
    var dlg = ConfirmationDialog.new()
    dlg.dialog_text = "¿Seguro? Esto borra todo tu progreso y no se puede deshacer."
    dlg.ok_button_text = "Sí, borrá todo"
    dlg.cancel_button_text = "Cancelar"
    add_child(dlg)
    dlg.confirmed.connect(_perform_delete)
    dlg.popup_centered()

func _perform_delete() -> void:
    var session = AuthManager.session
    var resp = await NakamaClient.client.rpc_async(session, "delete_account", "")
    if resp.is_exception():
        push_error("[HomeScreen] delete_account failed: %s" % resp.get_exception().message)
        return
    AuthManager.logout()
    PlayerStore.clear()
    FlowRouter.go_splash()
```

### scripts/components/NavButton.gd
```gdscript
extends VBoxContainer

@export var label_text: String = ""
@export var is_active: bool = false :
    set(v):
        is_active = v
        _refresh()

@onready var dot: ColorRect = $Dot
@onready var icon: ColorRect = $Icon
@onready var label: Label = $Label

signal tapped

func _ready() -> void:
    label.text = label_text
    _refresh()

func _refresh() -> void:
    dot.visible = is_active
    label.add_theme_color_override("font_color", AppTheme.TEXT_PRIMARY if is_active else AppTheme.TEXT_SECONDARY)

func _gui_input(event: InputEvent) -> void:
    if event is InputEventScreenTouch and event.pressed:
        tapped.emit()
    elif event is InputEventMouseButton and event.pressed and event.button_index == MOUSE_BUTTON_LEFT:
        tapped.emit()
```

### Project.godot — autoload additions
```
PlayerStore="*res://scripts/autoloads/PlayerStore.gd"
FlowRouter="*res://scripts/autoloads/FlowRouter.gd"
```

**run/main_scene** must change from PlaceholderMain.tscn to SplashScreen.tscn in project.godot [application] section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autoloads (FlowRouter, PlayerStore) + components (ClubCard, ChipButton, NavButton) + Splash + Auth screens</name>
  <files>
    scripts/autoloads/FlowRouter.gd
    scripts/autoloads/PlayerStore.gd
    scenes/components/ClubCard.tscn
    scripts/components/ClubCard.gd
    scenes/components/ChipButton.tscn
    scripts/components/ChipButton.gd
    scenes/components/NavButton.tscn
    scripts/components/NavButton.gd
    scenes/SplashScreen.tscn
    scripts/screens/SplashScreen.gd
    scenes/AuthScreen.tscn
    scripts/screens/AuthScreen.gd
    project.godot
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md §Component Inventory (Splash, Auth, ClubCard, ChipButton, NavButton)
    - .planning/phases/01-foundation/01-02-SUMMARY.md (autoloads existentes + Theme.tres)
    - scripts/autoloads/AuthManager.gd (login/register API)
    - scripts/autoloads/AppTheme.gd (color tokens)
    - project.godot (existing [autoload] section)
  </read_first>
  <action>
    **1. `scripts/autoloads/FlowRouter.gd`** — copiar bloque `<interfaces>` LITERAL. Incluye el helper `go_forgot_password()` que rutea a ForgotPasswordScreen (Task 2 lo crea).

    **2. `scripts/autoloads/PlayerStore.gd`** — copiar bloque `<interfaces>` LITERAL.

    **3. `scenes/components/ChipButton.tscn` + `scripts/components/ChipButton.gd`** — Componente reutilizable per UI-SPEC §ClubPickerScreen ChipButton:
    - Root: PanelContainer
    - Child: HBoxContainer → Label
    - Script export: `label_text: String`, `is_selected: bool`
    - Unselected: bg `#2D2D2D`, border 1px `#3D3D3D`, text 14px Bold `#A0A0A0`, corner radius 16, padding 8px/8px, min_height 32
    - Selected: bg `#D62828`, no border, text `#F5F5F5`
    - Min touch width 64px
    - Signal `pressed` emit on `_gui_input` para InputEventScreenTouch + InputEventMouseButton LEFT pressed
    - Re-aplica StyleBoxFlat según `is_selected` en setter
    - Min size custom_minimum_size = Vector2(64, 32)

    **4. `scenes/components/ClubCard.tscn` + `scripts/components/ClubCard.gd`** — copiar `<interfaces>` `ClubCard.gd` LITERAL. Estructura .tscn:
    ```
    PanelContainer (root, bg #2D2D2D, corner radius 8, padding 16, min_height 64)
      HBoxContainer (name "H", separation 16)
        ColorRect (name "Crest", custom_minimum_size 40x40)
        VBoxContainer (name "V", separation 4)
          Label (name "Name", 16px Bold #F5F5F5)
          Label (name "Division", 14px Regular #A0A0A0)
    ```

    **5. `scenes/components/NavButton.tscn` + `scripts/components/NavButton.gd`** — copiar `<interfaces>` `NavButton.gd` LITERAL. Estructura .tscn:
    ```
    VBoxContainer (root, custom_minimum_size 64x56, alignment center)
      ColorRect (name "Dot", custom_minimum_size 4x4, color = AppTheme.ACCENT, visible toggleable)
      ColorRect (name "Icon", custom_minimum_size 24x24, color = AppTheme.TEXT_SECONDARY)
      Label (name "Label", 14px Bold)
    ```

    **6. `scenes/SplashScreen.tscn` + `scripts/screens/SplashScreen.gd`** — copiar `<interfaces>` `SplashScreen.gd` LITERAL. Estructura .tscn per UI-SPEC §SplashScreen:
    ```
    Control (root, full anchors, bg via ColorRect #1A1A1A o Theme inherit)
      ColorRect (background, full rect, color = AppTheme.DOMINANT)
      VBoxContainer (name "VBox", centered vertically, separation 24, top margin = safe_area)
        Label (name "Logo", text "BarraBrava", 28px Bold #D62828)
        Label (name "Disclaimer", text per UI-SPEC, 16px Regular #A0A0A0, autowrap, max width 600px)
        ProgressBar (name "ProgressBar", custom_minimum_size: full width, height 4, value 0)
        Label (name "LoadingLabel", text "Cargando...", 14px Bold #A0A0A0)
    ```
    Disclaimer copy EXACTO: `"Esto es una parodia de fútbol argentino. Cualquier parecido con personas reales es coincidencia."`

    **7. `scenes/AuthScreen.tscn` + `scripts/screens/AuthScreen.gd`** — copiar `<interfaces>` `AuthScreen.gd` LITERAL. Estructura .tscn per UI-SPEC §AuthScreen:
    ```
    Control (root)
      ColorRect (bg, color = AppTheme.DOMINANT)
      VBoxContainer (name "VBox", top padding 48, horizontal padding 24, separation 24)
        Label (text "BarraBrava", 22px Bold #F5F5F5)
        TabContainer (name "TabContainer", tab labels via set_tab_title)
          VBoxContainer (name "Entrar", spacing 16)
            LineEdit (name "Email", placeholder "tu@correo.com", min_height 44)
            LineEdit (name "Password", secret true, placeholder "contraseña", min_height 44)
            Button (name "Submit", text "Entrá", primary CTA style)
            Label (name "ErrorLabel", 16px Regular #E67E22, hidden, autowrap)
            RichTextLabel (name "ForgotLink", bbcode_enabled true, fit_content true, meta_underlined true,
                            text "[url=forgot]¿Olvidaste tu contraseña?[/url]", text color #A0A0A0)
          VBoxContainer (name "Registrarse", spacing 16)
            LineEdit (name "Email")
            LineEdit (name "Password", secret true)
            LineEdit (name "Confirm", secret true, placeholder "confirmá contraseña")
            RichTextLabel (name "PrivacyLink", bbcode_enabled true, fit_content true, meta_underlined true)
            Button (name "Submit", text "Armar mi cuenta")
            Label (name "ErrorLabel")
    ```
    Tab titles via script in `_ready()`: `tabs.set_tab_title(0, "Entrar"); tabs.set_tab_title(1, "Registrarse")`.

    El `ForgotLink` se conecta en `_on_forgot_clicked` → `FlowRouter.go_forgot_password()` (helper definido en FlowRouter creado en este task).

    LineEdit styling debe venir del Theme global sin overrides locales.

    **8. `project.godot`** — agregar dos líneas a `[autoload]`:
    ```
    PlayerStore="*res://scripts/autoloads/PlayerStore.gd"
    FlowRouter="*res://scripts/autoloads/FlowRouter.gd"
    ```
    Cambiar `run/main_scene` de `res://scenes/PlaceholderMain.tscn` a `res://scenes/SplashScreen.tscn`. NO eliminar PlaceholderMain.tscn.

    **Verificación local** (sin device):
    ```
    godot --headless --check-only --path . 2>&1 | head -50
    ```
    Debe terminar sin "ERROR" lines.

    Commit: `feat(01): wave3 splash + auth screens + autoloads + reusable components`
  </action>
  <verify>
    <automated>test -f scripts/autoloads/FlowRouter.gd && test -f scripts/autoloads/PlayerStore.gd && test -f scenes/SplashScreen.tscn && test -f scripts/screens/SplashScreen.gd && test -f scenes/AuthScreen.tscn && test -f scripts/screens/AuthScreen.gd && test -f scenes/components/ClubCard.tscn && test -f scripts/components/ClubCard.gd && test -f scenes/components/ChipButton.tscn && test -f scripts/components/ChipButton.gd && test -f scenes/components/NavButton.tscn && test -f scripts/components/NavButton.gd && grep -q "PlayerStore=\"\*res://scripts/autoloads/PlayerStore.gd\"" project.godot && grep -q "FlowRouter=\"\*res://scripts/autoloads/FlowRouter.gd\"" project.godot && grep -q "run/main_scene=\"res://scenes/SplashScreen.tscn\"" project.godot && grep -q "FlowRouter.go_auth\|FlowRouter.go_club_picker\|FlowRouter.go_home" scripts/screens/SplashScreen.gd && grep -q "go_forgot_password" scripts/autoloads/FlowRouter.gd && grep -q "AuthManager.login\|AuthManager.register" scripts/screens/AuthScreen.gd && grep -q "Olvidaste tu contraseña" scripts/screens/AuthScreen.gd scenes/AuthScreen.tscn && grep -q "Esto es una parodia de fútbol argentino" scripts/screens/SplashScreen.gd scenes/SplashScreen.tscn && grep -q "Entrá\|Armar mi cuenta" scripts/screens/AuthScreen.gd scenes/AuthScreen.tscn && grep -q "Código equivocado" scripts/screens/AuthScreen.gd && grep -q "disclaimer_seen" scripts/screens/SplashScreen.gd</automated>
  </verify>
  <acceptance_criteria>
    - FlowRouter.gd autoload con `go_splash/auth/forgot_password/club_picker/pibe_creator/tutorial/home` helpers + fade 150ms
    - PlayerStore.gd con `pibe_id, pibe_name, club_id, club_name`, signals + `load_from_server`
    - ChipButton.tscn renderiza unselected/selected per UI-SPEC; min touch width 64px
    - ClubCard.tscn estructura HBox con Crest + VBox(Name + Division) per UI-SPEC
    - NavButton.tscn con Dot + Icon + Label per UI-SPEC; 4 estados activo/inactivo correctos
    - SplashScreen.tscn muestra logo, disclaimer EXACTO, ProgressBar, LoadingLabel
    - SplashScreen.gd persiste `disclaimer_seen=true` en user://app.cfg y NO lo muestra en relaunch (CLB-02)
    - SplashScreen.gd ruta según `AuthManager.is_authenticated()` y `PlayerStore.has_profile()`
    - AuthScreen.tscn tiene TabContainer con tabs "Entrar" + "Registrarse" y LineEdits + Button + ErrorLabel
    - AuthScreen.tscn tab Entrar incluye RichTextLabel "ForgotLink" con texto "¿Olvidaste tu contraseña?" (CHK-02 D-03 UI entry point)
    - AuthScreen.gd conecta forgot_link.meta_clicked → FlowRouter.go_forgot_password()
    - AuthScreen.gd llama `AuthManager.login` / `AuthManager.register` y traduce errores a copy lunfardo del UI-SPEC
    - project.godot tiene PlayerStore + FlowRouter en [autoload] y main_scene = SplashScreen.tscn
    - Commit local realizado
  </acceptance_criteria>
  <done>
    Wave 3 task 1 listo: estructura autoloads + componentes + 2 primeras pantallas + UI entry point para forgot password. Task 2 crea ForgotPasswordScreen.
  </done>
</task>

<task type="auto">
  <name>Task 2: ForgotPasswordScreen + extend AuthManager.request_password_reset (D-03 closure)</name>
  <files>
    scenes/ForgotPasswordScreen.tscn
    scripts/screens/ForgotPasswordScreen.gd
    scripts/autoloads/AuthManager.gd
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (buscar §ForgotPasswordScreen si existe, sino diseñar consistente con AuthScreen)
    - .planning/phases/01-foundation/01-03-SUMMARY.md (RPC request_password_reset signature)
    - scripts/autoloads/AuthManager.gd (Plan 02 — existing login/register methods)
    - scripts/screens/AuthScreen.gd (verificar que conecta ForgotLink → go_forgot_password)
    - scripts/autoloads/FlowRouter.gd (verificar helper go_forgot_password existe)
  </read_first>
  <action>
    **1. Extender `scripts/autoloads/AuthManager.gd`** (creado en Plan 02) — agregar el método público `request_password_reset(email)`:

    Localizar el final del archivo AuthManager.gd y agregar:
    ```gdscript
    # Added in Plan 04 Task 2 (D-03 UI entry point — ForgotPasswordScreen)
    func request_password_reset(email: String) -> Dictionary:
        var payload = JSON.stringify({"email": email.strip_edges()})
        # Unauthenticated RPC — Nakama allows server key auth for public RPCs.
        # The SDK exposes rpc_async with null session for this case.
        var resp = await NakamaClient.client.rpc_async(null, "request_password_reset", payload)
        if resp.is_exception():
            return {"ok": false, "error": str(resp.get_exception().message)}
        return {"ok": true}
    ```

    **2. Crear `scripts/screens/ForgotPasswordScreen.gd`** — copiar `<interfaces>` `ForgotPasswordScreen.gd` LITERAL.

    **3. Crear `scenes/ForgotPasswordScreen.tscn`** — estructura mínima consistente con AuthScreen visual style:
    ```
    Control (root, bg ColorRect AppTheme.DOMINANT)
      VBoxContainer (name "VBox", padding 48 top, 24 horizontal, separation 24)
        Label (text "Recuperá tu contraseña", 22px Bold #F5F5F5)
        Label (text "Te mandamos un link para resetear la contraseña al email que pongas acá.", 16px Regular #A0A0A0, autowrap)
        LineEdit (name "EmailInput", placeholder "tu@correo.com", min_height 44, keyboard_type email)
        Button (name "Submit", text "Enviar reseteo", primary CTA style, min_height 44)
        Label (name "Status", 16px Regular #A0A0A0, autowrap, visible false)
        Control (size_flags_vertical EXPAND_FILL)  # spacer
        RichTextLabel (name "BackLink", bbcode_enabled true, fit_content true, meta_underlined true, text "[url=back]← Volver a Entrar[/url]", text color #A0A0A0)
    ```

    El comportamiento:
    - Submit con email vacío o sin "@" → muestra "Poné un email válido, chabón." (validación cliente mínima — server hace anti-enumeration de todos modos)
    - Submit válido → llama `AuthManager.request_password_reset(email)` → muestra "Si ese email está en la base, te llega un link en unos minutos. Revisá spam también." (UI uniform anti-enumeration response per T-1-RT-02 Plan 03)
    - Después de submit exitoso, deshabilitar el input + button para que no se pueda reenviar (rate limit awareness)
    - BackLink → `FlowRouter.go_auth()`

    **Verificación headless:**
    ```
    godot --headless --check-only --path . 2>&1 | head -50
    ```
    Sin errores.

    Commit: `feat(01): forgot password screen + AuthManager.request_password_reset (D-03 UI entry)`
  </action>
  <verify>
    <automated>test -f scenes/ForgotPasswordScreen.tscn && test -f scripts/screens/ForgotPasswordScreen.gd && grep -q "request_password_reset" scripts/screens/ForgotPasswordScreen.gd && grep -q "request_password_reset" scripts/autoloads/AuthManager.gd && grep -q "Enviar reseteo" scripts/screens/ForgotPasswordScreen.gd scenes/ForgotPasswordScreen.tscn && grep -q "Si ese email está en la base" scripts/screens/ForgotPasswordScreen.gd && grep -q "FlowRouter.go_auth\|FlowRouter.go_forgot" scripts/screens/ForgotPasswordScreen.gd</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/ForgotPasswordScreen.tscn` existe con VBox > Title + Description + EmailInput + Submit + Status + BackLink
    - `scripts/screens/ForgotPasswordScreen.gd` llama `AuthManager.request_password_reset(email)` al submit
    - Status label muestra mensaje uniforme anti-enumeration ("Si ese email está en la base...")
    - Submit disabled después de enviar (rate limit awareness)
    - BackLink rutea a AuthScreen vía FlowRouter.go_auth()
    - `scripts/autoloads/AuthManager.gd` tiene método `request_password_reset(email)` que llama RPC nakama
    - `godot --headless --check-only` exit 0
    - Commit local realizado
  </acceptance_criteria>
  <done>
    D-03 UI entry point cerrado. CHK-02 BLOCKER resuelto: el usuario puede acceder al reset password desde la app sin necesidad de curl manual.
  </done>
</task>

<task type="auto">
  <name>Task 3: ClubPicker + PibeCreator + Tutorial + HomeScreen (4 pantallas restantes del flow)</name>
  <files>
    scenes/ClubPickerScreen.tscn
    scripts/screens/ClubPickerScreen.gd
    scenes/PibeCreatorScreen.tscn
    scripts/screens/PibeCreatorScreen.gd
    scenes/TutorialScreen.tscn
    scripts/screens/TutorialScreen.gd
    scenes/HomeScreen.tscn
    scripts/screens/HomeScreen.gd
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md §ClubPickerScreen, §PibeCreatorScreen, §TutorialScreen, §HomeScreen, §Copywriting Contract
    - .planning/phases/01-foundation/01-03-SUMMARY.md (RPC payload shapes)
    - scripts/autoloads/PlayerStore.gd (cache contract)
    - scripts/components/ClubCard.gd (set_club / set_selected / tapped signal)
    - scripts/components/ChipButton.gd (label_text / is_selected / pressed signal)
    - scripts/components/NavButton.gd (label_text / is_active / tapped signal)
  </read_first>
  <action>
    **1. `scenes/ClubPickerScreen.tscn` + `scripts/screens/ClubPickerScreen.gd`** — copiar `<interfaces>` `ClubPickerScreen.gd` LITERAL. Estructura .tscn per UI-SPEC:
    ```
    Control (root)
      ColorRect (bg, AppTheme.DOMINANT)
      VBoxContainer (name "VBox", padding 24/16, separation 16)
        Label (text "Elegí tu club", 22px Bold #F5F5F5)
        LineEdit (name "Search", placeholder "Buscar club...", min_height 44)
        ScrollContainer (name "ChipsScroll", horizontal scroll only, custom_minimum_size.y 40)
          HBoxContainer (name "Chips", separation 8)
        ScrollContainer (name "ClubScroll", vertical, size_flags_vertical = EXPAND_FILL)
          VBoxContainer (name "List", separation 8)
        VBoxContainer (name "EmptyState", visible false, centered)
          Label (text "Nada por acá.", 22px Bold #F5F5F5)
          Label (text "Ese club no está en la liga. Buscá de nuevo.", 16px Regular #A0A0A0)
        Button (name "CTA", text "Me banco este club", primary CTA, disabled true initially, min_height 44)
    ```

    **2. `scenes/PibeCreatorScreen.tscn` + `scripts/screens/PibeCreatorScreen.gd`** — copiar `<interfaces>` `PibeCreatorScreen.gd` LITERAL. Estructura .tscn per UI-SPEC:
    ```
    Control (root, bg ColorRect AppTheme.DOMINANT)
      VBoxContainer (name "VBox", padding 24/24, separation 16)
        Label (text "Poné el nombre de tu pibe", 22px Bold #F5F5F5)
        LineEdit (name "NameInput", placeholder "Nombre del pibe", max_length 20, min_height 44)
        Label (name "Preview", 28px Bold #F5F5F5, autowrap)
        Label (name "HintLabel", text "Máximo 20 caracteres, chabón.", 16px Regular #E67E22, visible false)
        Label (name "ErrorLabel", 16px Regular #E67E22, visible false)
        ColorRect (separator, custom_minimum_size.y 1, color #3D3D3D, size_flags_horizontal EXPAND_FILL)
        Label (text "Stats base de tu pibe", 16px Regular #A0A0A0)
        GridContainer (name "Stats", columns 2, h_separation 16, v_separation 8)
          Label (text "Aguante", 14px Bold #A0A0A0)
          Label (name "AguanteValue", text "50", 14px Bold #F5F5F5)
          Label (text "Velocidad", 14px Bold #A0A0A0)
          Label (name "VelocidadValue", text "50", 14px Bold #F5F5F5)
          Label (text "Astucia", 14px Bold #A0A0A0)
          Label (name "AstuciaValue", text "50", 14px Bold #F5F5F5)
          Label (text "Carisma", 14px Bold #A0A0A0)
          Label (name "CarismaValue", text "50", 14px Bold #F5F5F5)
        Button (name "CTA", text "Así se llama mi pibe", disabled true, min_height 44)
    ```

    **3. `scenes/TutorialScreen.tscn` + `scripts/screens/TutorialScreen.gd`** — copiar `<interfaces>` `TutorialScreen.gd` LITERAL. Estructura per UI-SPEC §TutorialScreen:
    ```
    Control (root, bg ColorRect AppTheme.DOMINANT)
      VBoxContainer (name "VBox", padding 32/32, separation 24, alignment center vertical)
        Label (text "Bienvenido a la barra", 22px Bold #F5F5F5)
        RichTextLabel (text per UI-SPEC §TutorialScreen exact copy, 16px Regular #F5F5F5, fit_content true, autowrap)
        Control (size_flags_vertical EXPAND_FILL)
        Button (name "CTA", text "Dale, empezamos", min_height 44)
    ```
    Copy EXACTO del UI-SPEC: `"Tu pibe acaba de llegar al barrio. Laburá duro, ganate el respeto de la hinchada, y llevá a tu club a lo más alto.\n\nAcá manda quien más se la banca."`

    **4. `scenes/HomeScreen.tscn` + `scripts/screens/HomeScreen.gd`** — copiar `<interfaces>` `HomeScreen.gd` LITERAL. Estructura per UI-SPEC §HomeScreen:
    ```
    Control (root, bg ColorRect AppTheme.DOMINANT)
      VBoxContainer
        HBoxContainer (name "TopBar", padding 16, separation 8)
          Label (name "PibeName", 22px Bold #F5F5F5)
          Control (size_flags_horizontal EXPAND_FILL)
          Label (name "ClubName", 14px Bold #A0A0A0)
        ScrollContainer (name "Content", size_flags_vertical EXPAND_FILL)
          VBoxContainer (separation 24, padding 16)
            VBoxContainer (name "Empty", alignment center, separation 8)
              Label (name "Heading", text "Tu barra te espera.", 22px Bold #F5F5F5)
              Label (name "Body", text "Empezá a laburar.", 16px Regular #A0A0A0)
            Button (name "DeleteAccount", text "Borrar mi cuenta", min_height 44, custom style: bg AppTheme.DESTRUCTIVE)
        HBoxContainer (name "BottomNav", custom_minimum_size.y 56, bg ColorRect #2D2D2D, padding bottom = safe_area_bottom_min_34)
          NavButton (label "Inicio", is_active true)
          NavButton (label "Barra", is_active false)
          NavButton (label "Partidos", is_active false)
          NavButton (label "Perfil", is_active false)
    ```

    **5. Smoke local headless**:
    ```
    godot --headless --check-only --path . 2>&1 | tee /tmp/godot-check.log
    ```

    Commit: `feat(01): wave3 club picker + pibe creator + tutorial + home screens with full flow wiring`
  </action>
  <verify>
    <automated>test -f scenes/ClubPickerScreen.tscn && test -f scripts/screens/ClubPickerScreen.gd && test -f scenes/PibeCreatorScreen.tscn && test -f scripts/screens/PibeCreatorScreen.gd && test -f scenes/TutorialScreen.tscn && test -f scripts/screens/TutorialScreen.gd && test -f scenes/HomeScreen.tscn && test -f scripts/screens/HomeScreen.gd && grep -q "get_clubs" scripts/screens/ClubPickerScreen.gd && grep -q "Me banco este club" scripts/screens/ClubPickerScreen.gd scenes/ClubPickerScreen.tscn && grep -q "Nada por acá" scripts/screens/ClubPickerScreen.gd scenes/ClubPickerScreen.tscn && grep -q "create_pibe" scripts/screens/PibeCreatorScreen.gd && grep -q "Ese nombre no va" scripts/screens/PibeCreatorScreen.gd && grep -q "Así se llama mi pibe" scripts/screens/PibeCreatorScreen.gd scenes/PibeCreatorScreen.tscn && grep -v '^#' scripts/screens/PibeCreatorScreen.gd | grep -c '"stats"' | grep -q "^0$" && grep -q '"50"' scripts/screens/PibeCreatorScreen.gd scenes/PibeCreatorScreen.tscn && grep -q "Dale, empezamos" scripts/screens/TutorialScreen.gd scenes/TutorialScreen.tscn && grep -q "Bienvenido a la barra" scenes/TutorialScreen.tscn && grep -q "delete_account" scripts/screens/HomeScreen.gd && grep -q "Borrar mi cuenta" scripts/screens/HomeScreen.gd scenes/HomeScreen.tscn && grep -q "Tu barra te espera" scripts/screens/HomeScreen.gd scenes/HomeScreen.tscn && grep -q "ConfirmationDialog" scripts/screens/HomeScreen.gd</automated>
  </verify>
  <acceptance_criteria>
    - ClubPickerScreen.gd llama RPC get_clubs con paginación automática hasta has_more=false
    - ClubPickerScreen.tscn tiene Search + ChipsScroll + ClubScroll + EmptyState + CTA en ese orden
    - 6 chip divisions instanciadas: "Todos", "Primera", "Nacional", "B Metro", "Federal A", "C Metro"
    - PibeCreatorScreen.gd llama RPC create_pibe con payload {name, club_id} — sin stats (T-1-04)
    - PibeCreatorScreen.tscn muestra Aguante/Velocidad/Astucia/Carisma = 50 hardcoded
    - PibeCreatorScreen.gd traduce error server a "Ese nombre no va. Elegí otro."
    - TutorialScreen.tscn tiene copy "Bienvenido a la barra" + RichTextLabel + CTA "Dale, empezamos"
    - HomeScreen.gd carga PlayerStore profile y muestra pibe_name + club_name
    - HomeScreen.tscn tiene TopBar + Content + BottomNav con 4 NavButtons
    - HomeScreen.gd implementa flujo de delete_account con ConfirmationDialog + AuthManager.logout + PlayerStore.clear + FlowRouter.go_splash
    - `godot --headless --check-only` exit 0 sin ERROR lines
    - Commit local realizado
  </acceptance_criteria>
  <done>
    Todas las pantallas Godot Phase 1 implementadas. Flow end-to-end posible. CI rebuild produce APK para Task 4 checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificación humana — Flow onboarding end-to-end en device físico + forgot password UI</name>
  <what-built>
    - 7 pantallas Godot conectadas a los 5 RPCs server-side (Splash, Auth, ForgotPassword, ClubPicker, PibeCreator, Tutorial, Home)
    - 2 nuevos autoloads (FlowRouter con go_forgot_password, PlayerStore)
    - 3 componentes reutilizables (ClubCard, ChipButton, NavButton)
    - Disclaimer first-launch CLB-02
    - Delete account flow PRV-03
    - Auto-login determinístico
    - Forgot password UI entry point operativo (D-03)
  </what-built>
  <how-to-verify>
    **Pre-requisito:** Push del commit del task anterior a `main`. Esperar que GitHub Actions termine verde el job `build-android-debug` y descargar el APK del artifacts tab.

    **Test 1 — First launch experience (CLB-02):** [Como antes]

    **Test 2 — Registro nuevo (ONB-01):** [Como antes]

    **Test 3 — Club picker (CLB-01, ONB-02):** [Como antes]

    **Test 4 — Pibe creator (ONB-04, D-11, T-1-04):** [Como antes]

    **Test 5 — Tutorial (D-13):** [Como antes]

    **Test 6 — Home + auto-login (ONB-01):** [Como antes]

    **Test 7 — Delete account (PRV-03):** [Como antes]

    **Test 8 — Error mapping (ONB-01 errors):** [Como antes]

    **Test 9 — Visual contract (UI-SPEC compliance):** [Como antes]

    **Test 10 (NEW) — Forgot Password UI (D-03 CHK-02 fix):**
    1. En AuthScreen tab "Entrar", tap el link "¿Olvidaste tu contraseña?"
    2. Verificar fade a ForgotPasswordScreen
    3. Verificar el copy: "Recuperá tu contraseña" + descripción + EmailInput + Submit + BackLink
    4. Sin email → tap "Enviar reseteo" → muestra "Poné un email válido, chabón."
    5. Con email REAL (mismo que usaste en Test 2 — registro nuevo) → tap "Enviar reseteo"
    6. Verificar status: "Si ese email está en la base, te llega un link en unos minutos..."
    7. Verificar que el button + email input quedan deshabilitados (no se puede reenviar)
    8. Tap BackLink "← Volver a Entrar" → fade a AuthScreen
    9. **Confirmar end-to-end:** revisar la bandeja de email — debe llegar el email Resend con link al reset HTML (Plan 05). Confirmar que el link funciona (lleva al HTML de GitHub Pages). El test completo de reset → re-login se hace en checkpoint del Plan 05.

    **Test 11 (NEW) — Anti-enumeration (T-1-RT-02 client respect):**
    1. En ForgotPasswordScreen, ingresar un email que NO existe (ej `nadie-existe-en-esta-base@test.com`)
    2. Tap "Enviar reseteo"
    3. Verificar que el status muestra exactamente el MISMO mensaje uniforme "Si ese email está en la base, te llega un link..."
    4. NO debe aparecer "ese email no existe" o similar (cliente respeta la mitigation del server)

    Si los 11 tests pasan, escribí "approved". Si reportás issues, describí cuál test falló.
  </how-to-verify>
  <resume-signal>
    "approved" o descripción de los issues por test
  </resume-signal>
  <acceptance_criteria>
    - Tests 1-9 (flow original): pass
    - Test 10 forgot password UI navigable + form funcional + email recibido: pass
    - Test 11 anti-enumeration uniform response: pass
    - Usuario escribió "approved"
  </acceptance_criteria>
  <done>
    Flow onboarding completo end-to-end operativo en device físico. Phase 1 success criteria 2-4 cumplidos. D-03 UI entry point operativo (CHK-02 closed). Plan 05 cierra los outputs restantes (privacy policy hosting + AAIP + reset password HTML real funcional).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Godot client → Nakama RPCs | Toda la lógica de negocio (stats, validación de nombre, club existence, password reset email) está en server. Cliente solo presenta y envía inputs |
| LineEdit user input → RPC payload | El cliente envía `name`, `club_id`, `email` raw; server valida en sus respectivos RPCs |
| User device storage → app.cfg / session.cfg | Datos no sensibles (flag disclaimer_seen) + Nakama session token. Filesystem permission por OS sandbox |
| OS.shell_open → Privacy URL | El cliente abre un URL externo (Plan 05 hospeda el HTML) en el browser del sistema |
| ConfirmationDialog "Borrar mi cuenta" | Doble confirmación antes de delete_account RPC |
| ForgotPasswordScreen → request_password_reset RPC | Unauthenticated call (no session yet); server retorna ok:true uniformly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-UI-01 | Tampering | Client sends fake stats in create_pibe payload | mitigate | PibeCreatorScreen.gd construye payload con SOLO `{name, club_id}` — grep verifica que no hay `stats` field en el JSON.stringify. Server además ignora cualquier stats (defensa en profundidad — Plan 03 T-1-RT-01) |
| T-1-UI-02 | Information Disclosure | Session token logged in stdout | mitigate | NakamaClient.gd y AuthManager.gd NUNCA loggean `session.token` directo, solo `user_id` o length |
| T-1-UI-03 | Tampering | Client modifies club_id to point to non-existent club | mitigate | Server (Plan 03 create_pibe) verifica club existence vía storageRead colección clubs |
| T-1-UI-04 | Spoofing | Phishing via deep link to fake AuthScreen | accept | Phase 1 sin deep linking activo |
| T-1-UI-05 | Repudiation | Account deletion sin audit trail visible al user | mitigate | ConfirmationDialog explícito + server log "Account deleted user_id=X" |
| T-1-UI-06 | Information Disclosure | Privacy URL hardcoded to wrong domain | mitigate | URL inyectada via AppConfig.PRIVACY_URL + verificación visual |
| T-1-UI-07 | Denial of Service | User spams create_pibe button | mitigate | UI button.disabled = true durante RPC + server rate limit 60 rpc/user/min |
| T-1-UI-08 | Tampering | LineEdit accepts unicode injection chars | mitigate | Server-side validatePibeName regex strict (Plan 03) |
| T-1-UI-09 | Elevation of Privilege | User intenta crear segundo pibe en mismo account | mitigate | Server enforza "already exists" check |
| T-1-UI-10 | Information Disclosure | Profile data cached after logout | mitigate | AuthManager.logout() invoca PlayerStore.clear() siempre |
| T-1-UI-11 | Information Disclosure | ForgotPasswordScreen reveals if email exists | mitigate | Client muestra mensaje uniform "Si ese email está en la base..." regardless de la respuesta del server (que también es uniforme per Plan 03 T-1-RT-02) |
| T-1-UI-12 | Denial of Service | User spams Enviar reseteo button | mitigate | Submit button.disabled = true después del primer tap + email input deshabilitado. Server rate limit 60 rpc/min como backstop |
</threat_model>

<verification>
1. `godot --headless --check-only --path .` exit 0 sin ERROR
2. `grep -v '^#' scripts/screens/PibeCreatorScreen.gd | grep -c '"stats"'` == 0 (T-1-UI-01)
3. `grep -v '^#' scripts/screens/PibeCreatorScreen.gd | grep -c '"aguante"'` == 0 (stats NUNCA en payload del cliente)
4. `grep -c 'FlowRouter.go_' scripts/screens/*.gd` ≥ 7 (al menos 7 transiciones distintas incluyendo go_forgot_password)
5. `grep -q "disclaimer_seen" scripts/screens/SplashScreen.gd`
6. `grep -q "ConfirmationDialog" scripts/screens/HomeScreen.gd`
7. `grep -q "AuthManager.logout" scripts/screens/HomeScreen.gd`
8. `grep -q "PlayerStore.clear" scripts/screens/HomeScreen.gd`
9. `grep -q "request_password_reset" scripts/autoloads/AuthManager.gd scripts/screens/ForgotPasswordScreen.gd` (CHK-02 fix)
10. `grep -q "Olvidaste tu contraseña" scenes/AuthScreen.tscn scripts/screens/AuthScreen.gd` (CHK-02 fix)
11. `grep -q "go_forgot_password" scripts/autoloads/FlowRouter.gd scripts/screens/AuthScreen.gd` (CHK-02 fix)
12. UI-SPEC copy strings present (verified by automated grep in tasks 1-3)
13. Device install + 11-step smoke test passes (Task 4 checkpoint)
</verification>

<success_criteria>
- 7 pantallas Godot funcionales: Splash, Auth, ForgotPassword, ClubPicker, PibeCreator, Tutorial, Home
- 2 nuevos autoloads operativos: FlowRouter (transiciones fade + go_forgot_password) + PlayerStore
- 3 componentes reutilizables: ClubCard, ChipButton, NavButton
- Flow end-to-end registrable → club picker → pibe → tutorial → home en device físico
- Auto-login restaura sesión y rutea a HomeScreen directamente
- Delete account purges Nakama + local storage + vuelve a SplashScreen
- Disclaimer CLB-02 aparece solo en first launch
- Stats 50/50/50/50 server-assigned, NUNCA enviados desde cliente (T-1-UI-01)
- Forgot password UI entry point operativo: AuthScreen link → ForgotPasswordScreen → RPC request_password_reset → email recibido (D-03 closure)
- Copy lunfardo exacto del UI-SPEC en todas las pantallas
- Theme global aplica colores Nunito sin overrides locales
- CI rebuild produce APK que funciona en device
</success_criteria>

<output>
Crear `.planning/phases/01-foundation/01-04-SUMMARY.md` con:
- Screenshots de las 7 pantallas (capturadas del device durante checkpoint)
- Output del `godot --headless --check-only`
- Lista de archivos creados
- Decisiones de implementación
- Issues encontrados durante el checkpoint + cómo se resolvieron
- Confirmación de CHK-02 fix: ForgotPasswordScreen operativo, no más curl manual required
- Próximos pasos para Plan 05
</output>
